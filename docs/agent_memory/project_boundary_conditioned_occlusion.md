---
name: project-boundary-conditioned-occlusion
description: "OSN-GS active direction reset (2026-07-23): from global visible-surface decomposition to boundary-conditioned occluded-surface construction; Phase A-F done + Gate B/C/D/E approved + Gate F reported, next approval scope is Phase G only"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3ab75852-24f1-41ab-9e47-206246eb025c
  modified: 2026-07-24T05:47:25.293Z
---

**Direction reset, 2026-07-23.** User retired global visible-surface-component-decomposition remediation (Phase 1 topology/connectivity fixes on `curved_annulus`/`mild_curved_sheet`/crease/parallel/disconnected scenes, see [[project_boundary_first_phase1]]) as an over-strong upstream requirement. New governing doc: `docs/Urgent_Work/OSN_GS_Direction_Reset_Plan.md` (top-level methodology, supersedes the old Phase 1-5 plans as strategy, does NOT delete them). Active implementation gate: `docs/Urgent_Work/OSN_GS_Boundary_Conditioned_Occlusion_Impl_Plan.md`.

**New target flow:** `certain Gaussian -> visible NURBS -> occluded NURBS -> uncertain Gaussian proposal` (one-way, unchanged from [[project_osn_gs_direction]]). Global component correctness is explicitly NOT a prerequisite anymore — local visible-patch boundaries plus patch-boundary reconciliation plus parametric continuation domains drive occluded-region *candidates*, evaluated by constrained-NURBS-chart validity/uncertainty rather than gated upfront by facing/normal-similarity strictness.

**Phases A-H, each with its own user-approval gate (no auto-advance), per the impl plan:**
- **A (NURBS/boundary interface) — done.** `TorchNURBSSurface` gained read-only knot access + analytic second-derivative API (`evaluate_with_second_derivatives`, S/S_u/S_v/S_uu/S_uv/S_vv). New `osn_gs/surface/torch_patch_boundary.py`: stable ordered/oriented `PatchBoundarySegment` records (states: unclassified/reconciled_internal/unsupported/extension_candidate) with inner isocurve, tangent/normal, deterministic ID.
- **B (artificial patch-boundary reconciliation) — isolated prototype done, Gate B reviewed and approved.** New `osn_gs/surface/torch_boundary_reconciliation.py` + generic coupled solver (`SharedBoundaryConstraint`, `fit_coupled_patch_graph_lsq()` in `torch_nurbs.py`, separate from and non-modifying of production `fit_coupled_wedge_ring_lsq()`). Verified: orthogonal boundary pair (~90° normal mismatch) still reconciles cleanly — direct proof normal similarity is NOT used as a hard gate. Disconnected-gap negative control correctly stays `unclassified`. Full detail + numbers: `docs/worklogs/75_boundary_conditioned_phase_ab.md`.
- **C (observation evidence / free-space query) — done, Gate C follow-up applied, awaiting final user re-review.** New `osn_gs/surface/torch_observation_evidence.py`. Per-view 5-state classification (`known_free_space`/`on_observed_surface`/`behind_first_observed_surface`/`unobserved`/`outside_valid_view`) is a DIFFERENT enum from the aggregate 5-state cross-camera classification (`known_free_space`/`occluded_candidate`/`unobserved`/`outside_valid_view`/`conflicting_evidence`) — user explicitly rejected a first draft that (a) named the "farther than first hit" state `behind_observed_surface` (too strong a claim — renamed to `behind_first_observed_surface`), (b) folded the depth-epsilon tie band into "behind" (added a dedicated `on_observed_surface` state instead), (c) let any single camera's "behind" verdict dominate aggregation (now preserves disagreement via `conflicting_evidence` + always-populated per-camera index lists), (d) inverted CUDA's invalid (zero) depth via bare `1/depth.clamp_min(eps)` before masking (now masks-then-inverts), (e) used one `coverage_threshold` across backends despite CUDA having no real coverage fraction (now `CameraViewEvidence.coverage_kind`/`depth_kind`/`depth_is_approximate` per backend). Fallback renderer's OWN depth is a pre-existing defect (never applies camera transform, uses raw world-frame z) — Phase C bypasses it entirely via a new evidence-only `_fallback_view_depth()` that reruns the same accumulation formula on camera-projected coordinates, without touching production `fallback_render`. Two renderer files (`torch_fallback.py`, `gaussian_rasterizer.py`) got additive-only new dict keys (`alpha`, `valid_depth_mask`) — verified safe via caller grep, no existing key changed. Full detail: `docs/worklogs/77_observation_evidence_phase_c.md`.
  - **Gate C follow-up round 1 (same day, worklog 78)**: user approved the above but withheld final sign-off pending two fixes. (1) `free_space_confirmed_by` + `on_surface_in` co-occurrence now also aggregates to `conflicting_evidence` (reused the existing state rather than adding a 6th one). (2) `evidence_cache_key()` signature changed from `(topology_version: str, camera_set_version: str)` to `(evidence: ObservationEvidence)` and now folds in `near`/`far`/`depth_epsilon` and each view's `backend_source`/`depth_kind`/`coverage_kind`; `_topology_version` now hashes `get_scaling`/`get_rotation`/`get_opacity` too, `_camera_set_version` now hashes `full_proj_transform` too. 10/10 tests, full suite 234 passed/1 skipped.
  - **Gate C follow-up round 2 (same day, worklog 79)**: user withheld sign-off AGAIN on round 1, asking to check what was already done before redoing anything (lesson: always re-read current code before acting on a multi-part correction — most of the camera-fingerprint field list was already satisfied, only the identity-fallback piece was missing; see [[feedback_verify_before_reacting]]). Changes: (1) round 1's "fold on-surface into conflicting_evidence" was incomplete — user wanted a full state machine: on-surface ALONE is its own new aggregate `STATUS_ON_OBSERVED_SURFACE`; on-surface co-occurring with EITHER free OR behind (or both) is `conflicting_evidence`. Invariant now documented: a `SampleEvidence` with non-empty `on_surface_in` can never be `known_free_space` or `occluded_candidate`. (2) `_camera_set_version` now also includes an explicit `camera_index=N` token per camera. (3) real bug found and fixed: `_tensor_digest` hashed only raw bytes, not shape/dtype. (4) `evidence_cache_key`'s docstring now explicitly says POST-BUILD result fingerprint. New fixture `_on_surface_multi_view_scene` needed real numerical iteration: since the sample point IS on Wall A, ANY camera aimed at it necessarily also sees Wall A at that pixel (fallback renderer has no true depth-ordered occlusion) — getting a clean "behind" reading from a second camera required a much-higher-opacity (0.95 vs 0.1) near occluder to dominate the naive blend. 14/14 tests, full suite 238 passed/1 skipped.
  - **Gate C FINAL APPROVAL (same day)**: user approved Gate C outright. Two non-blocking notes recorded for later: (a) two fully-duplicate cameras swapped in list order can still get the same fingerprint — not considered a problem since the observed content is identical; (b) a pure aggregation truth-table test fed synthetic per-view payloads directly (bypassing the renderer fixture) would be good to add eventually, but is explicitly NOT a precondition for starting Phase D. Production pipeline/trainer integration remains unapproved.
- **D (Parametric Continuation Domain) — IMPLEMENTED, Gate D reported.** User asked for a design document first, not code — audited `torch_nurbs.py` (full derivative/knot API) and `torch_annulus_chart.py` (Jacobian-SVD diagnostics) plus two pre-reset extension-design docs before writing. Key audit findings (still valid): knot vectors are hardcoded to `[0,1]` clamped uniform B-splines with no domain-extension helper; `PatchBoundarySegment` carries only an integer `patch_id`, not the actual `TorchNURBSSurface` reference (caller must supply that mapping); `predict_torch_occlusion_curves`/`build_torch_surface`/`sample_torch_occluded_surface` are self-documented pre-reset "Stage 2 legacy" with zero call sites — not reused.
  - **Round 1 design** (worklog 80 §1-6): `ContinuationDomain` seeded from analytic `S_u/S_v/S_uu/S_uv/S_vv`, outward direction via a UV-space least-squares perpendicular solve disambiguated by `inner_uv`, states `candidate/degenerate/rejected`, a continuous `evaluate(s,t)` closed-form API as "source of truth" alongside a cached grid.
  - **Round 2 corrections (worklog 80 §7, all applied)** — user approved the big direction (sampled strip only, no final NURBS chart, pairing is Phase E's job, first-order canonical geometry, no Phase C/production hookup) but rejected round 1's specifics on 11 points. Key fixes, worth remembering as lessons: (1) the UV-space least-squares perpendicular formula for outward direction was WRONG — UV parameterization skew/scale doesn't preserve perpendicularity into world space. Replaced with a pure world-space formula: `N = normalize(S_u × S_v)`, `C = normalize(N × T)` (T = world tangent via world-arclength finite differencing), sign chosen by dot product with `inner_world` direction — this is now the only outward-direction method, and UV-projection (least-squares onto `[S_u S_v]`, renormalized so `‖Jq‖=1`) is used ONLY afterward, only for projecting into UV space to evaluate the curvature diagnostic, never to determine direction itself. (2) `s` must be cumulative world arclength specifically — not sample index, not normalized parameter — for density-independent derivatives. (3) Sampled grid (`world`/`s_world`/`t_world`/`tangent_s`/`tangent_t`) is the ONLY source of truth — no continuous analytic `evaluate(s,t)` API, since `PatchBoundarySegment` itself only has discrete samples, not a continuous curve; an optional piecewise-linear `interpolate_boundary_arclength()` helper is explicitly non-analytic. (4) Second-order info is diagnostic-only (`curvature_growth_ratio`) — canonical `world` position is ALWAYS first-order, no curvature-aware position variant. (5) Renamed `inner_distance_median` reuse as extent into three separate concepts: `inner_probe_distance` (sign-selection only) / `local_surface_scale` (robust median over multiple finite-positive scale candidates — boundary spacing, inner-isocurve distance, control-net edge length) / `continuation_extent` (`= extent_multiplier * local_surface_scale`). (6) States renamed `candidate/degenerate/rejected` → `valid/degenerate/rejected` to avoid colliding with Phase E's own "candidate" concept. (7) Input-contract violations (reconciled_internal boundary, patch_id mismatch, shape mismatch, below-minimum sample count) now raise `ValueError` eagerly — a SEPARATE, non-raising category (Jacobian collapse, orientation inconsistency, curvature growth) returns `state=degenerate` with masks instead of NaN (`normal_valid_mask`/`direction_valid_mask`/`sample_valid_mask`). (8) Added minimum-sample-count contract (open ≥3, closed ≥4 unique samples). (9) Split the planned Jacobian-diagnostics-refactor prerequisite into TWO helpers, not one: `compute_parametric_jacobian_metrics` (shared with annulus) and `compute_orientation_consistency` (topology-specific wrapper owns it, since annulus rings are cyclic and continuation strips generally aren't) — and relaxed the refactor's regression bar from byte-identical to "same state classifications + same report fields + tolerance-equal numbers."
  - Edited only the two existing docs (`OSN_GS_Phase_D_Continuation_Domain_Design.md`, `OSN_GS_Boundary_Conditioned_Occlusion_Impl_Plan.md`) plus an appended §7 on worklog 80 — no new worklog file, per explicit instruction. Also used this pass to fix impl-plan-doc staleness the user caught: Phase B's status line still said "awaiting Gate B review" and Phase C's still said "awaiting final re-review" even though both were already approved — and compressed §11's growing approval-history narrative into a canonical-state table + links.
  - **Round 3 (worklog 80 §8, all applied)** — user approved the big direction AND round 2's core math contracts (world-space outward formula, sampled-grid source of truth, first-order-only geometry, `valid/degenerate/rejected` state names) but still withheld implementation approval pending 5 more document fixes: (1) Closed-loop arclength completed — `s_world[i]` is cumulative length to sample i ONLY, closing-segment length lives in a new separate field `boundary_length` (open: `s_world[-1]`; closed: `s_world[-1] + closing segment`); periodic central differencing at wrap points offsets neighbor "periodic s" by ± `boundary_length`. Canonical representation is duplicate-free ordered unique samples — discovered `torch_patch_boundary.py`'s `_canonicalize_closed_loop` actually DOES store a duplicate closing vertex (`ordered + [ordered[0]]`), so `build_continuation_domain` must strip it first as a normalization step (§2.1), not a validity check. (2) Added explicit adjacent-duplicate/zero-length-segment rejection: any adjacent sample pair (including the closed-loop closing segment) with world distance ≤ a new `arclength_epsilon` param (default `1e-6`) raises `ValueError` — no auto-dedup/repair, and specifically NOT just an eps-clamp (which would silently produce a garbage huge tangent). (3) Introduced `ContinuationDomainBuildError(RuntimeError)` as a THIRD category distinct from both `ValueError` (input contract violations) and `state=degenerate/rejected` (post-grid quality issues) — reserved for cases where a `ContinuationDomain` object literally cannot be constructed (e.g. automatic `local_surface_scale` derivation failing, or no tangent/direction derivable anywhere on the boundary). Fixed a real ambiguity: the round-2 doc read as if scale-derivation failure produced a "complete" `state=rejected` object, which was wrong — no object exists in that case now. (4) Fixed `local_surface_scale`'s canonical aggregate formula precisely (previously left vague as "some robust aggregate"): `L_boundary` = median positive adjacent boundary segment lengths (closed includes closing segment), `L_inner` = median positive `‖inner_world - boundary_world‖`, `L_control` = median positive adjacent control-grid edge lengths (u and v directions) from the source NURBS `control_grid`; `valid_scales` = the finite-and-positive subset of `{L_boundary, L_inner, L_control}`; `local_surface_scale = median(valid_scales)`; requires `len(valid_scales) >= 2` or raises `ContinuationDomainBuildError`. (5) Renamed `curvature_growth_ratio`/`curvature_displacement_at_t_max` to `second_order_growth_ratio`/`second_order_displacement_at_extent` with an explicit docstring warning that this is a directional continuation diagnostic, NOT an intrinsic mean/Gaussian/normal/geodesic curvature estimate — avoids misleading a reader who knows differential geometry. (6) Added a SUPERSEDED banner to the top of worklog 80 warning that §1-6 (revision 1) is historical only, and that even §7 (revision 2) was partially re-revised by §8 — always check the design doc itself as the canonical source, not just the latest worklog section. Edited only the three named files (design doc, impl plan doc, worklog 80 itself) — no new files, per explicit instruction each round.
  - **IMPLEMENTED (same day, worklog 81)** — user said "D 구현 시작해" (start Phase D implementation), the explicit go-ahead withheld across all three design rounds. Prerequisite refactor: new `osn_gs/surface/torch_parametric_diagnostics.py` (`compute_parametric_jacobian_metrics` — true `J^T J` closed-form eigenvalues from raw derivative tensors; `compute_orientation_consistency` — single-reference median-sign flip check, reused as-is from annulus's existing algorithm) extracted from `torch_annulus_chart.py`'s `_jacobian_diagnostics`, which now delegates to both (`tests/test_annulus_chart.py` 48/48 still pass, numerically identical — faithful extraction, not a rewrite). Core module `osn_gs/surface/torch_continuation_domain.py` implements the design exactly: `ContinuationDomain` dataclass, `STATE_VALID/DEGENERATE/REJECTED`, `ContinuationDomainBuildError`, `build_continuation_domain()`, `interpolate_boundary_arclength()`. **Real design-vs-implementation gap caught while writing tests**: design §2.3 says "no valid direction anywhere on the boundary" must raise `ContinuationDomainBuildError`, but the first implementation pass silently fell through to returning a `state=rejected` object instead — caught by a test that made ALL boundary samples degenerate and got an object back instead of the expected exception. Fixed by adding an explicit `if not direction_valid_mask.any(): raise ContinuationDomainBuildError(...)` before grid construction. `tests/test_continuation_domain.py` 22/22 pass. One fixture-construction lesson: a "flat plane" `TorchNURBSSurface` built with `degree=2` and evenly-spaced (not true Greville-abscissae) control points is geometrically flat but NOT linearly parameterized — gives a nonzero second-order diagnostic despite lying in a plane; had to use `degree=1` for the exact-zero test case (a nice real-world confirmation of why the design insisted this diagnostic is NOT "intrinsic curvature"). Full suite: pytest 260 passed/1 skipped (was 238 pre-Phase-D), unittest 261 OK. Master-plan Gate D reported in worklog 81. Not connected to Phase C evidence, Phase E, Phase F, or any production path.
  - **Gate D APPROVED (same day).** User approved Gate D outright with the checklist confirmed. Impl-plan doc updated: top status line → "Gate B/C/D 승인 완료", §11 table Phase D row → "완료, Gate D 승인". Worklog 81 kept as the final Phase D implementation record; NO separate Gate-D-approval worklog created (per explicit instruction).
- **E (High-Recall Bounded Candidate Builder) — DESIGN SPECCED, code not started.** User first asked for a conversational audit report only (no doc), then approved a specific methodology and asked for detailed design + impl-plan update (still no code). New design doc `docs/Urgent_Work/OSN_GS_Phase_E_Bounded_Candidate_Design.md`; impl-plan §7 compressed into the canonical summary + pointer. Locked design decisions: (1) Initial scope is PAIRWISE two-sided only (`ContinuationDomain A + B → OccludedRegionCandidate`); 3+-sided multi aggregation and one-sided are out of scope. (2) TWO-module split so the geometric builder never imports evidence: `torch_occluded_region_candidate.py` (`build_geometric_region_candidates`, owns `OccludedRegionCandidate`, no `torch_observation_evidence` import) + `torch_candidate_evidence.py` (`validate_candidate_observation_evidence`, applies Phase C evidence AFTER geometric candidates exist). (3) Broad phase = NEW surface-agnostic `torch_aabb_broad_phase.py` (sweep-and-prune), deliberately NOT rewiring the deprecated Stage 2 `torch_surface_candidate_graph.py` onto it (regression-safety; ~40-line duplication accepted as temporary). (4) `ContinuationDomain.state` input policy: valid=pair, degenerate=pair-but-record-provenance, rejected=exclude. (5) Narrow phase = mutual-nearest correspondence between valid samples + s-continuity connected components; per-edge soft evidence (outward/normal/tangent dots) but NEVER hard gates. (6) `OccludedRegionCandidate.state ∈ {candidate, unsupported, rejected}` — must NOT be conflated/promoted with `ContinuationDomain`'s `{valid, degenerate, rejected}`. (7) Known-free-space hard reject ONLY when ALL bridge sections are known_free_space-only with zero behind/on-surface/unobserved/conflicting; partial free space preserved not rejected. (8) empty-voxel `no_observed_support` + `conflicting_evidence` = metadata only, never auto approve/reject. (9) thresholds (correspondence, broad-phase expand) are configurable + sweep-tunable, NOT fixed constants; no hidden weighted score / early scalarization. (10) deterministic candidate_id = canonical domain-pair key + correspondence-component identity; multiple components from one pair stay distinct. 18-fixture table + Gate-E scale metrics specced. Design-stage worklog deliberately NOT written (worklog is post-implementation only, per instruction).
  - **E IMPLEMENTED (2026-07-24, worklog 82), Gate E reported.** User approved the design direction + 6 mandatory corrections and authorized proceeding through implementation without re-approval. Three new modules: `torch_aabb_broad_phase.py` (surface-agnostic sweep-and-prune, deliberately NOT importing/rewiring deprecated Stage 2 `torch_surface_candidate_graph.py` — regression-safe, Stage 2 tests 11/11 unchanged), `torch_occluded_region_candidate.py` (`build_geometric_region_candidates` + `OccludedRegionCandidate`/`SupportChain`/`CorrespondenceEdge`/`ConflictEdge` + `build_candidate_conflicts`; verified via grep it does NOT import `torch_observation_evidence`), `torch_candidate_evidence.py` (`validate_candidate_observation_evidence`, the ONLY Phase E module importing evidence). Key implemented corrections vs first design: (1) coplanar-close-pair is NOT a negative control — Phase E can't distinguish "artificially separated visible surface" from "narrow occluded region between coplanar boundaries" geometrically, so coplanarity/small-gap/normal-similarity/facing are never hard rejects; true structural negatives are zero-connector-separation / zero-support-interval / duplicate-same-source-domain / leaked reconciled_internal. (2) correspondence is `(s,t)`-index based: dedup same-`(s_a,s_b)` to min normalized distance, greedy 1:1 match per-s (mutual-nearest→normalized-distance→index), split into monotonic-in-s_b components (branch→separate candidate), closed uses modulo `_signed_step` with no duplicate seam edge. (3) bridge cells = adjacent correspondence pairs `[A_i,A_{i+1},B_{i+1},B_i]`, zero-area→rejected, self-intersection deferred to F. (4) evidence splits `support_endpoint_evidence` vs `bridge_interior_evidence`; interior-ONLY hard reject (all evaluable interior known_free_space, none behind/on/unobserved/conflicting, ≥1 evaluable nondegenerate section, ALL such sections contradiction); no-interior-evidence section = `insufficient_evidence` not reject; partial free preserved; endpoint on_observed_surface never offsets interior contradiction. (5) geometry source of truth is `ContinuationDomain.world`/`sample_valid_mask`/`closed` ONLY (confirmed `closed` field already exists on the dataclass — no field addition needed); `boundaries_by_id` for provenance/ID-validation only. (6) `OccludedRegionCandidate.state ∈ {candidate, unsupported, rejected}`, never copied/promoted from `ContinuationDomain.state`. empty-voxel `no_observed_support` + `conflicting_evidence` = metadata only. Provisional thresholds (broad_phase_expand_factor=1.5, correspondence_threshold=1.0, interior_samples=3) are sweep-tunable, NOT fixed constants — raw + threshold-used both preserved. Tests: `test_occluded_region_candidate.py` 22 + `test_candidate_evidence.py` 8, covering all 26 design fixtures. Full suite 290 passed/1 skipped (was 260 pre-E). Design doc `OSN_GS_Phase_E_Bounded_Candidate_Design.md` + impl-plan §7 updated with the corrections; NO design-stage worklog (worklog is post-impl only, per instruction). Not wired to Phase F/production.
  - **Gate E APPROVED (2026-07-24).** Impl-plan Phase E row → "구현·검증 완료, Gate E 승인". Also cleaned up `docs/Urgent_Work/` per user: deleted the two now-retired Boundary-First docs (`OSN_GS_Final_Boundary_First_NURBS_Direction.md`, `OSN_GS_Phase5_Boundary_Aligned_Extension_Plan.md` — superseded by the direction reset; completed experiments live in worklogs 39–56; production code path preserved) and fixed the stale README "governing plan" section. Urgent_Work now holds only 4 active docs (Direction Reset Plan, Boundary-Conditioned Occlusion Impl Plan, Phase D Design, Phase E Design).
- **F (Minimal Constrained Occluded NURBS Bridge) — IMPLEMENTED (2026-07-24, worklog 83), Gate F reported.** User approved a code/API audit, confirmed the full contract, and authorized design+implementation in one pass. Three new modules: `torch_coons_patch.py` (surface-agnostic bilinear transfinite seed), `torch_constrained_chart_lsq.py` (`solve_constrained_chart` — reuses `_lsq_normal_system`/`_second_difference_penalty` from torch_nurbs; does NOT modify them or `fit_coupled_patch_graph_lsq`), `torch_occluded_chart.py` (`fit_occluded_chart`, `OccludedChartResult`, `OccludedChartFitConfig`). Converts one Phase E `OccludedRegionCandidate(state=candidate)` → real occluded `TorchNURBSSurface`. Locked design decisions: (1) scope = open pairwise quadrilateral ribbon ONLY; cyclic candidate (connector_end None) → `state=unsupported, reason="cyclic_topology_deferred"` (no forced unrolling). (2) **chart spans the two continuation-supported matched chains (`world[s, t_selected]`), NOT back to the visible boundary `t=0`** — going back to t=0 would fabricate a wider region than the Phase D/E bounded candidate; visible boundary kept as provenance/validation reference only. (3) geometry source of truth = `ContinuationDomain` (needs a `domains_by_id` registry — candidate alone only has matched-tip samples + correspondence; `closed` field already existed). (4) solver = Coons transfinite seed + new single-chart constrained LSQ: support samples high-weight data, connector samples low-weight (`support_weight=1.0 >> connector_weight=0.03`), interior `_second_difference_penalty` fairness + Coons-seed tikhonov, single solve (weights=1 → linear in control pts). (5) C0 hard (boundary conformance chamfer residual vs tolerance = rel × local_surface_scale), G1/G2 soft diagnostic only (undirected angle stats; chart never forced to follow either visible patch normal). (6) evidence used for post-fit validation/metadata/rejection provenance only, NEVER as solver weight. (7) `state ∈ {fitted, validated, unsupported, rejected}` — deliberately NOT `candidate`/`chart` (Phase E collision). (8) hard reject: non-finite, jacobian collapse (`min_area_jacobian`/`sigma_min_normalized ≤ eps`), orientation flip, C0 > tolerance, zero-area, Phase E full known-free contradiction. self-intersection/penetration = `checked=False` + cheap AABB/normal-flip proxies only (deferred to Phase F+1). Key test lesson: Phase E nearest-correspondence CANNOT produce a folded/crossed ribbon (it always un-crosses by matching nearest), so fold/orientation-flip fixtures needed a MANUALLY-constructed candidate with a crossed `(s_a=k ↔ s_b=N-1-k)` correspondence — itself a proof that Phase E only emits sane candidates. Tests: `test_occluded_chart.py` 21 passing (all 21 design fixtures + Coons). Full suite 311 passed/1 skipped (was 290 pre-F). Design doc `OSN_GS_Phase_F_Constrained_Occluded_Chart_Design.md`; impl-plan §8/§11 updated. Not wired to Phase G/production.
- **G-H (uncertainty/Gaussian proposal, production adoption)** — not started. Next pre-authorized request scope is **Phase G — Uncertainty와 Gaussian Proposal** — do not skip ahead; also do not start global candidate selection/ranking, multi-sided topology, or production integration without separate approval.

**Standing constraints carried into the new direction (do not violate):**
- No production/main-training-path wiring for any of this yet — everything stays in isolated benchmark/prototype code until Phase H is separately approved.
- Facing/normal-similarity/tangent-alignment/curvature-agreement are soft evidence only, never hard rejection gates.
- No voxel-ID-only auto-merge of adjacent patches; no absolute-distance-only threshold; no scene-specific merge rules.
- No post-hoc boundary/control-point overwrite after independent fit (this exact pattern was already proven to hurt accuracy in Phase 4 hardening, see [[project_boundary_first_phase1]] — don't reintroduce it under a new name).
- No unsupported one-sided extrapolation, no true-object-boundary semantic classifier, no GT-topology/component-count runtime branch.
- Old Phase 1-5 Boundary-First production path (Step 5-A coupled boundary fit, etc.) is explicitly preserved/unchanged — this new work reuses its NURBS/data structures but must not alter its production defaults.

**Deprecated-but-preserved (not deleted) research branches**, per the reset plan: `torch_surface_proxy.py`, `torch_surface_candidate_graph.py`, `torch_surface_decomposition.py`, `torch_gaussian_support_continuity.py`, proxy Stage 0-3/3-R script+tests+artifacts — kept for failure-reproduction evidence, never imported into production/continuation/occluded-chart code.
