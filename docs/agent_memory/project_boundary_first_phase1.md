---
name: project-boundary-first-phase1
description: "OSN-GS Boundary-First NURBS direction (governing plan), Phase 1-4 status/hardening (now closed, seam-placement search exhausted), and Phase 5 (Boundary-Aligned Extension) kickoff with its Step 5-A/B coupled patch-boundary prerequisite"
metadata: 
  node_type: memory
  type: project
  originSessionId: ce2402f7-496a-46cf-8bcd-e1cd8bc965d3
  modified: 2026-07-22T10:43:31.751Z
---

User adopted `OSN_GS_Final_Boundary_First_NURBS_Direction.md` as the new governing plan (2026-07-19), superseding `OSN_GS_Voxel_Driven_NURBS_Migration_Plan.md`'s Stage 2 (Stage 1-A~F stay as the ablation baseline, explicitly preserved, not deleted). Total phases: 0 (preservation) through 6 (Stage-2-style robustness, added later); each phase ends at a user-approval gate (§14), no auto-advance. Model used: sonnet (user confirmed sufficient, 2026-07-20). User committed Phase 0/1 groundwork as `71a4ae0 "Implement boundary-first NURBS construction groundwork"`; all Phase 2/3 diffs verified via `git diff --stat` to touch zero production/trainer files beyond one additive dataclass-field change.

**Phase 1 (Surface-Cell Component Builder) complete.** `osn_gs/surface/torch_surface_components.py`. Tuned: `normal_threshold_degrees=40.0`, `offset_threshold_ratio=0.5` via `_effective_offset_slope` (offset/tangential-distance ratio, not point-spacing). Found+fixed a real adjacency bug (`compute_leaf_face_adjacency`'s global eps broke x/y adjacency on flat z=0 scenes) via opt-in `degenerate_axis_tolerant` param, byte-identical to old default.

**Phase 2 (Component-Level Boundary Extraction) complete.** `osn_gs/surface/torch_component_boundary.py`. One shared UV frame per component (needs `tangent_u`/`tangent_v`, added to `SurfaceComponent`), coarse polygon union + ONE component-wide adaptive-bandwidth KDE (reuses Stage 1-F's `torch_boundary_refinement.py` unchanged) + marching-squares contour + outer/hole/tiny-artifact loop labeling with nesting. Found+fixed: curved components' coarse polygon union develops reprojection seam gaps (leaf-local polygons don't tile under a shared flat frame) — fixed via `coarse_gap_closing_cells=2` dilating ONLY the coarse mask, never the density field (verified the real 262-cell planar_hole hole is bit-identical with/without it).

**Phase 3 (Trimmed Component Correctness Baseline) complete, 2026-07-20.** New `osn_gs/surface/torch_trimmed_component_fitter.py`: fits ONE existing-fitter NURBS chart per component (IDW+LSQ+footpoint, UNCHANGED — `fit_torch_visible_surface_lsq` with `initial_uv` = Phase 2's own frame), control grid spans the whole rectangle and may cross the hole (§3.3), Phase 2's `refined_mask` applied directly as `uv_support_mask`. New `nurbs_constructor_benchmark/phase3_report.py`: reuses `ground_truth_metrics`/`patch_union_metrics`/`support_boundary_conformality` from `metrics.py` UNCHANGED via a duck-typed `_PseudoState` — same scoring machinery as legacy/Stage 1, so numbers are directly comparable.

**Important correction to my own earlier Phase 2 report:** I had compared Phase 2's internal `false_fill_cells` diagnostic (coarse-polygon-minus-density, NOT GT-compared) against Stage 1-F's GT-compared `union_false_fill_ratio` as if they were the same metric — they are not. The first real apples-to-apples GT comparison only exists from Phase 3 onward. At Phase 2's inherited default `density_threshold=2.0`, Phase 3's actual rendered false-fill was 0.384 (WORSE than Stage 1-F's 0.210) — traced to the KDE's inherent boundary bias (bandwidth ~2x local point spacing has real mass on the wrong side of a true edge; at component/whole-scene scale this bias is proportionally much bigger relative to a small feature like planar_hole's hole than it was at Stage 1-F's per-leaf scale). Fixed by raising Phase 3's OWN `density_threshold` default to 3.0 (swept 2/3/4/6/8; Phase 2's own already-approved gate at 2.0 was NOT reopened/changed) — brings false-fill to 0.200 (parity with Stage 1-F) while flat-scene coverage stays >=0.976. Documented at length in `phase3_report.py`'s docstring comment.

**Phase 3 completion criteria met (points=600, seed=0, density_threshold=3.0):** planar_hole patches=1/components=1/hole_count=1(gt 1)/seam=0, chamfer=0.008021 (comparable to Stage 1-F). sine chamfer=0.009198 (matches/slightly beats Stage 1's ~0.0094, far beats legacy's 0.0232). All Jacobians healthy (no degeneracy/fold-over), no control-grid collapse anywhere.

**Secondary finding (pre-existing, NOT Phase-3-specific):** `ground_truth_metrics`'s embedded `support_domain_metrics`/`sample_generated_surface` (fixed 128-resolution world-XY rasterization) shows huge spurious "hole" counts (thousands) for flat scenes across EVERY constructor including legacy (verified: legacy plane shows `support_generated_hole_count=3309` in the stored golden report) — a Moire/grid-mismatch artifact between a possibly-rotated UV sampling grid and the axis-aligned world-XY raster, unrelated to real topology. The reliable hole/false-fill number in this whole project has always been `patch_union_metrics`'s adaptive-density rasterization (`union_hole_count`, `union_false_fill_ratio`), not `ground_truth_metrics`'s own `support_generated_hole_count`/`support_iou` fields. Worth fixing generally at some point but out of scope for any single phase (shared code, all constructors).

**Phase 4 (Boundary-Conforming Chart Generator) complete, approved and implemented 2026-07-20.** `osn_gs/surface/torch_chart_topology.py` (disk_like/annulus/multi_hole/complex/non_chartable routing from Phase 2's own loop counts, with a `MIN_HOLE_AREA_FRACTION=0.02` significance filter — see below) + `osn_gs/surface/torch_annulus_chart.py` (N radial O-grid wedge NURBS charts per annulus component, each an unmodified `fit_torch_visible_surface_lsq` call seeded with a Coons-style polar-local UV). Non-annulus topologies fall back to Phase 3's trimmed baseline unchanged.

**Key finding — hard C0 continuity actively hurts accuracy, do not reintroduce it.** An intermediate implementation (written by another agent while I was blocked by a tool outage) forced exact seam continuity by overwriting each wedge's boundary control-point columns with values shared between adjacent slices after the free fit. This achieved ~1e-7 seam gaps but regressed both required accuracy metrics on `planar_hole` (chamfer 0.0058→0.0061, GT-compared false-fill 0.167→0.200; a variant sourcing the forced boundary from Phase 2's raster loop points instead of a 2-point chord was worse still: chamfer 0.0095, false-fill 0.311, because Phase 2's loop points carry their own raster-cell staircase noise). I reverted to a free LSQ fit where the Coons-seeded `initial_uv` is the only continuity mechanism and seam gap is *measured* (`_measure_seams`), not enforced — matching the plan's own §4.5 "measure, don't force, C0 in v1". Reverting also meant deleting a dead `_loop_points_at_angles` helper and an unused `outer_boundary_world_points` parameter the hard-constraint attempt had left behind. If a future phase revisits true C0/G1 continuity, start from this finding rather than re-deriving it.

**`MIN_HOLE_AREA_FRACTION=0.02` (hole-significance filter):** without it, `density_gradient`'s sparse-sampling region (a 17-cell density-threshold artifact against a 2468-cell outer loop, 0.7%) was misrouted into an 8-slice O-grid and collapsed (mean seam gap 0.267). `planar_hole`'s real hole is 8.2% of its outer loop. 2% sits with margin on both sides of this one labeled data point; revisit if a third labeled case narrows the margin.

**Verified Phase 4 numbers (planar_hole, points=600, seed=0):** chamfer=0.0058 vs Phase 3's 0.0080; GT-compared false-fill (`patch_union_metrics`) 0.167 vs Phase 3's 0.200; conformality=1.000 (nothing to trim, unlike Phase 3's rectangle); mean/max seam gap 0.012/0.064; zero Jacobian folds. `union_hole_count=2` (gt 1) is a known/expected 1-cell seam-speckle artifact, already separated from the real hole by the existing tiny-hole diagnostics.

**Also fixed during Phase 4 review, `nurbs_constructor_benchmark/metrics.py`:** `support_domain_metrics` sampled the generated surface at one UV point per output raster cell, which fragments any non-trivially-shaped trimmed chart into thousands of spurious "holes" unrelated to real topology (confirmed: legacy's own `plane` scene showed `support_generated_hole_count` in the thousands before this fix). Now reuses `patch_union_metrics`'s adaptive-density rasterization (`_patch_xy_mask`) instead. This only touches benchmark scoring code, not any constructor/production path.

**Infra note: `nurbs_constructor_benchmark/results/` (including `report.json`) is gitignored** (since commit `3c0975e "Ignore_generated_benchmark_outputs"`). It is NOT a stable regression reference across sessions — always re-run the relevant `phaseN_report.py`/`component_report.py` script and compare against numbers recorded in this memory file or `docs/worklogs/*.md`, never against a stale local `report.json`.

**Refactor pass, 2026-07-20 (user request: "강제로 덮어쓰거나 임시로 작업된 코드 없이 전체 구조가 매끄럽게 이어지도록"):** audited all Phase 1-4 modules and the three benchmark report scripts for forced-overwrite/dead-code/ad-hoc patterns. Found and fixed one: `phase4_report.py` was importing private (`_`-prefixed) `_PseudoState`/`_PseudoModel`/`_uv_support_payload` directly out of `phase3_report.py`. Extracted these into a new shared `nurbs_constructor_benchmark/benchmark_common.py` (public names `PseudoState`/`PseudoModel`/`uv_support_payload`) and updated both report scripts to import from there. No other hacky/temporary code found — `torch_annulus_chart.py`'s earlier hard-constraint remnants were already fully removed during the Phase 4 review (see above). Verified behavior-preserving: full test suite still 86/86 passing, and both `phase3_report`/`phase4_report` re-runs on `planar_hole` reproduce byte-identical numbers to those recorded above.

**Consolidated into the unified `osn-gs benchmark` CLI, 2026-07-20 (user correction: "phase별로 벤치마크 나누지 말고 기존 벤치마크를 대체하는 식으로 작업해야지").** Previously each phase had its own standalone script (`component_report.py`, `phase3_report.py`, `phase4_report.py`) outside the real `osn-gs benchmark` entry point (`nurbs_constructor_benchmark/runner.py`, which only ever supported `--constructor legacy|voxel_patch_stage1` via `TorchOSNGSPipeline`) — user was rightly upset that running the actual benchmark command never reflected Phase 4 work. Fixed by adding boundary_first as a third `--constructor` choice on `runner.py` itself: new `nurbs_constructor_benchmark/boundary_first.py` builds a duck-typed `BoundaryFirstState` (hierarchy->components->boundary->topology-routed chart, i.e. Phase 4's own logic, which already subsumes Phase 3 as its non-annulus fallback and Phase 1/2 as prerequisites) and is scored by the SAME generalized `score_state` body in `runner.py` that legacy/Stage1 use — one `report.json`, directly comparable numbers across all three constructors. The three old per-phase scripts were deleted. Deliberately does NOT touch `osn_gs/core/torch_pipeline.py` or the trainer (construction logic stays in `osn_gs/surface/*` + this benchmark-side orchestration) — only the benchmark CLI surface was unified, not the trainer's own constructor set. Usage: `osn-gs benchmark --constructor boundary_first [--bf-annulus-segments N --bf-density-threshold X ...]`. Verified byte-identical numbers to pre-consolidation (planar_hole chamfer=0.0058, false_fill=0.167). **Lesson: when adding a new construction phase, wire it into the existing benchmark CLI immediately, not a parallel script — do not repeat this pattern in future phases.**

**Fixed, 2026-07-20: benchmark's `--constructor` default was still `legacy` despite "unification."** Phase 1-4 work was committed (`5e37d1d`, single commit, all Phase 1-4 files + worklogs). User then asked whether plain `osn-gs benchmark` actually runs the latest pipeline and told me not to trust the docs — verified directly by running it and diffing `report.json`'s numbers against the known boundary_first baseline; confirmed it was still silently running legacy. Removed `"legacy"` from `runner.py`'s `--constructor` choices and flipped the default to `"boundary_first"` (commit `9aa491c`); also fixed `scripts/stage1_ablation.py`'s ablation-matrix row that relied on "empty args = legacy". `voxel_patch_stage1` stays selectable (Stage 1 ablation baseline, per the plan's explicit preservation requirement). Full detail in [[feedback_benchmark_cli_unification]]. User also confirmed (2026-07-20): full legacy removal from the whole codebase (trainer default in `torch_pipeline.py`, per plan §10.1) is deferred until the boundary-first work fully matures, but is a real end-state goal to actually execute then, not just a background policy line — revisit this when a later phase's gate is reached.

**Phase 4 hardening pass opened, 2026-07-21 (new governing doc `OSN_GS_Phase4_Hardening_Plan.md` at repo root, plan file kept in sync with progress; separate from but does not change the Phase 5 gate below).** User reviewed a rendered annulus O-grid screenshot (visible seam crack + uneven iso-line spacing) via external GPT critique, corrected my metric-definition draft twice (true Jacobian singular values via `J^TJ` eigenvalues not `‖Su×Sv‖`; along-seam vs across-seam continuity kept separate; symmetric not one-directional boundary distance; per-slice propagated not global-fixed orientation reference — full corrected definitions in the plan doc), then approved. Steps 1-4(partial) done, commit `72b3a4e`, 99/99 tests (86+13 new).

- **Step 1** (`osn_gs/surface/torch_annulus_chart.py`): added Jacobian singular-value/condition/orientation, seam tangent/cross-derivative/normal-angle, iso-line parameter-quality, symmetric Phase-2 boundary-conformance diagnostics — pure reporting, new `chart_quality` namespace, zero behavior change (verified). Immediately found a REAL bug this way: an O-grid inner-pole degeneracy — near the hole, physical tangential arc length shrinks toward 0 while radial extent doesn't, so `Su` gets tiny and free LSQ fits occasionally self-intersect at the `(u≈0,v≈0)` corner (nearest hole AND seam). Confirmed structural across seeds (3/6 seeds affected), not a one-off.
- **Step 2**: 13 new tests in `tests/test_annulus_chart.py` proving each new metric reacts to injected failures (collapsed/twisted grids, tangent/normal reversal, a collapsed boundary edge — the exact "one-directional metric misses collapse" failure the user warned about — plus a known-bad-seed regression guard for the Step 1 finding).
- **Step 3**: added 4 annulus scene variants (`planar_hole_offcenter`, `planar_hole_elliptical`, `planar_hole_density_gradient`, `curved_annulus` — `nurbs_constructor_benchmark/scenes.py`/`support_domains.py`). **`planar_hole_offcenter` is the worst-case stressor** for the inner-corner degeneracy (4x flips, 3x condition number, 2x false-fill vs centered). **Outer boundary conformance is systematically poor across every scene** (chamfer 0.06-0.10, coverage 0.53-0.80 vs inner's consistent 0.02-0.03/0.96-1.0) — confirmed real, scene-independent Step 4 target. **`curved_annulus` doesn't route to the O-grid at all** (Phase 1 splits it into 2 disk_like components regardless of normal/offset threshold flags) — open item, out of this plan's scope (Stage-1-hierarchy issue).
- **Step 4, candidate 1 REJECTED**: equal-arc-length segment placement along the OUTER boundary only (renamed `outer_radius_weighted_segment_placement` after a 2nd GPT review round called the original name misleading). A/B tested on all 4 scenes: didn't fix offcenter at all, catastrophically regressed density_gradient (0→20 flips). Kept as opt-in ablation tool, not deleted.
- **2nd external review round (2026-07-21) re-sequenced Step 4 into 4-A/4-B/4-C/4-D** (`OSN_GS_Phase4_Hardening_Plan.md` "Step 4 revision" section has full corrected definitions). Verified the review's top safety concern (reference-normal propagation hiding real flips) was NOT a bug via direct code read + existing test evidence.
- **Step 4-A** (commit `19af0b1`): renamed candidate 1 as above; added scale-normalized `min_jacobian_singular_value_normalized`; added component-level orientation **holonomy check** — first implementation was a mathematically-guaranteed-always-true no-op (greedy self-correcting walk), **self-caught by a new test failing when it should pass** (test-first discipline working as intended); replaced with correct parity-invariant (product of pairwise reference-normal dot signs around the ring), with documented limitation (a lone flip is an even-count local anomaly, undetectable by design, already caught elsewhere). Added optional per-sample heatmap export (`collect_diagnostic_samples`, default off).
- **Step 4-B REJECTED** (commit `19af0b1`): `seam_phase_offset` (rotates all 8 uniform-width seams by a constant). Jacobian-only lexicographic selection picked offset=0.2945 (flips 27→7 total), but checking the ACTUAL chamfer/false_fill regression bar across the full 8-offset sweep showed every offset just relocates false_fill regressions onto a different scene subset — no clean winner. Lesson: never select on Jacobian metrics alone, always check the full sweep against the real accuracy bar (this caught what a narrower check would have missed).
- **Step 4-C MARGINAL, not adopted** (commit `52ab79d`): `hermite_boundary_seed` (cubic Hermite Coons seed with shared central-difference boundary slope, targets seam continuity only per design, not inner-corner collapse). Modest offcenter seam improvement (flips 20→17) but consistent small chamfer regression across all 4 scenes — doesn't clear the no-regression bar.
- **Step 4-D BEST RESULT SO FAR, on HOLD** (commit `47e8f2f`, hold decision `6171000`): `worst_wedge_optimized` — local coordinate-descent optimizer over seam angles (not global DP, rejected as disproportionate for 8 wedges) minimizing worst-wedge `kappa_k = max(d,w_inner)/(min(d,w_inner)+eps)`, the actual objective per the review (not equal arc length). **First candidate where the priority target (`planar_hole_offcenter`) improved on BOTH Jacobian health (flips 20→12, cond_p95 8.64→6.54) AND accuracy (chamfer/false_fill both improved) simultaneously.** Real cost: `planar_hole_elliptical`'s false_fill regressed 22% relative; outer boundary conformance (separate unresolved Step 3 finding) essentially untouched (different mechanism, as scoped). **User decision 2026-07-21: HOLD — default stays `uniform_angle`, `worst_wedge_optimized` stays opt-in (`--bf-annulus-segment-placement worst_wedge_optimized`), not adopted for now.** Session paused here.
- Worklogs 39-43, all in **Korean** per explicit user instruction (see [[feedback_worklog_korean]]).
- **Recurring pattern this session**: user repeatedly showed rendered screenshots asking "did this get worse?" — every time, verifying against actual report.json numbers showed it was either (a) a pre-existing, already-documented weakness unrelated to my changes (e.g. `density_gradient` scene's known extrapolation issue, `planar_hole_density_gradient`'s known outer-boundary-conformance gap), never (b) an actual regression from any Step 4 work (all Step 4 candidates are opt-in, default path verified byte-identical every single time). Lesson: when a user shows a "worse" render, verify against real numbers before agreeing or disagreeing — don't assume either way, and be very explicit that "candidate X tested worse than baseline" (an experiment being rejected) is different from "the default pipeline got worse" (a real regression) — this distinction confused the user once and needed an explicit correction.

**Boundary-support bias investigation chain, 2026-07-21 (worklogs 45-47, commits `e87497e`/`946c454`/`a377625`), triggered by Step 4-D's `planar_hole_elliptical` regression.** All diagnostics-only, zero production estimator code changed throughout — still awaiting a user decision on adoption.

- **Worklog 45**: root-caused the outward boundary bias (worklog 39's "outer conformance poor everywhere") to Phase 1's `plane_aabb_intersection_polygon` (`torch_voxel_hierarchy.py:466`) using each leaf's FULL AABB regardless of actual point occupancy — confirmed via a new diagnostics module (`nurbs_constructor_benchmark/boundary_bias_analysis.py`) with 7 new analytic-GT circle/ellipse scenes (`boundary_bias_*`) and a 7-stage pipeline extraction. Stage 2 (voxel union) is 1.5-3x the final bias in every scene; KDE/threshold (current defaults) actually partially corrects it, not causes it.
- **Worklog 46**: prototyped convex-hull clipping of BOUNDARY-leaf polygons only (interior untouched) — helped 5/7 scenes cleanly but overshot into new under-coverage on 2 sparse-boundary scenes (`density_gradient`, `sparse_outer_rim`). Also found: on flat (z=0) synthetic scenes, `compute_leaf_face_adjacency`'s root-boundary check marks EVERY leaf as "boundary" (z has zero thickness → always touches root AABB), a structural fact not a bug.
- **Worklog 47**: user reframed the under-coverage "problem" as a support-ELIGIBILITY question, not a hull-shape defect — sparse near-boundary Gaussians shouldn't auto-promote to "observed", matching OSN-GS's own certain/uncertain distinction ([[project_osn_gs_direction]]) and `TODO.md`'s existing Priority-6 Input-Eligibility roadmap. Added a static 4-tier classifier (`ACTIVE_OBSERVED`/`UNCERTAIN`/`INACTIVE`/`COMPLEX`) per boundary leaf: primary signal `spacing_ratio` (NN spacing / leaf UV cell scale, scale-aware) as a two-threshold band (explicitly NOT "hysteresis" — corrected after review, no state memory), secondary votes (plane residual normalized, normal consistency via `|dot|` for sign-ambiguity, neighbor continuity using ONLY pre-existing Phase-1 `STATE_ACTIVE` to avoid circularity) recorded as an explicit auditable trace, never a hidden weighted score. INACTIVE requires sparsity AND ≥2 corroborating bad signals (conservative by design — sparsity alone never demotes past UNCERTAIN). COMPLEX leaves get their own mask, never merged into ACTIVE. Reports 3 separate cumulative views (`active_only`/`active_plus_uncertain`/`active_plus_uncertain_plus_complex`) — matching GT coverage on `active_only` is explicitly NOT a goal. Result: the 2 previously-problematic sparse scenes now correctly classify most leaves UNCERTAIN; `active_only` stays near-zero false-fill (0.006, 0.000), `active_plus_uncertain` recovers MORE coverage (0.674 both) than worklog 46's plain hull-clip with near-zero bias.
- Caught 2 real bugs during calibration against actual data (not assumed correct): `neighbor_phase1_active_ratio`'s denominator was including root-boundary contacts (diluting every leaf's ratio); an all-empty mask made `_fill_nan_nearest` return all-NaN instead of 0.
- **Worklog 48 (2026-07-22, commit `9f32143`)**: end-to-end validation — fed worklog 47's masks into the REAL `build_annulus_chart` fit (not just raster comparisons) and scored with production `ground_truth_metrics`/`patch_union_metrics` on all 4 annulus scenes. Result changed the recommendation: `active_only` alone actually REGRESSES chamfer_rms on 3/4 scenes (coverage loss hurts completeness more than the false-fill gain helps) — confirmed it must not be used as a standalone fitting input, only as a precision diagnostic. `active_plus_uncertain` is the clear winner: chamfer_rms at parity with baseline (±small) or better, false_fill cut 54-82% on 3/4 scenes, seam_gap flat-to-better, coverage barely reduced (0.96-0.98). One scene (`density_gradient`) saw no false_fill improvement — not further decomposed, flagged as a follow-up if revisited.
- 131/131 tests passing (unchanged by worklog 48 — no new production or test code, only existing functions called with a different mask argument from a scratch script).
- **Worklog 49 (2026-07-22, commit `5844e6d`): PRODUCTION ADOPTED**, user-approved ("그게 좋겠다. 적용하고 보고해볼래?"). Eligibility logic promoted from diagnostics to a new production module `osn_gs/surface/torch_boundary_eligibility.py` (`boundary_bias_analysis.py` now imports from it, no more duplication). `extract_component_boundary` (`torch_component_boundary.py`) gained `filter_boundary_leaf_eligibility: bool = True` (new default) using `active_plus_uncertain_plus_complex` as the coarse-support mask (not `active_plus_uncertain` — the 4 validated scenes had zero COMPLEX leaves so the two views are identical there, but `_plus_complex` avoids silently dropping COMPLEX-leaf support on untested scenes that do have them).
- **Real bug found during adoption that worklog 48's own validation missed**: worklog 48's script fed the modified mask into `build_annulus_chart` but reused the OLD boundary's `hole_loops`/topology unchanged — it never re-ran topology classification on the new mask. Wiring into real production (which DOES reclassify topology from the new mask) revealed the existing `coarse_gap_closing_cells=2` dilation (a pure dilation, no erosion, meant to fix curved-component reprojection seams) applied to the now-tighter eligibility mask: first nearly erased the whole improvement (2605→3051 cells on planar_hole, vs. 3059 baseline), and when disabled entirely, BROKE hole/ring topology on 3 of 4 real annulus scenes (`annulus`→`disk_like`/`complex` — individually hull-clipped boundary leaves losing raster contact with their ring neighbors, hole merging into exterior background). Fixed with a separate `eligibility_gap_closing_cells=1` (swept 0/1/2 against the real fit + real metrics on all 4 scenes; k=1 is the ONLY value topology-safe on all 4 while keeping most of the accuracy gain).
- **Final validated production numbers** (real `extract_component_boundary` defaults, real `build_annulus_chart` fit, real CLI cross-checked): 3/4 scenes improved (false_fill down to -53%, chamfer_rms down to -28%), 1/4 (`density_gradient`) slightly regressed (chamfer +3.3%, coverage -1.2%) — smaller gains than worklog 48's undilated headline numbers but topology-safe and net positive.
- 133/133 tests passing (1 skipped, structural; +2 new regression tests in `test_annulus_chart.py::EligibilityFilteringTopologySafetyTest` locking in the topology finding — default keeps `annulus`, `eligibility_gap_closing_cells=0` on a `count=600` fixture reproduces the break).
- **Lesson for next time**: an end-to-end validation script that substitutes a mask into a downstream fit but reuses upstream topology/loop objects from the OLD run is NOT a full end-to-end validation — always re-run classification/topology steps on the actually-modified intermediate, not just the final fit, or a real integration bug (like this dilation interaction) can hide until production wiring.

- **Worklog 50 (2026-07-22)**: user said to treat Phase 4 hardening as done and follow through on any planned completion tasks — found the plan's own Step 6 ("Regression gate") had been defined but marked "not yet started." Executed it: compared Step 3's original baseline (worklog 39) against current production defaults (worklog 45-49 applied) on all 4 Step-3 scenes. `chamfer_rms`/`false_fill` cleanly pass (improved or flat everywhere). `orientation_flips`/`max_jacobian_condition` are MIXED and don't cleanly clear the gate as literally worded: `planar_hole`'s flips rose 5→10, `planar_hole_offcenter`'s max_jacobian_condition rose 190.3→324.3 (+70%) — both still `holonomy_consistent=True` with zero near-degenerate samples, but the raw "no orientation flips" gate text isn't met on those two. `min_jacobian_singular_value`'s "above threshold" gate was never given a concrete number in the plan, so it's unscoreable (reported only: 0.0014-0.13). Outer-boundary-conformance gap (Step 3's other finding) unchanged, as expected. **Not auto-closed** — flagged to the user as a judgment call given the mixed Jacobian/flip result.

- **Worklog 51 (2026-07-22, commit `ac8060b`)**: user asked for a limited, production-unchanged diagnostic (Step 6-B) to localize worklog 50's flip/Jacobian findings — 5-seed analysis of `planar_hole`/`planar_hole_offcenter` using the existing `collect_diagnostic_samples=True` per-sample data (condition/sigma_min/orientation_dot per UV sample, wedge id, inner/outer/seam region classification, flood-fill contiguity, outer-boundary-specific check). **Verdict split by scene**: `planar_hole` meets the user's "documented exception" bar cleanly — flip/high-condition samples always a small isolated inner-corner cluster, p95/p99 healthy, outer boundary has ZERO flip/highcond samples across all 5 seeds. `planar_hole_offcenter` FAILS the bar — 2/5 seeds (1 and 3) show broad, non-isolated degradation (seed 1: 19% of samples flipped, one wedge 94% flipped end-to-end INCLUDING its outer edge — 28 outer flip samples, condition_max=3109; seed 3: p99=13.2, condition_max=1557), not a one-off outlier. Root-caused (not fixed) to the same known inner-corner-collapse mechanism as Step 1/worklog 39/41 — the bad seed's problem wedge has `inner_radius≈0.033` (near-zero), severe enough to fold nearly the whole wedge rather than just its corner. Step 4-D (`worst_wedge_optimized`, still on HOLD) already targets exactly this mechanism; re-evaluating it specifically on offcenter-like scenes is the proposed next step, NOT implemented (user said stop after reporting, no code changes). **Phase 4 is still NOT formally closed** — split verdict means `planar_hole_offcenter` needs a user decision (investigate/fix Step 4-D re-evaluation, or accept as a second documented exception, or something else) before the hardening plan can close.

- **Worklog 52 (2026-07-22)**: re-evaluated `worst_wedge_optimized` specifically on `planar_hole_offcenter`-like stress cases across the full 4-scene x 5-seed protocol (not just the localization worklog 51 did). Confirmed NOT eligible as canonical default: relocates rather than removes offcenter seed 1's fold, no change on seed 3, keeps the elliptical false-fill trade-off, severely regresses `planar_hole_density_gradient` (extreme wedge-width imbalance — no upper angular-width bound existed).

- **Worklog 53 (2026-07-22)**: user approved (`a) 진행해`) implementing worklog 52's own "canonical objective revision proposal" as a new opt-in `profile_constrained` segment_placement in `osn_gs/surface/torch_annulus_chart.py` — robust loop-based radial profiles (median/quantile, not raw min/max), interior-sampled (not endpoint-only) worst-case kappa, an explicit upper angular-width bound alongside the existing lower one (the exact gap `worst_wedge_optimized` lacked), an absolute inner-width floor, and a Phase-2-geometry-only outer-loop conformance term. Fully opt-in; `uniform_angle` byte-identical (all 43 tests incl. 4 new ones pass; full suite 137/1-skipped, no regression). Re-ran the same 4-scene x 5-seed A/B: **also not eligible** — doesn't cleanly beat either baseline; regresses `planar_hole_elliptical` flips (2.8/1.0→5.2 mean), makes `planar_hole_density_gradient`'s worst-seed Jacobian condition even worse (451 vs worst_wedge's 254 vs uniform's 44), and introduces a large new regression on an offcenter seed the prior optimizer already handled well. `false_fill` barely moves under any placement (union-raster metric insensitive to this axis). Production untouched.

- **User decision (2026-07-22), closing the Step 4-D avenue**: seam-angle-repositioning search (both `worst_wedge_optimized` and `profile_constrained`) is closed — no further proxy refinement. Both stay opt-in ablation tools only, production stays `uniform_angle`. `planar_hole_offcenter`/`planar_hole_density_gradient` are explicitly **NOT** accepted as documented exceptions (unlike `planar_hole`, worklog 51) — reasoning given: no evidence the failure is underfitting, so raising per-wedge resolution/degree or knot refinement would only add expressive power to an already-invalid parameterization, not fix it. `OSN_GS_Phase4_Hardening_Plan.md` deleted per its own closing instruction (full detail preserved in worklogs 39-43/45-53).

- **Phase 5 approved to start (2026-07-22)**, per user's explicit choice between "just write up the Step 5-A/B continuation" vs "kick off the master plan's real Phase 5" — user chose the latter. New living plan doc `OSN_GS_Phase5_Boundary_Aligned_Extension_Plan.md` (repo root) created, same update-as-you-go convention as the retired hardening plan. It sequences a **prerequisite** before Phase 5 proper: **Step 5-A/B coupled patch-boundary fitting** (relayed from the user via an external GPT review they tightened) — shared seam boundary control points become JOINT fit variables across adjacent wedges (not the previously-rejected post-hoc overwrite-after-independent-fit pattern that regressed accuracy in Phase 4's own hard-C0 attempt), with each patch's interior refit against the shared, once-solved edge. My proposal (approved): couple ONLY the shared boundary curve's own control points, not the full grid. Step 5-B (soft G1) only if 5-A proves insufficient, no upfront hard C1. Explicit emphasis from the user (not just "seal the seam nicely"): the report must show whether coupling also relieves patch-INTERIOR fold (Step 1's root cause, inner-pole `Su->0` degeneracy, is a per-wedge parameterization issue coupling isn't guaranteed to fix) — flip counts must be segmented seam-adjacent/inner/outer/patch-interior, not aggregated. Same 4-scene x 5-seed x count=600 protocol as every Step 4 candidate; no selector/retry/fallback/scene-branch; report only, no production adoption without separate approval. Only after Step 5-A/B is resolved and reported does actual Phase 5 scope begin (§5.1-5.4 of the governing doc: boundary segment classification, per-sample local frame, `S_ext(s,t)` extension chart, `C_ext(s,t)` controlled-extrapolation field) — not yet started.

- **Worklog 55 (2026-07-22): Step 5-A implemented and evaluated — exceptionally clean win.** (Note: originally drafted as "worklog 54" but renumbered to 55 by/for the concurrent Codex session working the same repo — the number 54 was independently claimed by that session for unrelated work, `54_argument_free_inspect_surface.md`; user confirmed the two sessions' work areas don't overlap and to ignore Codex's own concurrent doc changes.) New `fit_coupled_wedge_ring_lsq` (`osn_gs/surface/torch_nurbs.py`) joint-fits all annulus wedges in one linear system with shared seam boundary columns as joint variables (scatter-accumulate each wedge's own local normal-equations system into shared/private global variable slots, one solve, gather back — interior columns and smoothness/Tikhonov stay wedge-private, no cross-seam term). Same 4-scene x 5-seed protocol: **orientation flips go to exactly 0 in every single scene/seed**, including the two worst-documented cases (`planar_hole_offcenter` seed 1: 219→0 flips, cond_max 3109→35.4; seed 3: 18→0, cond_max 1556→5.1) and `planar_hole_density_gradient`'s condition regression (cond_max 675→21.7, far better than either failed optimizer's own 254/451). Region-segmented flip counts (seam-adjacent/inner/outer/patch-interior) are ALL zero — the fold is genuinely REMOVED, not relocated to the interior (the exact question this whole prerequisite existed to answer). `chamfer_rms` flat-or-improved on all 4 scenes (no regression anywhere); `false_fill` roughly flat (small non-alarming upticks on 2/4 scenes). Seam tangent/normal mismatch also drop near-zero as an EMERGENT side effect even though Step 5-B (soft G1) was never implemented — the shared boundary curve's own along-seam derivatives are identical by construction; cross-seam agreement doing this well too was not designed for, don't over-generalize past the 4 tested scenes.

- **Worklog 56 (2026-07-22): PRODUCTION ADOPTED**, user-approved immediately ("일단 지금 Production으로 채택하는 것으로 하고"). `build_annulus_chart`'s `coupled_boundary_fit` default flipped `False`→`True`; `construct_boundary_first`'s `annulus_coupled_boundary_fit` likewise. CLI flag renamed from opt-in `--bf-coupled-boundary-fit` to opt-out `--bf-disable-coupled-boundary-fit` (independent per-wedge fit stays available as a tested fallback, not deleted — matches `--no-stage1-boundary-refinement`'s naming convention, unlike worklog 49's eligibility adoption which added no CLI toggle at all). Two pre-existing tests assumed independent-fit-was-default and needed updating (not regressions — anticipated): `test_coupled_boundary_fit_default_off_is_unchanged`→split into `test_coupled_boundary_fit_is_now_the_default` + `test_coupled_boundary_fit_false_recovers_independent_fit`; `test_known_bad_seed_reproduces_inner_corner_degeneracy` (seed=14, asserted 8 flips under old default)→kept as `..._under_independent_fit` with explicit `coupled_boundary_fit=False`, plus new `test_known_bad_seed_is_resolved_by_default_coupled_fit` confirming that exact fixture now gives 0 flips under the new default. Full suite 145/1-skipped after the swap. **Step 5-B (soft G1) explicitly ON HOLD — user said wait for separate instruction, do not implement.**

- **Stopped per plan §14 gate — Phase 6 requires explicit user approval before starting regardless of how Phase 5 closes. `torch_voxel_hierarchy.py`/`torch_component_boundary.py` remain unmodified from worklog 49's eligibility-filtered default; `torch_annulus_chart.py`/`torch_nurbs.py`/`boundary_first.py` gained the new opt-in `profile_constrained` placement (default off) and `coupled_boundary_fit` (default ON, production-adopted per worklog 56). Follow-ups: curved-component (non-annulus fallback) reprojection-seam interaction under eligibility filtering not separately re-validated; `density_gradient`'s small false_fill regression (pre-existing, worklog 49) not decomposed; `DEFAULT_ELIGIBILITY_THRESHOLDS`/`eligibility_gap_closing_cells=1` still first-pass values; Step 5-B stays unimplemented pending explicit user go-ahead; Phase 5 proper (§5.1-5.4, extension charts) not yet started.**
