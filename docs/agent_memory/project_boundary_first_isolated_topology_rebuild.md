---
name: project-boundary-first-isolated-topology-rebuild
description: "OSN-GS isolated Boundary-first NURBS construction rebuild (2026-07-27) fixing v6 artifact's linear fan/wedge problem; separate thread from [[project_boundary_conditioned_occlusion]]'s Phase A-H append-adapter work; canonical doc OSN_GS_Urgent_Work_Master.md §3.2; worklogs 89-110+; worklog 110 found real support-curve crossing defect in 6/15 scenes via new crossing gate"
metadata: 
  node_type: memory
  type: project
  modified: 2026-07-27T09:20:14.652Z
  originSessionId: b04ebd79-3166-417b-94ae-ea59069d1297
---

**What this thread is.** A distinct, ongoing rebuild of the *visible-surface* Boundary-first constructor (topology → NURBS chart), triggered by the `curved_annulus` misclassification bug documented in `OSN_GS_Urgent_Work_Master.md` §3.1-§3.2. This is NOT the same thread as [[project_boundary_conditioned_occlusion]] (which is about occluded-region charts/uncertain-Gaussian-proposal/append-adapter, Phase A-H, already Gate-approved through the append adapter). This thread lives entirely in an **isolated** codepath — `nurbs_constructor_benchmark/boundary_first_support.py` + `boundary_first_support_runner.py` + `osn_gs/surface/torch_boundary_*` modules — and explicitly must never touch: the default constructor dispatcher (`nurbs_constructor_benchmark/boundary_first.py::construct_boundary_first`), the legacy/default visible constructor, the trainer, the production pipeline, uncertain-Gaussian proposal/append/ownership/checkpoint code, or real multi-hole patch materialization.

**Canonical principle (user-mandated, §3.2 of the master doc):** one common Boundary-first role contract for ALL topologies. Topology (loop count/nesting) is used only for loop interpretation, ownership, orientation, and validation — never as a dispatcher key choosing a different construction methodology. O-grid, observed-anchor fan, and future multi-loop partitioned patch networks are all *materialization forms* of the same contract, not separate code paths. Constructed/materialized must never be conflated with eligible — quality gate defaults to `review_required` until fidelity/crossing/curved-fit gates are in place.

**v6 artifact diagnosis (do not treat v6 as a valid/production result):** `artifacts/boundary_first_support_review_20260727_v6_resolution96/` has three root causes, all now partially addressed: (1) `torch_boundary_support_network.py` did pure linear interpolation between inner/outer boundary correspondence points; (2) `torch_boundary_constrained_surface.py` materialized the support grid as a degree-1 bilinear wedge; (3) `torch_boundary_central_cap.py` generated one degree-1 anchor-fan patch per raster contour cell (~430 patches for `sine`), producing radial-line blowup.

**What's implemented so far (chronological, each its own worklog under `docs/worklogs/`):**
- 89-91: support-curve foundation, pre-surface visible builder, curved_annulus recovery proof.
- 92-93: negative-control routing gate (parallel-sheets rejection) + point-count/seed recovery sweep.
- 94: deterministic Jacobian/seam/corner quality gate.
- 95: isolated 15-scene review runner (first full-benchmark isolated snapshot).
- 96 (auto correspondence): closed inner/outer loop correspondence now picks reverse×phase minimizing observed connector length (was previously explicit `reverse=False, phase=0` default) — provenance keys `reverse_boundary_b`/`boundary_b_phase`/`connector_length_score`. **Support-curve crossing detection is NOT yet implemented** — this auto correspondence has no rejection gate for crossing curves.
- Cubic closed wedge (`torch_boundary_constrained_surface.py`): closed network patches are now 4×N control grids, `degree_u=3` (circumferential, tangent-estimated via `_closed_cubic_wedge`), `degree_v=1` (radial — still linear interpolation, NOT a curved LSQ fit yet).
- Observed-anchor cubic fan (`torch_boundary_central_cap.py`): replaced per-contour-cell degree-1 fan with `curve_count`-segment (default 8) cubic outer-boundary pole patches; pole singularity stays explicit (never disguised as a regular quad).
- Pole-aware quality gate (`torch_boundary_surface_quality.py`): Jacobian regularity check now detects/excludes the shared-pole row/column instead of assuming a fixed 2×2 fan shape.
- State separation: `BoundaryFirstVisibleSurfaceResult.materialization_state` (materialized/not_materialized) vs `.quality_state` (currently always `review_required` when materialized — no automatic `eligible` path exists yet).
- 102: unified `outer_boundary + interior_boundary`/`interior_anchor` role contract; tiny-KDE-hole restoration limited to sub-threshold area (material holes preserved, not silently filled).
- 103-104: `torch_boundary_source_fidelity.py` bidirectional-ish (actually still mostly reconstructed→raw-point, one direction) fidelity metric as review-only diagnostic (not an eligibility gate); raster resolution raised 64→96 with 1-cell anchor-ray tolerance.
- 106: resolution/tolerance sweep across point-count {400,600}×seed{0,1,2} — sine/curved_annulus always constructed, U-shape always unsupported (no silent central fill).
- 107-109: multi-loop role evidence kept **review-only** (never materializes overlapping per-hole annulus duplication); `torch_boundary_planar_partition.py` foundation for non-overlapping planar-domain partition invariants (ownership, UV degeneracy, orientation) — still review-only, no chart materialization.
- **2026-07-27 round 1 (worklog 109 append): exporter review layers.** `torch_boundary_first_visible_builder.py` now records raw `observed_outer_boundary`/`observed_inner_boundary`/`observed_interior_anchor` in provenance (JSON-safe, even for rejected/unsupported items). `boundary_first_support_runner.py` derived `reconstructed_outer_boundary`/`reconstructed_inner_boundary`/`support_curves`/`patch_seams` by reading the materialized surfaces' raw `control_grid` rows directly. `renderer_payload()` in `boundary_first.py` gained an additive, keyword-only, default-`None` `boundary_first_review` param so the legacy dispatcher call site and existing tests are unaffected.
- **2026-07-27 round 2 (worklog 110, own file per the reversed rule below): geometry-semantics correction + crossing gate.** Round 1's "control_grid row == support curve" approach was flagged as conflating *control polygon* (fitting data) with *evaluated curve* (actual geometry) — only true by coincidence for the radial (`degree_v=1`, non-rational) axis, false for any cubic (`degree_u=3`) edge where only the two Bezier endpoints are on-curve. New module `osn_gs/surface/torch_boundary_review_geometry.py` introduces 5 explicit representation kinds (`observed_evidence_points`, `resampled_observed_evidence`, `control_polygon`, `correspondence_chord`, `evaluated_curve`) via a `ReviewGeometryEntity` dataclass, and always evaluates real iso-parametric edges/curves through `surface.evaluate()` (`evaluate_iso_edge`, `evaluate_interior_iso_curve`) rather than reading `control_grid` directly. The observed-anchor fan's pole↔corner 2-point data is now explicitly a `correspondence_chord`, not a claimed support curve; the actual support curve is evaluated at each patch's own interior `u=0.5` iso-curve. `combine_ordered_patch_boundary()` stitches per-patch evaluated edges into one closed reconstructed boundary with deterministic shared-endpoint dedup (including the wraparound closure). New `detect_support_curve_crossings()`/`classify_support_curve_pair()` classify each pair of *actual evaluated* support curves as `valid_shared_pole`/`valid_shared_boundary_endpoint`/`invalid_interior_crossing`/`near_touching_ambiguous`/`no_crossing`/`not_checked`, using an explicit `expected_shared_point` so legitimate pole convergence is never itself flagged. The runner's `gate_pass` now also requires `not has_invalid_crossing`, so `record.state` can no longer be `constructed` when a crossing is found (still never reaches an `eligible` state — none exists yet).
  - **Real discovery, not a false positive:** running this gate over all 15 benchmark scenes found genuine, previously-invisible support-curve overlap in 6 hole-free (observed-anchor-fan) scenes — `plane`, `sine`, `crease`, `triangle`, `elongated_plane`, `close_parallel_sheets`. E.g. for `plane` (600 pts, seed 0, curve_count=8), segments 3 and 7 of 8 nearly coincide away from the shared pole (~0.003–0.018 units apart across their whole interior, vs. ~0.94 local scale) while all other 27 of 28 segment pairs correctly show only the expected shared-pole touch. Root cause is suspected to be the anchor (pole) sitting near the boundary rather than the geometric center, combined with pure-arclength (not angular) boundary resampling around it — **not yet fixed, root-cause repair of anchor selection/resampling is explicitly out of scope for round 2** and flagged as the top item to decide direction on next.

**What's still missing / explicitly not started:**
1. Root-cause fix for the anchor-fan crossing defect found above (anchor selection and/or angular vs. arclength resampling) — 6 of 15 scenes affected.
2. Actual curved boundary/support fit — the cubic wedge is still circumferential-only-cubic; interior support along the *radial* direction is still linear (this is now honestly labeled `evaluated_curve` reflecting the true degree-1 geometry, not mislabeled — but a genuinely higher-order constrained LSQ fit from observed samples is still not implemented). `torch_constrained_chart_lsq.py::solve_constrained_chart` exists (occluded-chart Phase F work) but is occluded-chart-specific; would need an isolated visible-adapter wrapper if reused, never a direct import into this path or production.
3. True bidirectional source-boundary fidelity (reconstructed-evaluated-curve → observed AND observed → reconstructed, not just one direction) — explicitly deferred again in round 2 since it was optional ("구현할 수 있다면") for that round.
4. False-hole correction hardening: current tiny-hole fill is area-ratio-only; canonical bar requires multiresolution/KDE persistence, raw-support evidence, local-spacing-normalized area, correction provenance, and a genuine-small-hole negative control. **Do not promote the current area-ratio correction to canonical/automatic status.**
5. Multi-hole real non-overlapping patch materialization (evidence foundation exists in `torch_boundary_multi_loop.py`/`torch_boundary_planar_partition.py`; materialization itself is explicitly out of scope until separately approved).

**Test baseline (2026-07-27, after round 2 / worklog 110):** targeted boundary-first suite (52 tests, includes new `tests/test_boundary_review_geometry.py`) all pass; full `pytest` = 495 passed, 2 failed, 1 skipped, 1 warning, 8 subtests passed. The 2 failures are pre-existing and unrelated to this thread: `tests/test_trimmed_component_fitter.py::test_fits_flat_plane_with_low_residual` and `::test_jacobian_metrics_detect_a_healthy_flat_fit`, both expecting `degenerate_fraction == 0.0` but observing ≈0.0017361111. The previously-listed `test_annulus_chart.py` orientation-flip failure (expected 8, got 0) was root-caused as a stale fixture (see worklog 105) — canonical `frame_margin=0.05` extractor genuinely produces 0 flips; the fixture was updated, it is not a live failure anymore.

**Standing rule for this thread (REVERSED same day):** the session's opening
instructions said to append progress to the most recent relevant existing
worklog (109) rather than create a new one. After that append happened, the
user immediately corrected it: "다음부턴 그냥 새 worklog 만들어" (from now on
just make a new worklog). So as of 2026-07-27, this thread follows
[[feedback_new_worklog_per_round]]'s general default again — **each new round
of work in this thread gets its own new `docs/worklogs/NN_*.md` file**,
cross-referencing 109/etc. by number rather than growing them. Don't revert to
append-only behavior here unless the user explicitly names a specific worklog
to append to again for a specific round.
