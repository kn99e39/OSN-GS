# OSN-GS Urgent Work Master

최종 갱신: 2026-08-10

이 문서는 현재 진행 방향과 승인 경계를 정의하는 canonical master다. 과거 실험의 상세 경과는 Git 이력에 보존하며, 현재 판단에 필요 없는 작업로그는 `docs/worklogs/`에서 제거했다. 작업로그 번호는 `docs/worklogs/README.md`의 최신 인덱스(1부터 재번호, 현재 62까지)를 기준으로 한다.

## 1. 목표 모델과 불변 조건

OSN-GS에서 NURBS는 관측 가능한 표면을 설명하고 가려진 영역의 불확실 Gaussian 생성에만 기하 정보를 제공하는 중간 표현이다. visible/certain Gaussian의 위치는 NURBS가 아니라 영상 손실로 최적화한다.

- visible surface는 topology별 별도 방법론으로 분기하지 않는다.
- 모든 topology는 observed boundary loop, boundary role, source provenance, interior support를 공통 입력 계약으로 사용한다.
- multi-hole은 outer loop와 모든 interior loop를 보존한다. 비중첩 planar partition 증거가 없으면 `review_required`이며 임의 central fill 또는 hole별 overlapping annulus 복제는 금지한다.
- artifact의 chart가 생성되었다는 사실은 품질·안전·사용 가능성을 뜻하지 않는다.
- **evidence가 없으면 강제로 닫지 않는다.** gap 보간, 임의 candidate 생성, closure만을 위한 낮은-evidence edge 선택은 어떤 단계에서도 금지한다 — 이 원칙은 아래 2절 전체에서 반복적으로 검증·재확인됐다.

## 2. 현재 활성 작업 A — Canonical visible-NURBS production pipeline (full-cloud continuation → representative selection → boundary ordering → materialization)

**2026-07-30(worklog 18) 기준으로 이 경로가 `train.py`/benchmark의 유일한 production 경로가 됐다.** `legacy`, `voxel_patch_stage1`, IDW/local split fallback과 해당 CLI는 전부 제거됐다. 이전 절(과거 버전)에서 "isolated, dispatcher 미연결"로 기술했던 world-space boundary half-edge → ordered graph → materialization adapter 계열 작업(worklog 12-15)은 이 병합 이후 더 이상 isolated가 아니다 — 지금은 `construct_visible_nurbs_from_gaussians` / `TorchOSNGSPipeline._construct_canonical_with_full_evidence`가 곧 production 경로다.

### 2.1 파이프라인 단계

1. **Representative selection** (`torch_density_preserving_representative_selection.py`): 대량 Gaussian을 `canonical_construction_max_points`(기본 2048) 예산으로 mode-aware farthest-point sampling. worklog 49에서 boundary-evidence swap-in(예산 경쟁으로 탈락한 진짜 orientation-divergent evidence를 안전하게 복구)을 추가했다.
2. **Reliability + region formation** (`torch_gaussian_structural_reliability.py`, `torch_gaussian_surface_region_formation.py`): full-cloud contextual evidence 기반 reliability, seed/merge DSU 기반 region consolidation.
3. **Boundary candidate 추출** (`torch_full_cloud_continuation_shell.py`, `torch_boundary_support_termination.py`): representative 주변 same-mode angular gap을 관측해 `observed_support_termination`/`no_gap`/`parallel_sheet_conflict`/`crease_discontinuity`/`ambiguous_continuation`/`reliability_frontier`/`unresolved_sampling_gap`으로 분류. worklog 47(cross-surface leakage), 48(fold/gap-crossing locality), 50(single-radius over-reach)에서 순차적으로 오분류를 좁게 수정했다.
4. **Directed ordering / materialization** (`torch_directed_boundary_ordering.py`, `torch_visible_boundary_region_status.py`): region별 compatible edge에 대한 exact one-in/one-out Hungarian matching으로 closed cycle과 open path를 결정론적으로 복원. worklog 53에서 downstream-invalid 2-cycle이 capacity를 낭비하는 결함과, 그로 인해 노출된 direct/reverse tangent quality 비교의 self-intersection 인식 결함을 수정했다. worklog 54는 region별 5-state 계약(`eligible_closed_boundary`/`open_observed_fragment`/`insufficient_observation`/`ambiguous_boundary`/`rejected_unsafe`)을 확정해 validated closed loop만 materialization에 전달하도록 제한했다.

### 2.2 현재 결론 (worklog 40~53에 걸친 6단계 소진 감사)

Real 3k/5k/10k checkpoint(cap 2048) replay에서 **5k만 2개 region을 닫고(closed/materialized 2/2), 3k와 10k는 0개**다. worklog 40부터 53까지 다음 단계를 각각 독립적으로 감사했고, 전부 실제 결함을 찾아 좁게 수정했지만 **어느 것도 closed-loop 개수를 바꾸지 못했다**:

| worklog | 감사 대상 | 결과 |
|---|---|---|
| 40 | region-pair 단위 sphere seam 오탐 | 수정, sphere 22 false candidate 제거 |
| 45 | directed compatibility gate의 target tangent 부호 | 수정, Box 6번째 face + real 5k 0→2 closed 회복 |
| 47 | no_gap의 cross-surface(mode) leakage | 수정, 효과는 재분류만 |
| 48 | no_gap의 candidate-local fold/gap-crossing | 수정, 3k/5k 각 1 node만 재분류 |
| 49 | representative selection의 FPS 예산 손실 | 수정(swap-in), real swap 22/51/76건이나 closed 불변 |
| 50 | 단일 4x 반경의 원거리 support 과다 신뢰 | 수정(scale-persistence), 76/36/39 node 재분류, closed 불변 |
| 51 | raw full-cloud에 representative 축약으로 잃은 chain이 있는지 | 기각(3곳 raw에도 chain 없음) + 2곳은 Hungarian 경쟁 문제로 재분류 |
| 52 | 그 Hungarian "경쟁" 주장 자체의 정확성 | 정정 — edge는 실제로 matching에 포함돼 있었음, 진짜 원인은 topology 불가능(region 52) 또는 evidence 열세(region 56) |
| 53 | region 56의 진짜 원인(2-cycle 낭비) | 수정, fragment 2개→4-node open path 1개로 개선(닫히진 않음, 진짜 최댓값이 open이므로) |
| 54 | region별 boundary 상태를 production 계약으로 확정 | `eligible_closed_boundary`/`open_observed_fragment`/`insufficient_observation`/`ambiguous_boundary`/`rejected_unsafe` 5-state 도입, validated closed loop만 materialize, 2-cycle budget exhaustion fail-closed 추가 |
| 55 | eligible region의 downstream 전달 완성 | fail-closed 일관성 검사 + provenance threading + 단일 진입점(`eligible_materialized_surfaces()`) 추가. 첫 시도(region당 최대 1개 closed loop만 유지)가 thin_slab materialization을 회귀시켜 즉시 되돌리고 "전부 유지 + reason으로 명시 disclosure"로 확정 |
| 56 | `eligible_materialized_surfaces()` → Phase D/E(continuation domain·occluded candidate) production bridge | `build_eligible_boundary_continuation_bridge()` + `run_eligible_boundary_continuation_bridge_from_gaussians()` 신설, Phase D/E 두 모듈은 미변경. Phase D의 최소 4-sample 계약이 기하적 안전 조건이 아니라 표현 밀도 관례임을 실측 확인하고 검증된 loop를 결정론적으로 재표본화 — real 5k 2개 region 모두 domain 생성(candidate는 AABB 비접촉으로 0), 남는 실패는 `eligible_visible_only_not_continuation_ready`로 typed 분리 |
| 57 | Occluded candidate → safe uncertain proposal production integration | 기존 Phase F constrained fit → Phase F.1 sampled safety → Phase G proposal을 `run_safe_uncertain_proposals_from_gaussians()`로 Worklog 56 bridge 뒤에 연결. candidate별 1:1 typed accounting과 provenance chain 유지, `degenerate` domain은 Phase E provenance로만 보존하고 Phase F/G 입력으로는 fail-closed rejection. model append/appearance/opacity는 미수행 |
| 58 | Safe uncertain proposal → atomic append production integration | Worklog 57의 `proposed` proposal만 기존 `UncertainGaussianAppendAdapter.append()`(model-only, 4-way transactional)로 전달하는 production 진입점 추가. `initialization_provider`가 명시적 appearance/opacity를 제공해야 하며 합성하지 않음. `_commit_ledger` 실패 주입 시 model/sidecar/registry/ledger 전부 pre-transaction 상태로 롤백 확인 |
| 59 | Appended uncertain Gaussian trainer activation | `appended` receipt만 소비해 기존 trainer optimizer에 안전하게 활성화. Append adapter의 `model.optimizer is None` 전제조건을 만족시키는 임시 분리/재부착을 이 모듈이 담당하고, 기존 `_preserve_optimizer_state()`(ADC clone/split 재사용, 미변경)로 기존 Adam state는 bit-for-bit 보존·신규 행은 0 초기화. Visible/uncertain 분리는 새로 구현하지 않고 기존 `is_uncertain` 마스킹 관행에 의존. 동기화 실패는 전부 롤백 후 `appended_inactive`로 typed 분리 |
| 60 | Atomic trainer activation contract closure | `TorchOSNGSTrainer.activate_and_train_uncertain_step()`으로 실제 rasterizer render+loss 경로에 연결. `append_and_activate()`를 candidate 단위 composite transaction으로 확장해 activation 실패 시 model/sidecar/registry/ledger/optimizer 전부 롤백(`rolled_back`). `masked_optimizer_step()`으로 Adam momentum까지 포함한 true row-level isolation 구현, 롤백 후 optimizer parameter identity를 group-name 재-keying으로 보장 |
| 61 | Real visible chart boundary materialization (physical 경로와 무관한 신규 확장, §2.2 6단계 감사 대상 아님) | Accepted region topology만으로 chart boundary를 구성하는 `eligible_parametric_chart_boundary` 경로 신설(§2.5). Physical `eligible_closed_boundary` 경로는 완전히 미변경. Real 5k/10k 합산 materialized 3→92/4→78 |

**결론: 남은 병목은 파이프라인 결함이 아니라 candidate evidence 자체의 밀도/위상이다.** 3k/10k의 큰 region은 perimeter 전체를 덮을 만큼 충분히 독립적인 observed-termination evidence가 없다 — representative를 더 정확한 위치로 옮기거나(49), 오분류를 고치거나(47/48/50), matching 낭비를 없애도(53) 이 밀도 자체는 바뀌지 않는다. 5k가 성공하는 이유는 이런 결함이 없어서가 아니라 해당 region이 작아서(candidate 4개)다. worklog 54는 이 결론을 region별 5-state production 계약으로 확정했다 — real 5k는 region 130/141만 `eligible_closed_boundary`, 3k/10k는 전부 나머지 4-state로 명시 기록되며 빈 결과/실패로 뭉뚱그려지지 않는다.

**Worklog 61 addendum**: 위 결론은 physical-termination 경로(`eligible_closed_boundary`)에만 적용된다 — 이 경로 자체는 worklog 61에서도 변경하지 않았다. 대신 accepted region topology만으로 chart boundary를 구성하는 완전히 별도인 `eligible_parametric_chart_boundary` 경로를 추가해, physical evidence 밀도와 무관하게 visible NURBS materialization coverage를 real 5k/10k 기준 수십 배 확장했다(§2.5 참고). physical 경로의 candidate evidence 밀도 문제 자체는 여전히 미해결이다.

### 2.3 검증된 negative control (모든 worklog가 반복 확인)

cap=64 기준: Box `physical=51 closed=6 materialized=6`, Cylinder `16/2/2`, Sphere `14/0/0`, Thin slab `37/3/3`. 이 수치는 worklog 47부터 61까지 매 라운드 재확인했다 — 새 작업은 이 표를 반드시 재현해야 하며, 벗어나면 즉시 원인을 규명한다(worklog 53의 2차 self-intersection 결함, worklog 55의 multi-loop exclusion 회귀가 각각 이렇게 발견됐다).

### 2.4 다음 착수 후보 (미승인, 방향 제안일 뿐)

- Candidate GENERATION 밀도 자체(왜 큰 region이 candidate 3~6개에 그치는지) — 지금까지 전부 candidate 전달/분류/정렬 단계만 감사했고 생성 자체의 근본 원인은 아직 안 봤다.
- ordering/quality 비교(`recover_directed_boundary_components`)가 direct/reverse 전체 scene 단위로 이뤄지는 구조 자체의 재검토 — worklog 53에서 이 결합이 예상치 못한 부작용을 만든 사례가 있었다.

### 2.5 Parametric chart boundary 경로 (worklog 61, physical 경로와 완전 분리)

`osn_gs/surface/torch_region_parametric_chart_boundary.py`가 physical termination evidence와 무관하게, region의 이미 승인된 accepted-edge topology만으로 chart boundary를 구성한다(convex hull/bounding box 미사용, leftmost-turn walk로 outer face 추적). Boundary edge는 `physical_termination`/`crease`/`observation_frontier`/`partition_seam` 중 하나로 분류되며 위장하지 않는다. `eligible_parametric_chart_boundary` region만 기존 `materialize_visible_boundary_component()`(미변경)로 materialize되고, `eligible_parametric_chart_surfaces()`로 physical 경로(`eligible_materialized_surfaces()`)와 완전히 분리 노출된다 — Worklog 56 continuation bridge 등 기존 소비자는 여전히 physical 경로만 읽는다(연결 안 됨, 별도 승인 필요).

Real 5k/10k(cap 2048)에서 physical+parametric 합산 materialized surface가 5k 3→92, 10k 4→78로 확장됐다(§2.2 표의 physical-only 숫자와는 다른 지표). §2.3의 cap=64 negative-control physical 수치(6/2/0/3)는 byte-identical로 유지되고, parametric 경로가 추가로 Box 4·Cylinder 1·Sphere 1(physical 0 유지, 별도 provenance만)·Thin-slab 3(partition_seam 1건 명시)를 materialize한다.

## 3. 현재 활성 작업 B — Isolated Boundary-first hardening (구 Section 2, 현재 사실상 비활성)

과거(2026-07 초) 이 절이 기술하던 exporter/cubic seam wedge/observed-anchor central cap 작업(worklog 4-6)은 그 자체로는 dispatcher/production에 연결된 적이 없다. 2026-07-30(worklog 18) 이후 production은 §2의 canonical 경로로 통합됐고, 이 isolated hardening 라인은 이후 세션에서 별도로 재개되지 않았다. 재개할 경우 다음을 여전히 유지한다: exporter의 outer/interior/support/seam/chart 명시적 분리, sampled crossing/fidelity gate, degree-1/fan 임시면의 최종 근거 사용 금지, false-hole evidence 부족 시 `review_required` 유지.

## 4. 현재 활성 작업 C — Uncertain Gaussian model foundations

Phase G proposal, model-only append adapter, occluded chart ownership foundation은 각각 구현·검증된 계약으로 유지한다. 이들은 visible-surface quality를 대신 증명하지 않으며, append 대상의 appearance/opacity와 downstream lifecycle은 여전히 명시적 차단 조건이다.

- raw Gaussian evidence → safe uncertain proposal → model-only atomic append(worklog 57/58) → trainer optimizer activation(worklog 59)까지 production orchestration이 구현됐다. `appended` receipt만 활성화 대상이며, appearance/opacity는 여전히 `UncertainAppendInitialization`이 명시 제공한 값만 사용한다(합성 없음).
- Worklog 59의 activation은 `TorchGaussianModel.optimizer`(Adam)를 기존 `_preserve_optimizer_state()` grow-in-place 로직으로 확장하는 데 그쳤다. Worklog 60은 `TorchOSNGSTrainer.activate_and_train_uncertain_step()`으로 실제 render+loss 경로에 연결하고, append+activate를 candidate 단위 composite transaction으로(activation 실패 시 append까지 전부 롤백), `masked_optimizer_step()`으로 Adam momentum까지 포함한 row-level 학습 격리를 구현했다 — 여전히 `train()`의 실제 iteration 루프/CLI/checkpoint 스케줄에는 연결하지 않았다(`activate_and_train_uncertain_step()`은 호출자가 명시적으로 불러야 하는 별도 메서드). Visible/uncertain 분리 자체는 새로 구현하지 않고 기존 `is_uncertain` 마스킹(`torch_density_control.py`/`torch_pipeline.py`)에 의존한다.
- global ranking/selection, conflict resolution, checkpoint schema 변경, renderer 통합, 실제 `TorchTrainer` 루프로의 배선은 여전히 시작하지 않는다.

근거: `docs/worklogs/1_phase_g_uncertain_gaussian_proposal_foundation.md`, `docs/worklogs/2_uncertain_gaussian_append_adapter_foundation.md`, `docs/worklogs/3_occluded_chart_ownership_foundation.md`, `docs/worklogs/57_occluded_candidate_safe_uncertain_proposal_production_integration.md`, `docs/worklogs/58_safe_uncertain_proposal_atomic_append_production_integration.md`, `docs/worklogs/59_appended_uncertain_gaussian_trainer_activation.md`, `docs/worklogs/60_atomic_trainer_activation_contract_closure.md`.

## 5. 명시적 비범위

다음은 현재 착수 금지다.

- §2 canonical 경로의 candidate/threshold를 scene-specific하게 튜닝하는 일 (box/cylinder/sphere/thin_slab처럼 shape별 예외를 만드는 것 포함)
- gap 보간, 임의 candidate 생성, 또는 closure만을 위한 낮은-evidence edge/cycle 강제 선택
- representative cap 증가로 evidence 부족을 우회하는 일
- optimizer/trainer/renderer/checkpoint 통합 범위 확대(worklog 59/60의 좁은 예외: 이미 append된 uncertain row를 `TorchGaussianModel.optimizer`에 활성화하고, `TorchOSNGSTrainer.activate_and_train_uncertain_step()`을 통해 실제 render+loss로 row-level 격리 학습하는 것까지만 승인됐다 -- `train()`의 실제 iteration 루프·CLI·checkpoint schema로의 배선은 여전히 금지)
- global chart ranking·selection 또는 conflict resolution
- 불완전한 false-hole evidence를 이용한 자동 topology 확정
- benchmark artifact만으로 visible surface 품질이 해결되었다고 선언하는 일

## 6. Training core parity (worklog 62, §2와 독립)

Baseline과의 render/scale/anisotropy 격차(§2와 무관, `output/osn_gs_scene`의 iteration 600 이후 극단적 anisotropy 성장 및 3k+ screen-size prune 폭주의 실제 원인)를 실제 baseline 코드와의 lockstep parity harness로 추적해 `osn_gs/data/colmap_scene.py`의 데이터 로더 결함 2건을 확정·수정했다:

1. `camera_fovs()`가 downscale된 렌더 해상도로 FoV를 재계산하고 있었다(baseline은 COLMAP 원본 해상도에서 1회만 계산하고 이후 절대 건드리지 않음) — 원본 해상도 고정으로 수정.
2. Ground-truth 이미지 resize filter가 `BILINEAR`였다(baseline은 Pillow 기본값 `BICUBIC`) — `BICUBIC`으로 수정.

Fix 후 lockstep 재검증: step 1은 float32 noise 수준까지 일치, step 600(ADC 비활성화, population 통계만) anisotropy median이 baseline과 거의 일치(1.58 vs 1.59) — fix 전 real training에서 관측된 극단적 격차가 사라짐. Surface reconstruction/reliability 코드는 미변경. 자세한 내용은 `docs/worklogs/62_graphdeco_lockstep_training_parity.md` 참고.

**Worklog 63 addendum**: 위 fix를 real 3k production 학습(2900/3000/3100)에 적용했으나, iteration 3100 anisotropy median이 fix 전후 사실상 동일(35.06→35.68, baseline 5.46)하고 screen-size prune도 줄지 않았다(오히려 소폭 증가) — **완료 기준 미충족.** loader 결함은 lockstep 조건에서의 확인된 최초 발산 원인 중 하나일 뿐, 실사용 규모 anisotropy 폭주의 지배적 원인은 아니었다. 자세한 내용은 `docs/worklogs/63_fixed_loader_3k_production_replay.md` 참고.

**Worklog 64 addendum(지배적 원인 확정 및 fix)**: OSN-GS의 유일한 Gaussian 초기화 경로(로컬 PCA planar-surfel)가 `normal_scale = tangent_scale * 0.04`로 모든 새 Gaussian의 iteration-0 anisotropy를 설계상 ~25로 고정하고 있었다 — 이것이 실제 지배적 원인이다. `TorchPipelineConfig.gaussian_initialization_mode`("baseline_compatible" 기본값 / "covariance_knn" experimental)를 신설해 surface construction용 covariance(미변경)와 모델 자신의 학습 가능한 scale/rotation init(신규, Graphdeco의 `create_from_pcd`와 텐서 단위로 동일한 등방 init)을 분리했다. 3-way parity harness로 baseline_compatible이 step 0(anisotropy 정확히 1.0)부터 step 600(ADC 후 population/split 후보 규모)까지 baseline에 근접함을 확인했다. `initialize_deferred` 스케줄은 surface reconstruction 불변 유지를 위해 의도적으로 이 플래그 영향 밖에 둔다. 자세한 내용은 `docs/worklogs/64_gaussian_initialization_parity.md` 참고.

**Worklog 65 addendum(실사용 3k production 검증)**: `baseline_compatible`을 실제 3k 학습(iteration 0/600/2900/3000/3100)으로 검증했다. anisotropy median 35.77(covariance_knn)→3.66(baseline_compatible, baseline 5.49)로 회복, min-scale collapse도 baseline보다 낮은 0.027%로 개선, chart materialization 수 감소(90→11 @2900)는 baseline PLY를 동일 파이프라인에 돌린 참조값(3~8 regions)과 비교해 진짜 over-segmentation 완화로 확인했다(covariance_knn은 그 참조값의 20~60배). 렌더 품질은 매 checkpoint에서 일관되게 baseline 방향 개선이나(PSNR +0.3~0.4) 격차를 완전히 닫지는 못한다. **다만 iteration 3100의 screen-size prune 건수는 224,164→233,178로 줄지 않았다** — anisotropy와는 별개로 opacity-reset 이벤트 자체에 내재한 현상으로 보이며, 아직 원인 미규명이다. 자세한 내용은 `docs/worklogs/65_baseline_compatible_3k_production_validation.md` 참고.

**Worklog 66 addendum(patch fidelity 검증 + 부수 성능 수정)**: materialized visible NURBS patch가 실제 표면을 정확히 재구성하는지 patch당 point-to-surface/surface-to-evidence 정규화 거리·coverage·overlap·Jacobian/orientation/self-intersection·boundary provenance로 검증하고 5-way 분류(threshold는 전부 기존 코드 관례 재사용)했다. valid_supported 비율 baseline_compatible 0%→36%, covariance_knn 27%→33%, baseline 참조 50% — over-segmentation 완화가 진짜 품질 개선과 함께 옴을 확인(covariance_knn은 patch 수는 많지만 gap 비율이 오히려 더 나쁨, 55~59% vs 41~43%). 대다수 patch가 `under_supported`(evidence 3~4개)인 것은 양쪽 OSN-GS 조건 공통의 별개 근본 한계로 남음. 부수적으로 이번 분석 실행 중 `osn_gs/surface/torch_density_preserving_representative_selection.py`의 `_boundary_evidence_swap_in`에서 O(pool²) 개의 개별 GPU sync(파이썬 loop 안에서 CUDA tensor를 한 쌍씩 스칼라로 인덱싱)를 벡터화 — git-stash A/B로 결과 bit-identical 확인 후 10.8배 speedup(338→31초). 자세한 내용과 대표 patch 시각화는 `docs/worklogs/66_visible_patch_coverage_and_fidelity_validation.md` 참고.

**Worklog 67 addendum(under_supported 근본 원인 확정)**: worklog 66의 `under_supported`가 §2.2의 candidate evidence density 병목과 같은 계열인지 검증하기 위해, representative topology/chart boundary는 그대로 두고 각 region이 대표하는 원본 full-cloud evidence를 NURBS fitting support로 복원했다(신규 `osn_gs/surface/torch_region_owned_full_evidence.py`, 기존 production `_propagate_with_evidence_gating()`의 normal-alignment/residual 게이트를 재사용 — 재구현 없음 — 을 소비해 region-owned evidence를 모으고, additive-only로 `_construct_canonical_with_full_evidence`에 연결). 결과: representative-only 대비 full-evidence support가 55~333배 늘었고 `under_supported`/`unsafe_geometry`가 22개 patch 전부에서 사라졌다(전부 `materialized`, worklog 69에서 총계 오류 정정: 기존 "21개"는 오기) — **evidence 부족이 아니라 representative에게만 묻던 fitting 설계의 한계였다.** 대신 95%(21/22)가 `extrapolative`로 재분류됐는데, Graphdeco baseline 참조도 동일 패턴(4/4, 2/2)이라 OSN-GS 학습 품질 문제가 아니라 6×6/degree-2 NURBS fitting 해상도가 조밀한 실제 evidence의 국소 변화를 못 따라가는, 별개의 새로운 발견이다(이번 라운드는 지시대로 해상도를 조정하지 않고 발견만 기록). 자세한 내용은 `docs/worklogs/67_region_owned_full_evidence_patch_support.md` 참고.

**Worklog 68 addendum(extrapolative 원인 확정: overfitting)**: worklog 67의 21/22 `extrapolative`가 fitting capacity 부족/정규화 척도의 밀도 의존성/비균일 weighting 중 무엇인지 가려냈다. 신규 `osn_gs/surface/torch_local_orientation_folding.py`로 global reversal(결함 아님)과 local folding(인접 grid 샘플 간 normal 불일치, 실제 결함 신호)을 분리하고, 22개 patch 전부에(worklog 69에서 총계 오류 정정: 기존 "21개"는 오기) 6×6/8×8/10×10 grid × {uniform, density-compensated} weighting, deterministic spatial holdout, 4종 정규화 척도를 측정했다. **결과: 22개 중 20개(90.9%)가 `overfitting`**(해상도를 올리면 local fold fraction이 늘거나 train error만 개선되고 held-out은 개선 안 됨), 1개만 `capacity_insufficient`, 1개 `inconclusive`. `metric_density_dependent`/`weighting_problem`은 0건 — **grid 해상도를 올리는 것은 안전한 해결책이 아니다**(overfitting을 유발). 정규화 척도의 밀도 의존성 가설과 비균일 weighting 가설도 이번 데이터로는 지지되지 않았다. 자세한 내용은 `docs/worklogs/68_dense_evidence_fit_capacity_and_fidelity_calibration.md` 참고.

**Worklog 69 addendum(single-chart parameterization validity)**: worklog 68의 20/22 `extrapolative`/`overfitting`이 invalid single-chart 때문인지 검증했다. 신규 `osn_gs/surface/torch_single_chart_uv_validity.py`로 UV duplicate/neighborhood preservation/accepted-edge crossing/local fold(3D normal 부호)/boundary containment/parallel-sheet suspicion 6개 기준을 측정. **결과: 22개 전부 `partition_materialization_required`, `uv_valid` 0개.** 다만 neighborhood_preservation은 대부분 건강해(3/22만 위반) 진짜 다중 시트/접힘이 지배적 원인은 아님을 시사하고, 대신 **boundary loop(3~4개 representative 점)가 실제 evidence 범위(수백~수천 점)에 비해 지나치게 작다는 별개의 구조적 문제**(interior_outside_boundary 22/22 위반, 대부분 90~100%)가 드러났다. Partition은 기존 accepted-edge graph에서 안전한 분할 근거가 22/22 모두 없어 전부 fail-closed(0건 적용) — 임의 seam 없이는 분할하지 않는다는 원칙을 지켰다. Density subsampling에서 raw/정규화 오차가 함께 움직여 metric density-dependence 가설을 다시 기각했다. `parallel_sheet_suspected`가 outlier 1개에도 오탐하던 버그를 발견·수정(gap_ratio 39~2088→7~25). 자세한 내용은 `docs/worklogs/69_single_chart_parameterization_validity_and_repair.md` 참고.

**Worklog 70 addendum(dense boundary materialization 시도, 부분 개선·미해결)**: worklog 69의 22/22 `partition_materialization_required`를 canonical 판정으로 채택하지 않고, representative topology/chart boundary는 불변으로 둔 채 region-owned full evidence로 DENSE boundary를 별도 materialize했다(신규 `osn_gs/surface/torch_region_owned_boundary_materialization.py`: edge별 evidence 소유권을 3D point-to-segment distance로 판정, loop centroid 기준 `local_evidence_scale`(worklog 32 재사용, 새 상수 아님) 이상 먼 후보만 채택, edge tangent를 그 scale 폭 bin으로 나눠 bin당 최원점 1개만 순서대로 삽입 — hull/rectangle/강제 폐쇄/타 region 병합 없음, `validate_simple_closed_loop`(미변경) 실패 시 fail-closed). 이번 라운드 안에서 "edge당 1점"과 "모든 후보를 그대로 삽입" 두 대안을 실측 후 기각하고 binning 방식을 채택했으며, PCA-UV 공유 프레임이 outlier evidence에 따라 회전해버리는 불안정성도 발견해 이 모듈은 world 3D 좌표만 사용하도록 설계했다. **결과: 22개 patch 중 `full_evidence_boundary_materialization_required` 11개(dense 확장 자체가 self-intersection으로 fail-closed), `partition_materialization_required` 11개(dense boundary는 성공했으나 `interior_outside_boundary`가 11/11에서 여전히 10% 기준 초과), `valid_supported`/`extrapolative` 0개.** boundary vertex 평균 3.27→8.73, `interior_outside_boundary` 91.1%→54.8%로 측정 가능한 개선은 있었지만(worklog 69 진단을 구조적으로 뒷받침) 어떤 patch도 승인 기준을 통과하지 못했고 held-out fitting 오차도 개선되지 않았다(23.87→28.86) — **원본 topology가 3~4개 edge/wedge만 가져서, 아무리 조밀하게 채워도 evidence의 실제 외곽 형태를 wedge 경계 자체가 따라가지 못한다는 더 구체적인 구조적 한계**로 재확인됐다. `parallel_sheet_suspected`/raw-evidence `triangle_fold_fraction`은 지시대로 진단 전용으로만 기록, gate에서 제외했다. 자세한 내용은 `docs/worklogs/70_region_owned_full_evidence_boundary_materialization.md` 참고.

**Worklog 71 addendum(topology를 직접 복원해도 같은 결론에 수렴)**: worklog 70의 representative-edge densification을 canonical 방식에서 폐기(진단 결과는 유지)하고, worklog 61의 고정 3~4점 loop를 전혀 참조하지 않은 채 typed half-edge evidence(physical termination/crease/observation frontier/ambiguous)와 region-owned full evidence로부터 ordered boundary TOPOLOGY 자체를 복원하는 신규 `osn_gs/surface/torch_region_owned_full_evidence_boundary_topology.py`를 만들었다. 1차로 시도한 "evidence를 반경 기준으로 typed candidate에 직접 붙여 evidence-density 그래프를 통째로 구성"하는 설계는 실측 결과 모든 region이 최대 260-node짜리 하나의 branching blob으로 붕괴해 폐기했다. 최종 설계는 기존 production `build_boundary_compatibility`/`recover_ordered_boundary_components`(미변경)를 representative 밀도에서 그대로 재사용해 SEED-level topology를 복원하고(부수적으로 production이 crease/ambiguous 후보를 계산만 하고 실제로는 병합하지 않는 dead-code 결함을 발견, production은 미변경 유지하고 직접 호출로 우회), 그 함수의 "ordered" 출력이 실제로는 gaussian_id 정렬일 뿐 진짜 geometric walk가 아님을 확인해 동일 accepted edge로 진짜 walk를 재구성했다. `ordered_closed_loop`/`ordered_open_chain`만 region-owned evidence로 edge 단위 densify(worklog 70과 같은 world-3D/binning 알고리즘이지만 `validate_simple_closed_loop`의 nonplanar-즉시-실패 결합을 풀기 위해 별도 재구현)하고, 신규 `evaluate_closed_loop_geometry()`로 3D nonplanarity와 2D proper-crossing 검사를 분리했다(nonplanar만으로는 실패 처리하지 않음). **결과: 실제 37개 region·282개 seed-level component 중 `boundary_topology_closed_loop_recovered` 17건**(self-intersecting 0건, 일부 region은 독립 loop 2개를 병합 없이 각각 보존 — worklog 70과 달리 실제로 evidence-topology 기반 closed loop 복원에 성공). 그러나 17건 전부 최소 크기인 3-vertex 삼각형에 머물러 densify 후에도 `interior_outside_boundary`가 16/17에서 정확히 100%로 남고, 전부 `partition_materialization_required`(0 valid_supported/extrapolative)로 귀결됐다. **worklog 61의 leftmost-turn과 이번 half-edge-그래프 복원이라는 완전히 다른 두 방법이 같은 결론에 수렴**했다 — 문제는 특정 boundary 구성 알고리즘의 한계가 아니라, typed half-edge evidence 자체가 어떤 방법으로 조립하든 region의 실제 evidence 범위에 비해 본질적으로 희소하고 국소적이라는 더 근본적인 사실이다. 자세한 내용은 `docs/worklogs/71_region_owned_full_evidence_boundary_topology_reconstruction.md` 참고.

**Worklog 72~74 addendum(다른 세션, dense boundary support 및 closure attribution)**: sparse typed half-edge loop 대신 region-owned full evidence의 local angular support에서 dense boundary 후보를 추출하고(`torch_region_owned_dense_boundary_support.py`, baseline-compatible@2900에서 후보 785개), mutual directional connectivity의 실패를 half-line 단위로 귀속했다. half-line 68.03%가 full-evidence local scale 내 continuation 부재로 종료됐고, distance-stage cycle 34개 중 13개가 tangent incompatibility로 제거돼 covariance-derived orientation이 scale mismatch와 **독립된** cycle-destruction 병목임을 확인했다. closed loop은 여전히 0. 자세한 내용은 `docs/worklogs/72~74` 참고.

**Worklog 75 addendum(normal-source 아키텍처 결정 종결: KEEP covariance_normal)**: worklog 74가 지목한 "explicit structural-normal A/B"를 단일 bounded 비교로 수행해 **normal-source 질문을 종결**했다(추가 진단 라운드를 만들지 않는 것이 이 라운드의 명시적 목적). 신규 실험 전용 `osn_gs/surface/torch_structural_normal.py`가 region-owned observed 점의 **위치만**으로 local PCA normal을 만들고(scale/rotation/covariance/SH/opacity/renderer/optimizer 미사용, photometric training에서 완전 분리), 기존 missing-sector/outward-direction 로직으로 tangent만 재유도한다. candidate id·xyz, region ownership, boundary reason, candidate extraction, connectivity scale/distance threshold, ambiguity/mutuality, topology acceptance를 전부 고정했고 **B는 candidate를 독립적으로 재추출하지 않는다**(frozen 집합의 orientation frame만 재계산). Scene은 기존 fixture/checkpoint만 사용: `box_face`(평면), `cylinder`(crease/곡면), real `baseline_compatible@2900` 4개 region.

**결과: 합성면에서는 두 모드가 모든 지표에서 완전히 동일**(normal 불일치 median 0.16~0.42°, 곡면 side 7.31°) — structural normal이 단순 표면을 열화시키지는 않는다. **그러나 real 데이터에서는 normal이 median 30.8~63.6°(p90 76.9~82.8°) 어긋나면서도 final closed loop은 8개 region 전부 0 vs 0으로 동일하다.** 집계상 tangent rejection은 33→22로 줄지만 그 손실이 사라진 게 아니라 **normal 단계로 이동**할 뿐이고(5→40, 8배), 순 생존 edge는 tangent 240→198(−17.5%)·mutuality 94→86(−8.5%)로 real region 4개 중 3개에서 명확히 악화된다. 추가로 structural normal 기준 재검 시 A가 이미 인정한 candidate의 30~60%(cylinder side는 12/12)가 boundary admission을 통과하지 못해, drop-in 교체가 아니라 "어디가 boundary인가"를 조용히 바꾸는 변경이다. Rendering은 real CUDA rasterizer로 A/B 전후 `torch.equal` bitwise identical 확인, 비용은 오히려 저렴(structural normal 계산 region당 <3ms, 추가 메모리 N×3×4 byte, 영속 state 없음).

**판정: KEEP covariance_normal.** 단 이 결과는 covariance normal이 우수하다는 뜻이 **아니다** — worklog 74의 가설(covariance tangent이 cycle을 깬다)은 부분적으로 확인됐으나(normal source를 바꾸면 tangent rejection은 실제로 감소), 그 개선이 최종 topology로 이어지지 않고 손실이 상류로 재배치될 뿐이라는 것이 확정됐다. **즉 normal source는 현재 boundary-closure 실패의 구속 조건(binding constraint)이 아니다.** 두 orientation source가 real 데이터에서 median 30~64° 어긋나면서도 결과가 0 vs 0으로 같다는 것은 병목이 orientation 정확도가 아니라 그 상류(candidate/support 밀도·scale domain)에 있음을 가리킨다. 이 라운드에서 `boundary_support_spacing`은 활성화·재설계하지 않았고 connectivity threshold도 변경하지 않았다. 자세한 내용은 `docs/worklogs/75_structural_normal_vs_covariance_normal_decision.md` 참고.

**Worklog 76 addendum(scale domain도 병목에서 제거: full_evidence_spacing 유지)**: worklog 72~74가 지목한 "half-line 68%가 local scale 내 continuation 부재"가 scale domain 단위 오류인지 한 배치에서 종결했다. 신규 `osn_gs/surface/torch_boundary_support_spacing.py`가 `full_evidence_spacing`/`representative_spacing`(report-only)/`boundary_support_spacing`을 의미적으로 분리하고, estimator 3종(현행 full-evidence / region 단위 robust candidate spacing / candidate별 robust local spacing)만 비교한다. **2.5배 거리 배수와 0.1배 ambiguity 허용은 모드별로 조정하지 않았고 connectivity certificate도 재설계하지 않았다.** production에는 가산 인자만 추가했고(기본값이 이전 동작과 동일), real 7개 region 기본 경로가 worklog 72 발표 수치(open_or_ambiguous 621 / closed 0)와 정확히 일치함을 회귀 확인했다.

**결과: 단위 오류는 실재했고 두 독립 scale이 실제로 그것을 고친다** — `no_candidate` 1108/1652(67%)→254(region)/**21(local, 1.3%)**, neither 방향 486→46, branch explosion 0, proper crossing 0. **그러나 그 연속성의 출처가 관측되지 않은 gap이다**: 미지지(빈 구간 포함) edge가 9/185(4.9%)→191/397(48.1%)/211/427(49.4%)로 10배 뛰고, real 전 region에서 단일 edge 길이의 **75~92%가 관측된 빈 공간을 지나는** 사례가 나온다(baseline은 중앙값 0.0/p90 0.0). 이는 금지된 gap bridging이며, 그 대가로 얻는 real closed loop은 region6의 1개뿐이고 그 loop의 `interior_outside_boundary`는 99.78%로 worklog 70/71과 동일한 퇴화 결과다.

**결정: production 연결 scale은 `full_evidence_spacing` 유지(독립 scale 미채택).** 세 spacing을 분리한 계약 자체는 유지하되 기본 모드는 바뀌지 않으며, 남은 실패는 **boundary-support predicate/evidence 자체**에 귀속한다. production이 미변경이므로 real dense boundary는 closed loop 0 그대로이고, reduced UV gate/6×6 NURBS에 도달하는 boundary가 없어 `valid_supported`/`extrapolative`/`partition_materialization_required`가 전부 0이다 — **gate 탈락이 아니라 gate 입력 부재**이며 두 상태를 혼동해 기록하지 않는다. worklog 73의 "68% 거리 실패"는 단위 오류가 가린 회복 가능한 연속성이 아니었다: 단위를 바로잡으면 실패는 사라지지만 그때 연결되는 상대가 관측 근거 없는 먼 candidate다 — candidate들이 애초에 공간적으로 인접하지 않다. 자세한 내용은 `docs/worklogs/76_boundary_support_spacing_scale_domain_decision.md` 참고.

**Worklog 77 addendum(predicate 결함 규명·수정, 그리고 아키텍처 답 확정)**: worklog 75/76이 normal source와 scale을 제거한 뒤 남은 dense boundary-support predicate를 감사해 **실재하고 국소적으로 귀속되는 결함 하나를 규명·수정**했다. 각도 gap은 양옆 이웃 "광선" 사이로 측정되는데 이웃은 연속 표면의 점 표본이므로 국소 각도 표본 해상도만큼 체계적으로 과소평가되고, **직선 boundary의 참 empty sector는 정확히 pi**라서 raw 측정값이 pi에 아래에서 수렴한다 — threshold가 엄격한 것이 아니라 **추정기가 점근적으로 편향**돼 있었다(box_face 실측: 참 boundary gap/pi median 0.9989로 32점 중 25점이 pi의 1% 이내, interior는 0.25pi로 완전 분리). threshold(pi)는 유지하고 **그 점 자신의 비최대 gap 중앙값만큼 편향만 보정**했다(새 상수 없음, 밀도가 오르면 보정항 0으로 수렴해 `gap >= pi`로 회귀). **완화가 아님의 직접 증거: precision 전 fixture 1.000 불변, 닫힌 sphere candidate 0 유지, 신규 테스트가 수정 전 직선-boundary 2건만 실패하고 fail-closed 계열은 전부 통과.**

합성 ground truth(fixture 생성식에서 직접 유도) 기준 recall box_face 0.438→**1.000**, cylinder side 0.250→**1.000**, 연속 누락 run 최대 4~5→**0**. 수정 후 **기존 미변경 connectivity**에서 **box_face가 worklog 69~77 통틀어 처음으로 interior evidence를 100% 포함하는(interior_outside_boundary 0.000) 유효 closed loop**을 생성했고, cylinder side는 위/아래 두 boundary ring을 병합 없이 각각 복원했다(closed 2, crossing 0). Real baseline_compatible@2900 7개 region에서는 candidate **785→940(+19.7%)**, degenerate frame 0, branch 0, crossing 0이지만 **closed loop은 여전히 0**이고, accepted candidate 사이 경로의 **71.7%가 관측 증거 자체의 부재**이며 predicate 거부가 놓인 경우는 28.3%뿐이다.

**아키텍처 답 확정: 둘 다이지만 지배적인 것은 관측 증거의 부재다.** predicate에는 실제 결함이 있었고 실제로 고쳐졌지만(합성에서 recall 100%·유효 loop 생성), real의 잔여 실패는 predicate가 아니라 evidence에 귀속된다 — **동일한 수정 predicate가 관측이 연속적인 경우에는 완전한 perimeter와 유효 loop을 만들어낸다는 합성/real 대비가 결정적 증거다.** 증거가 없는 곳에서는 fail-closed를 유지했다(sphere 0, real 0 loop, 기하 fallback 없음). gap 거부 점의 40~66%가 near-normal 이웃 오염을 포함한다는 2차 관찰은 합성에서 0건이라 ground truth 검증 수단이 없어 측정만 하고 조치하지 않았다. 자세한 내용은 `docs/worklogs/77_boundary_support_predicate_discretization_bias.md` 참고.

**Worklog 78/79 addendum(constructor-wide 귀속과 chart-domain coverage 계약, 그리고 최종 판정)**: worklog 78(다른 세션)은 physical termination과 parametric chart frontier의 typed provenance 누락을 수정해 relation half-edge가 chart provenance에 전달되도록 했다. worklog 79는 그 뒤 남은 real 실패를 one-factor ablation 없이 **chart topology→domain→UV→NURBS fitting을 한 번의 constructor-wide pass**로 귀속했다.

**Eligibility 계약 검증**: chart boundary edge를 만드는 `adjacency`는 오직 `region.internal_accepted_edge_ids`로만 구성되고 relation half-edge는 **라벨만 제공하며 edge를 만들 수 없다** — ambiguous 증거가 topology 지지 없이 boundary가 되는 경로는 구조적으로 없고 실측에서도 ambiguous 승격 0건이다. 다만 **별개의 공백**이 있다: eligibility가 "accepted topology가 simple cycle인가"만 보고 **그 chart domain이 자신이 fitting될 evidence를 담는지는 보지 않아** 3-node 삼각형이 무조건 eligible이 된다.

**7-region 매트릭스**: materialized 5개 chart가 **전부 동일 원인 `boundary_chart_extent_mismatch`이고 실패 단계가 전부 parameterization 이전**이다. representative topology는 3~7 노드로 evidence extent의 0.15~0.67배만 덮는데, ownership propagation에 in-plane 거리 상한이 없어 region이 93~1035점을 소유한다(evid/member **31~301배**) — 결과적으로 evidence의 **89.1~99.8%가 chart domain 바깥**이다. Jacobian near-degenerate 0, local fold ≤0.63%로 **fitting geometry는 전부 건강**하므로 fitting/UV 개선으로는 사라지지 않는다. worklog 78이 region 3을 valid(p95 4.0)로 본 것은 train 기준이며, 결정론적 held-out에서는 6.43으로 **held-out 기준 valid_supported는 5개 중 0개**임을 정정했다. no-chart인 region 4/5는 강제 폐쇄·임의 분할 없이 자기 accepted-edge 구조로 분류했다(5 = pendant를 가진 genuinely open, 4 = 2-core 단일 성분의 ambiguous branching — 다중 chart 필요로 단정할 근거 없음).

**교정**: `fit_region_owned_full_evidence_patch`에 **chart-domain coverage 계약**을 추가해, evidence의 과반이 domain 밖이면 fitting 이전에 `chart_domain_does_not_cover_evidence`로 fail-closed한다(containment는 boundary loop 자신의 best-fit 평면에서 측정, 기존 `interior_within_boundary` 재사용). 실측 위반이 82.6~99.6%로 0.5에서 멀어 **0.5~0.85 전 구간에서 판정이 동일**함을 전용 테스트로 고정했다 — 외삽을 줄이려는 threshold 튜닝이 아니다. 부수적으로 기존 테스트 fixture가 `grid[:4]`(모두 x=-0.5인 **공선 4점, 면적 0인 boundary**)로 이번에 고친 결함을 그대로 인코딩하고 있었음을 발견해 실제 perimeter loop으로 교정했다.

**Before/after(real baseline_compatible@2900 전체 재실행)**: full-evidence fit `materialized` 5→**0**, `extrapolative` 5→**0**, `chart_domain_does_not_cover_evidence` 0→**5**(82.6/94.8/93.2/82.6/99.6%). eligible chart boundary 5, representative-scale chart surface 5, physical surface 0, no-chart region 2는 전부 **불변** — 바뀐 것은 "이 chart가 region의 owned evidence를 대표한다"는 주장이 거짓일 때 typed fail-closed된다는 점뿐이다.

**최종 판정: 현재 visible NURBS constructor는 real 데이터에서 사용 가능하지 않으며, region/chart 표현 자체의 재설계가 필요하다.** 5개 chart가 전부 같은 원인으로, 전부 parameterization 이전에 실패하며, geometry는 건강하다. 원인은 국소 버그가 아니라 표현의 구조다 — **topology를 담는 단위(3~7 representative)와 evidence를 담는 단위(93~1035 point)가 31~301배 어긋나 있고 전자가 후자의 공간 범위를 0.15~0.67배만 덮는다.** worklog 79의 계약은 이 어긋남을 정직하게 드러낼 뿐 없앨 수 없다. 자세한 내용은 `docs/worklogs/79_constructor_wide_chart_domain_coverage.md` 참고.

**Worklog 80 addendum(parametric chart 표현 재설계: topology/geometry 역할 분리)**: worklog 79가 확정한 구조적 실패를 진단이 아니라 **표현 재설계**로 다뤘다. 신규 `osn_gs/surface/torch_dense_parametric_chart_support.py`가 두 역할을 분리한다 — **sparse accepted topology는 perimeter의 순환 순서와 arc별 typed frontier provenance만** 제공하고 chart의 기하 범위로는 절대 쓰이지 않으며, **기하는 worklog 77의 보정된 predicate가 뽑은 region-owned dense boundary-support candidate**가 제공한다(모든 chart 정점이 관측 Gaussian). 실측 근거: dense candidate의 공간 범위/owned evidence 범위 = **0.966~1.020**, 이들이 대체하는 representative는 **0.148~0.667**. 각 candidate는 가장 가까운 sparse arc에 귀속돼 typed provenance를 상속하고, arc 내부 단조 정렬 후 순환 순서로 연결된다 — **representative는 최종 loop의 정점이 아니다.** dense support 부재 시 sparse polygon fallback 없이 fail-closed, 미지지 arc는 `evidence_backed=False`로 명시. 다중 chart는 2-core가 서로소 성분 2개 이상일 때만 허용.

구현 중 자체 결함을 발견·수정했다: arc binning의 bin 개수를 **sparse chord 길이**에서 유도해 이 모듈이 없애려는 스케일 불일치를 재도입하고 있었다(region 6이 candidate 218개 중 16개만 유지). candidate 자신의 투영 span 기준으로 고쳐 88정점/coverage 56.1%, region 0은 6→17정점/59.1%→31.2%가 됐다.

**7-region before/after**: chart-domain coverage 통과 **0/5 → 4/5**, chart 정점 3,3,3,3,3 → **17,84,92,20,88**, domain 밖 evidence **82.6~99.6% → 29.0~56.1%**, Jacobian near-degenerate 0 유지, fitted-surface folding ≤0.45%. **그러나 통과한 4개는 전부 `extrapolative`(held-out p95 5.49~19.92)이고 valid_supported는 여전히 0이다.**

**원인이 분리돼 드러났다**: region evidence 자체를 PCA-UV로 파라미터화하면 **UV-인접 삼각형의 21~36%에서 3D normal 부호가 어긋난다**(neighborhood preservation 0.53~0.70) — region이 단일 정규 chart가 아니다. worklog 69도 같은 것을 봤으나 그때는 extent 문제와 뒤섞여 있었고, extent를 제거한 지금 이것이 분리된 지배 원인으로 남는다. 다중 chart가 답인지 확인했으나 `independent_chart_components`가 region 0/1/2/3/4/6 전부 1, region 5는 0으로 **기존 topology는 어떤 분할도 증명하지 못한다** — 근거 없는 분할 금지 제약에 따라 채택하지 않았다.

**판정 (3): 기존 region/evidence 표현은 evidence-backed chart domain은 공급할 수 있으나, 유효한 *단일* chart domain은 공급하지 못한다.** 재설계 자체는 viable하고 채택 가치가 있다(worklog 79의 구조적 결함을 실제로 해소). 다만 real 사용 가능한 chart는 만들지 못하며, 실패 위치가 "chart가 evidence를 담지 못함(parameterization 이전)"에서 **"evidence가 단일 정규 chart가 아님(parameterization 단계)"** 으로 이동했을 뿐이다. 남은 병목은 chart 구성 알고리즘이 아니라 **region 자체가 단일 chart가 아니라는 사실**과 **기존 accepted topology가 그 분할을 증명할 해상도를 갖지 못한다는 것**이다. 자세한 내용은 `docs/worklogs/80_dense_parametric_chart_representation_redesign.md` 참고.

**Worklog 81 addendum(intrinsic boundary-conditioned parameterization 판정)**: worklog 80이 통과시킨 4개 real chart(region 0/1/2/3)가 왜 전부 `extrapolative`인지 — PCA-UV가 부적절한 parameterization인지(A), evidence 자체가 단일 injective chart를 지지 못하는지(B/C) — 를 하나의 constructor-level 결정으로 닫았다. 신규 `osn_gs/surface/torch_intrinsic_boundary_parameterization.py`: worklog 80의 dense chart boundary를 고정 convex 도메인(unit circle, 원 순회 순서·상대 arc-length 유지)으로 못박고, interior evidence는 3D 원시 kNN 그래프(전역 축 없음)로 discrete Laplace(Tutte) 방정식을 boundary Dirichlet 조건 아래 풀어 **injectivity를 수학적으로 보장**하는 대안 UV를 만든다.

**동일한** `fit_torch_visible_surface_lsq`(6×6, degree 2) 호출에 `initial_uv`만 PCA-UV/intrinsic-UV로 바꿔 real 4-region을 재적합한 결과, **intrinsic이 PCA-UV보다 4개 region 전부에서 더 나쁘다**: neighborhood preservation 0.53~0.70 → 0.39~0.63로 하락, fold% 21~36% → 47~70%로 상승, region 1/2에서 UV 근접 충돌 신규 대량 발생(2→79, 2→100). `knn_k` 8→20으로도(그래프 희소성 배제) 결과 불변. `interior_outside_boundary_count`는 설계대로 4개 전부 0.

원인을 직접 측정했다: owned evidence의 국소 PCA normal(k=10)이 region canonical normal과 60도 넘게 어긋나는 비율이 **16.3~37.4%**, canonical-normal 방향 두께/tangent extent 비율이 **0.169~0.546** — evidence 자신이 국소적으로도 단일 tangent plane을 따르지 않는, parameterization과 무관한 3D 형상 문제임을 확인했다.

**판정: A(PCA-UV를 intrinsic parameterization으로 교체)는 명확히 기각된다** — injectivity를 수학적으로 보장하는 대안조차 실측상 더 나쁘므로, 실패는 parameterization 선택의 문제가 아니다. **B와 C의 경계**: evidence가 단일 chart를 지지하지 못한다는 근거(국소 normal 불일치·두께비)는 이번에 직접 확보했으나, worklog 80의 `independent_chart_components`가 4개 region 전부 1이라 그 분해를 증명할 topology 해상도가 없다. 지시대로 임의 다중 chart 분할은 적용하지 않았고, production PCA-UV도 교체하지 않았다(판정이 A가 아니므로 전체 regression 미실행, 지시). 자세한 내용은 `docs/worklogs/81_intrinsic_boundary_conditioned_parameterization_decision.md` 참고.

**Worklog 82 addendum(evidence-scale surface-consistency chart-unit 분해: Region↔Chart 1:1 해체)**: worklog 81이 확정한 evidence 자체의 국소 비평탄성(normal 불일치 16.3~37.4%, 두께비 0.169~0.546)을 **Region과 Parametric Chart의 암묵적 1:1 관계 해체**로 다뤘다. 신규 `osn_gs/surface/torch_dense_surface_consistency_components.py`: region-owned dense evidence 위에 bounded-degree kNN(raw radius graph·dense clique 아님) candidate adjacency를 만들고, `torch_gaussian_manifold_affinity.py`의 same_surface 기준(normal alignment≥0.85, mutual tangent residual≤0.35, **재조정 없이 재사용**)을 통과한 edge만 `same_surface`로 승인한다. worklog 80의 이미 타입된 crease/frontier arc 양쪽에 걸친 edge는 기하가 아무리 좋아도 veto된다(재사용된 provenance, 새 separator 발명 아님). component는 `same_surface` edge만으로 만든 connected component(crease/ambiguous는 절대 병합 안 함); same_surface edge가 0인 점은 unresolved로 fail-closed, 내부 normal 불일치가 15% 넘는 component는 `non_manifold_suspected`로 표시해 chart 재료에서 제외한다.

`scripts/devtools/dense_surface_consistency_replay.py`로 region-owned evidence→component(신규)→component별 worklog 80 dense chart support(region 대신 component 단위로 적용)→worklog 79 coverage(변경 없음)→PCA-UV(worklog 81 확정, 대안 없음)→6×6 NURBS(변경 없음)→held-out 평가를 7개 real region 전체에 실행했다. **residual 정규화 선택을 직접 검증**: kNN spacing(선택값) 대신 대표-그래프 기본값 `tangent_major_scale`로 바꾸면 median residual이 0.34→0.63으로 더 엄격해져, 선택값이 이미 더 관대한 쪽이었음을 확인 — 파편화가 정규화 버그가 아니라 evidence 자체의 잡음이라는 것을 worklog 81과 독립적으로 재확인했다.

**실측: 7-region 합계 component 364개 → valid_supported 16(region 1/2/6), extrapolative 8, unsafe_geometry 9, no_chart 331(91%)** — worklog 79~81 전 라운드에서 정확히 0이었던 valid_supported가 처음 나왔다. 그러나 component 크기 중앙값은 3~6점(evidence 92~1035점 대비)으로 심하게 파편화됐고, 가장 큰 component(24~157점, `non_manifold_suspected=False`로 국소 일관성 자체는 통과)도 대부분 extrapolative/no_chart로 끝난다. region 4/5는 여전히 유효 chart 0개(worklog 80의 ambiguous branching/open topology 분류와 일치, 강제 참여 없음).

**판정 (B): evidence는 다중 chart 단위를 지지하지만, 현재 evidence-scale topology 해상도로는 그것을 chart 스케일 단위로 안전하게 조립하지 못한다.** valid_supported 16개는 (A) canonical 채택 또는 (C) evidence가 근본적으로 boundary-first parametric 가정에 부적합하다는 주장을 둘 다 기각하는 실재 증거다 — 다만 91%가 no_chart로 버려지는 표현을 그대로 canonical Region→Chart 계층으로 채택할 수는 없다. 자세한 내용은 `docs/worklogs/82_dense_surface_consistency_chart_unit_decomposition.md` 참고.

**Worklog 83 addendum(chart-scale topology/assembly: worklog 82 micro-component 조립)**: worklog 82의 91% no_chart 파편화(중앙값 3~6점)를 threshold/kNN/normal/residual 재조정 없이 다뤘다. 신규 `osn_gs/surface/torch_dense_chart_unit_assembly.py`: worklog 82 micro-component를 보수적 atomic 증거로 취급하고, **단일 pointwise edge가 아니라 aggregate 증거**로만 component-level adjacency를 만든다 — proximity gate(worklog 72 `_connect`의 2.5x multiplier 재사용) 통과 후, typed crease veto(worklog 82와 동일)가 없고, 3개 독립 aggregate 신호(normal alignment 평균≥0.85, 개별 same_surface correspondence 개수≥3, `measure_edge_support_occupancy`(worklog 76, 처음으로 acceptance 신호에 사용) 기준 빈 구간 없음) 중 **2개 이상**을 통과해야 `ACCEPTED`, 아니면 `AMBIGUOUS`로 명시 disclosure(병합·폐기 없음). fit 품질은 함수 입력에 없음(전용 테스트로 시그니처 검증).

`scripts/devtools/dense_chart_unit_assembly_replay.py`로 7개 real region 전체 실행한 결과: **micro-component 364개 → chart unit 178개(52% 감소)로 파편화는 해소**됐다(region당 evidence의 75~93%가 4점 이상 chart-unit 후보로 회수, 최대 239점 단일 unit까지 병합). 그러나 **no_chart 비율은 90.9%→91.0%로 사실상 그대로**다. **gap bridging을 직접 감사**: ACCEPTED edge 219개 전부 occupancy 신호를 실제로 통과했고(0개가 normal+correspondence만으로 수락), 관측되지 않은 공백을 건넌 병합은 없었다.

**병목이 이동했다**: 남은 no_chart의 원인이 이제 "micro-component가 너무 작음"이 아니라 worklog 80의 sparse macro-topology arc-typing 해상도 부족(38건)과 dense chart boundary 구성 자체의 실패(coverage 실패 27+self-intersecting 18=45건)다. valid_supported는 16→4로 줄었는데, 병합 자체의 결함이 아니라 더 커진 evidence 규모에서 기존 PCA-UV+6×6 fit(재검토 금지 대상)이 hold-out 기준을 만족시키기 어려워졌기 때문(예: region 6 unit 3은 74점으로 병합되고도 valid_supported 유지, p95 3.58 — 큰 assembled chart도 유효할 수 있다는 실증). region 4/5는 이번에도 valid chart 0개(worklog 80 분류와 일치).

**판정 (B, 세분화): local surface unit은 실재하고 aggregate 증거로 chart 스케일까지 조립 가능하지만(파편화는 해소), 그것을 최종 chart로 안전하게 완성할 만큼 macro-topology 해상도와 chart-boundary 구성 능력이 아직 충분하지 않다.** (A) 지지 안 됨(no_chart 비율 불변), (C) 다시 기각(assembly가 실재하는 대규모 coherent 증거를 gap bridging 없이 회수). 자세한 내용은 `docs/worklogs/83_dense_chart_unit_assembly.md` 참고.

**Worklog 84 addendum(chart-unit coherence audit + evidence-scale boundary 표현)**: worklog 83이 남긴 결합된 두 질문 — assembled unit이 진짜 coherent인가 over-merge인가, sparse macro node 없이 자기 evidence만으로 boundary topology를 세울 수 있는가 — 를 한 배치로 닫았다.

**coherence audit**: worklog 82의 `internal_normal_disagreement_fraction`(0.15 bound, 동일 공식)을 함수로 분리해 assembled unit 전체 규모에 재적용했다(fit 품질 아님, 순수 evidence 기반).

**evidence-scale boundary**: 첫 설계(`extract_dense_boundary_support`의 `_connect` closed-loop을 순서로 재사용)는 real 178개 unit에서 **0/178 materialize**로 실패했다 — worklog 71이 이미 기록한 한계(282개 중 17개만 closed_loop_recovered)를 그대로 재현함을 직접 측정 후 확인하고 재설계했다. 신규 `osn_gs/surface/torch_chart_unit_evidence_scale_boundary.py`: `extract_dense_boundary_support`는 candidate admission(worklog 77 predicate, 미변경)에만 쓰고, 승인된 candidate를 **unit 자신의 tangent plane에서 centroid 기준 각도순 정렬**한다(sparse 의존 없음) — `evaluate_closed_loop_geometry`(worklog 71)로 self-intersection 검증, **`measure_edge_support_occupancy`(worklog 76, 이번엔 gap-bridging을 실제로 막는 최종 안전장치로 사용)로 loop의 모든 edge(wrap-around 포함)에 빈 interior bin이 없는지 검증**한다 — 열린 조각을 직선으로 닫아버리는 경우를 self-intersection이 못 잡는 것을 이 장치가 대신 잡는다(half-ring fixture로 직접 검증).

**실측(evidence 3526점 가중, 7개 real region 전체): assembled/coherent 88.1%(178/178 unit 전부 coherent, over-merge 0건), materialized 1.5%, valid_supported 0.9%, unsafe 0.5%, no_chart 86.7%**(unsupported_closure 95건, no_dense_support 71건, coverage_failed 2건). **16→4 손실을 11개 unit 전부 직접 추적해 판별**: 5개는 valid_supported 유지, 6개는 전부 `unsupported_closure`로 실패 — `ambiguous_or_over_merged`는 0건.

**판정 (B): chart unit은 조립 가능하고 over-merge는 실재하지 않지만(C 기각), 관측 evidence는 대부분에 대해 안전한 boundary를 세우기엔 아직 불충분하다(A 기각).** 손실은 legitimate exposure이지 over-merge나 혼합이 아니다. sparse macro node 의존 제거 목표는 달성했으나, evidence-scale candidate 밀도 자체가 unit이 커질수록 전체 perimeter를 감싸기에 부족해진다는 더 정확한 병목이 드러났다. 자세한 내용은 `docs/worklogs/84_chart_unit_coherence_audit_and_evidence_scale_boundary.md` 참고.

**Worklog 85 addendum(evidence-scale local surface topology로 chart-unit boundary 재구성 — CLOSED)**: worklog 84의 centroid 각도 정렬이 일반적인 perimeter-topology 재구성이 아니라는 지적에 따라 **local 2-manifold adjacency graph** 기반 재구성으로 완전히 교체했다. `torch_dense_surface_consistency_components.py`에서 kNN+same_surface+crease-veto 로직을 `build_same_surface_adjacency()`로 분리(동작 불변, 기존 테스트 확인)한 뒤, `torch_chart_unit_evidence_scale_boundary.py`를 재작성: admitted boundary candidate(worklog 77, 미변경)들 자신에게 worklog 82의 동일 same_surface 기준(0.85/0.35, 미변경)을 적용해 진짜 adjacency graph를 만들고, **모든 정점이 degree 정확히 2인 connected component는 그래프 이론상 정확히 하나의 simple cycle**이므로 각도 계산·투영·hull에 준하는 연산 없이 그래프 walk만으로 순서를 얻는다. degree≥3은 `branch_detected`, degree≤1은 `open_fragment`로 disclosure(강제 해소 없음), 여러 유효 loop은 전부 독립 boundary로 인정.

**구현 중 실측으로 자체 결함 발견·수정**: worklog 82의 interior-mesh 기본값(k=8/cap=12)을 그대로 재사용했더니 합성 ring도 dense-clique이 되어 0/178 real unit이 materialize됐다 — "탐색 폭"과 "위상적 degree 불변량(curve는 degree>2 불가)"을 혼동한 것이었다. cap=2(위상 불변량, 유지)는 그대로 두고 탐색 폭만 무제한으로 열어 재검증했다(합성 ring 완벽 복원 재확인). `measure_edge_support_occupancy`(worklog 76)는 topology가 만들지 않은 것을 발명하는 데 쓰지 않고 독립적 최종 안전장치로만 유지했다.

**실측(evidence 3526점 가중, 7개 real region 전체): 진짜 boundary-support 부재 4.7%, evidence는 있으나 유효한 topology 없음 83.0%, 성공적으로 복원된 perimeter 0.5%**(worklog 84의 결함 있는 centroid 정렬 방식의 1.5%보다 낮음 — 퇴보가 아니라 정직한 결과: 각도 정렬은 위상 미검증 순서를 종종 우연히 통과시켰다). real unit 직접 확인 결과 admitted candidate가 하나의 연속 perimeter가 아니라 다수의 작은 loop(3~5점)/open fragment로 파편화돼 있음을 확인(예: region 1 최대 unit 140점 중 candidate 47개가 7개의 작은 loop+5개의 open fragment로 흩어짐, 지배적 perimeter 없음).

**판정: 현재 관측된 Gaussian evidence는 evidence-scale local topology를 통한 boundary-first constructor에 불충분하다.** sparse macro topology 의존은 완전히 제거했지만(worklog 84까지의 병목), 그 자리를 대신한 evidence 자체가 chart-unit 규모의 완결된 perimeter를 이루기에 밀집·연속적이지 않다. 지시대로 별도의 고립된 boundary 실험은 새로 시작하지 않는다. materialized evidence가 늘지 않아 canonical 채택 근거가 없으므로 전체 regression은 실행하지 않았다. 자세한 내용은 `docs/worklogs/85_evidence_scale_local_surface_topology_boundary.md` 참고.

**Worklog 86 addendum(partition-seam parametric chart-domain 계약 — 최종 go/no-go, LINE CLOSED)**: worklog 85가 닫은 "물리 evidence만으로 chart 전체 경계를 만든다"는 전제를 **physical surface termination ≠ parametric chart boundary**라는 재설계로 최종 검증했다. 신규 `osn_gs/surface/torch_chart_unit_partition_seam.py`는 worklog 85의 물리 재구성(`materialize_chart_unit_boundary`)을 완전히 미변경으로 감싼다: 물리 loop이 이미 닫히면 그대로 사용(seam 시도 없음). 닫힌 loop이 없고 **정확히 하나의 열린 physical fragment**만 있을 때만, 그 두 끝점을 unit 자신의 interior adjacency graph(worklog 82의 원래 interior-mesh 기본값 k=8/cap=12, 미변경)를 통한 최단 경로로 잇는다 — 모든 seam edge는 진짜 관측된 same_surface adjacency이며, 다른 boundary candidate는 경유지에서 제외하고 기존 crease veto도 동일 적용된다. 열린 fragment가 2개 이상이면 조합적으로 모호하므로 시도하지 않고 명시 disclosure한다. seam이 만든 loop도 worklog 85와 동일한 self-intersection+occupancy 안전장치, 그다음 worklog 79 coverage로 검증하며, fit 오차·held-out p95는 seam 결정 어디에도 관여하지 않는다.

**실측(evidence 3526점 가중, 7개 real region 전체): coherent chart-unit coverage 88.1%, partitioned domain coverage 1.0%, valid_supported 0.5%, unresolved 87.1%.** materialized unit이 worklog 85의 4개(전부 physical_only)에서 **8개(physical_only 4 + physical_plus_seam 4)로 2배**, valid_supported도 2→4로 2배 증가 — seam이 fabrication 없이 실제 domain을 늘린다는 것은 실측으로 확인됐다. 그러나 178개 unit 전체 상태 분포를 보면 남은 실패의 87%가 seam으로 풀 수 없는 원인이다: `no_dense_support`(71, 물리 evidence 자체가 없음), `multi_fragment_unresolved`(23, fragment 2개 이상이라 의도적 미시도), `unsupported_closure`+`coverage_failed`+`self_intersecting`(67, loop은 만들어져도 evidence 밀도·정합성이 최종 안전장치를 통과 못함) — 전부 evidence 자체의 밀도·연속성·정합성 문제이지 seam 알고리즘으로 고쳐지지 않는다.

**판정: NO-GO.** partition seam 메커니즘은 건전하고 fabrication 없이 real domain을 늘리지만 산출량(1.0%)이 production 채택 규모에 크게 못 미친다. **현재 학습된 Gaussian evidence는 의도한 parametric visible-surface 표현에 불충분하다는 결론으로 boundary-first visible-constructor 재설계를 중단한다.** 자세한 내용은 `docs/worklogs/86_partition_seam_parametric_chart_domain.md` 참고.

**Worklog 87 addendum(partition_seam을 1급 parametric-boundary 타입으로 — historical, verdict superseded by Worklog 88)**: worklog 86의 "열린 fragment 정확히 1개+seam 1개"라는 제한을 없애고 partition_seam을 physical_termination/crease/observation_frontier와 동등한 1급 타입으로 일반화했다. `torch_chart_unit_partition_seam.py` 전면 재작성: 모든 독립 물리 loop은 각자 자기 domain이 되고(한 unit이 여러 domain 가능), 소비되지 않은 fragment·고립 candidate는 **결정론적 daisy-chain**(각 조각의 첫 candidate stable id로 정렬, 기하·각도 아님)으로 최대 1개 추가 domain을 만든다 — fragment 1개+seam 1개(worklog 86의 특수 사례), fragment 여러 개+seam 여러 개, physical loop 없이 seam만으로 닫히는 **seam-dominated** domain까지 하나의 메커니즘으로 지원한다. 체인 중 어느 seam이라도 없으면 전체가 fail-closed(부분 봉합·대체 순서 시도 없음). candidate 최소 개수를 3→2로 정정했다(fragment는 2점으로 성립, 물리 loop 전용 시절의 유물 제거).

**구현 중 실측으로 자체 결함 발견·수정**: 첫 구현이 "다른 모든 candidate를 seam 경유지에서 제외"했는데, real unit(140점, candidate 47개)에서 그 제외 자체가 실제 연결(제외 없이 BFS하면 38/140 도달, 목표점 포함)을 막는 병목이었음을 직접 확인했다. 과제 제약 어디에도 "다른 typed candidate를 경유하면 안 된다"는 요구는 없었고, 그 경유 edge도 실제 관측된 same_surface adjacency이지 발명된 chord가 아니므로, 제외 범위를 "이미 체인에 배치된 노드"로만 좁히고 중복-정점 명시 검사로 안전성을 보강했다.

**실측(evidence 3526점 가중, 7개 real region 전체): coherent 88.1%, partitioned domain 1.0%, valid_supported 0.4%, unresolved 87.1%.** boundary 구성에 **seam_dominated 0.5%**가 실제로 나타나 일반화가 새로운 종류의 domain을 만들어냄을 증명했지만, materialize된 unit 총수(8/178)는 worklog 86과 거의 동일하다. **worklog 86의 no_dense_support(71개)/multi_fragment_unresolved(23개) 회수율은 둘 다 0**: no_dense_support는 71개 중 64개가 candidate 정확히 0개라 seam을 시작할 anchor 자체가 없어 구조적으로 회수 불가능함을 실측 확인했고(prerequisite 제거는 "시도 조건"에 관한 것이지 evidence 없이 geometry를 발명하는 것이 될 수 없다), multi_fragment_unresolved는 daisy-chain의 **모든** 구간이 동시에 성공해야 하므로 조각이 많을수록 real data에서 어느 한 구간이 거의 항상 끊긴다(exclusion 버그 수정 후에도 실패 지점만 이동, 총량 불변).

**판정: NO-GO.** 아키텍처 일반화는 완성했으나 실질 산출량은 제한된 버전과 동일 — 병목은 여전히 evidence 자체의 밀도·연속성이다. 자세한 내용은 `docs/worklogs/87_general_partition_seam_final_go_no_go.md` 참고.

**Worklog 79~87의 당시 결론(Worklog 88이 판정 근거를 supersede).** chart-domain coverage 계약(79) → 표현 재설계(80) → parameterization 결정(81) → chart-unit 분해(82) → 조립(83) → coherence audit + evidence-scale boundary(84) → local surface topology 재구성(85) → 제한된 partition seam(86) → 일반화된 partition seam(87)까지 아홉 라운드에 걸쳐 매번 실제 결함을 찾아 고쳤고 매번 실측으로 검증했지만, 최종 결론은 일관되게 하나였다 — **관측된 Gaussian evidence의 밀도·연속성·정합성이 chart-scale parametric boundary-first 표현을 대량으로 지지하지 못한다.** 이 라인을 새 근거 없이 재개하지 않는다. 다음 방향은 boundary 재구성 자체가 아니라, evidence 밀도/연속성이 왜 부족한지(예: 학습·ADC 단계에서의 evidence 분포 특성) 또는 boundary-first 가정 자체를 재검토하는 상위 단계여야 한다.


**Worklog 88 addendum(chart-unit membership cut 방향 — historical verdict, Worklog 89가 topology construction을 교정)**: Worklog 87 구현은 보존하지만 global NO-GO의 근거였던 candidate-anchor/stable-ID daisy-chain은 최종 architecture test로 기각했다. 신규 실험 모듈 torch_chart_unit_topology_partition_boundary.py는 Worklog 82 기본값 그대로 full-region evidence-scale same-surface graph를 한 번 만들고, Worklog 83 chart-unit membership의 induced subgraph에서 PCA-UV tangent rotation system과 실제 graph half-edge만으로 ordered outer-face loop를 복원한다. Worklog 77 candidate는 admission 조건이 아니라 physical provenance/진단일 뿐이고, 나머지 edge는 partition_seam이다. stable ID는 동률 tie-break/cycle canonicalization에만 쓰며 adjacency를 만들지 않는다. self-intersection, occupancy, Worklog 79 coverage는 전부 fail-closed다.

**7-region 실측(3526 evidence)**: coherent 3108(88.15%), cut-boundary recoverable 43(1.22%, coherent 대비 1.38%), physical-only/mixed/seam-only 40/0/3 evidence, valid/extrapolative/unsafe/unresolved 31/0/12/3065, evidence-weighted held-out p95 3.754. Candidate-0 unit도 실제 동일 경로에서 평가되어 seam-only domain 1개를 만들었으므로 first-class contract는 충족한다. 다만 Worklog 87 본문의 candidate-0 64개는 재현되지 않으며 동일 artifact/코드의 정확한 수는 71개(전부 size 2~3)다: 61개 closed topology 부족, 5개 Worklog 79 coverage 실패, 3개 occupancy 실패, 2개 회수(typed physical-only 1 + seam-only 1, 둘 다 unsafe).

**당시 판정: NO-GO — Worklog 89가 supersede.** 이 수치는 global-PCA/induced-outer-face construction의 historical 결과이며 최종 architecture 근거가 아니다. 자세한 내용은 docs/worklogs/88_chart_unit_topology_partition_boundary_final_go_no_go.md 참고.

**Worklog 89 addendum(full-region observed face → chart-unit membership incidence — 실제 최종 go/no-go, SUPERSEDES WORKLOG 88 VERDICT)**: Worklog 88의 candidate-independent membership-cut 방향은 유지하되, topology construction에서 global PCA-UV와 unit induced-subgraph의 largest outer-face 선택을 제거했다. Worklog 82 full-region same-surface graph 전체에 각 evidence vertex의 기존 covariance local normal/tangent frame으로 rotation system을 만들고 observed face를 membership 이전에 복원한다. 그 뒤 unit-supported face incidence 2는 chart interior, incidence 1 half-edge는 boundary로 분류한다. 기존 physical/crease/frontier provenance를 보존하고, full-region 양면 observed surface를 membership이 자르는 edge만 `partition_seam`이다. 모든 독립 loop와 `outer_boundary`/`interior_boundary` role을 보존하며 open/branching/non-manifold/interior-loop 미표현은 fail-closed한다. Worklog 79 coverage→기존 PCA-UV→6x6 NURBS→held-out 체인과 threshold/kNN/normal/residual은 미변경이다.

**7-region 실측(3526 evidence)**: coherent 3108(88.15%), cut-boundary recoverable 6(0.170%, coherent 대비 0.193%), physical-only/mixed/seam-only 6/0/0, valid/extrapolative/unsafe/unresolved 0/0/6/3102, evidence-weighted held-out p95 2.675. 178 unit 중 10개 outer loop만 증명됐고 안전 domain은 0이다. Corrected full-region provenance 정의의 candidate-0은 38 unit/124 evidence이며 1 unit/3 evidence만 physical-only unsafe로 회수됐다. Worklog 87 본문의 64는 저장 artifact와 일치하지 않고 실제 집합은 71 unit/164 evidence다. 이 71개를 동일 region/unit index로 전부 대응한 fate는 open/non-manifold 61, coverage 실패 4, occupancy 실패 3, untyped physical exterior 1, physical-only unsafe 회수 2다.

**실제 최종 판정: NO-GO.** coherent evidence의 99.807%가 정확한 full-region-face→membership-incidence 계약에서도 closed coverage-valid domain에 도달하지 못했고 나머지도 전부 unsafe였다. Region→Charts canonical 통합은 하지 않으며 visible-constructor boundary redesign을 종료한다. 이 결과 뒤에 다른 boundary 실험을 시작하지 않는다. 자세한 내용은 `docs/worklogs/89_full_region_face_membership_incidence_final_go_no_go.md` 참고.

**Worklog 90 addendum(upstream root-cause attribution — boundary algorithm 고정)**: Worklog 89의 full-region local-frame face→membership-incidence algorithm, Worklogs 82~84, region ownership, visible Gaussian training, PCA-UV, 6×6 NURBS는 전부 고정했다. 새 `torch_chart_unit_surface_topology_attribution.py`는 center graph와 기존 covariance 1σ tangent footprint/normal thickness를 읽기 전용 비교할 뿐 edge/relation/face/loop/closure를 만들지 않는다.

**7-region 실측(3526 evidence)**: Worklog 89 `chart_unit_cut_non_manifold` coherent unit 전부인 167 unit/3073 evidence를 귀속했다. primary evidence는 `MULTILAYER_OR_VOLUMETRIC` 2810(91.44%), `CENTER_UNDERSAMPLING` 141(4.59%), `TRUE_SUPPORT_GAP` 95(3.09%), `GRAPH_TO_SURFACE_TOPOLOGY_MISMATCH` 27(0.88%), `RELATION_FALSE_NEGATIVE` 0이다. center spacing/equivalent tangent scale 1.247, compatible footprint coverage 49.20%, footprint continuation인데 missing same-surface edge 11.60%, relation-FN node fraction 6.87%, layer ambiguity 82.88%, footprint representation에서 local complex 추가 조사가 plausible한 evidence는 168(5.47%)뿐이다.

**Decision C.** covariance footprint를 포함해도 normal-depth multilayer/volumetric ambiguity와 일부 true gap이 압도적이다. production을 footprint graph로 교체하거나 Worklog 82 relation threshold를 조정하지 않는다. 다음 upstream 조사는 depth/visibility ordering, ADC clone/split/prune 전후 covariance·spacing, local normal-depth layer multiplicity와 confidence/ownership, conflict 위치의 gradient/opacity/radius/birth lineage를 추적한다. boundary topology heuristic은 재개하지 않는다. 자세한 내용은 `docs/worklogs/90_surface_topology_root_cause_attribution.md` 참고.

**Worklog 91 addendum(temporal + lineage 귀속 — boundary algorithm 계속 고정)**: Worklog 90의 covariance-footprint 로직을 그대로 재사용해 `MULTILAYER_OR_VOLUMETRIC` evidence를 (a) center 자체의 실제 다층 분포(각 Gaussian 자신의 covariance orientation과 독립적인 position-only PCA depth clustering)와 (b) covariance frame만의 표현 불일치로 분리했다. `baseline_compatible` checkpoint 5개(600/2900/3000/3100/final)를 전부 재생하고, clone/split마다 새로 발급되는 `stable_gaussian_ids` 집합 차이로 checkpoint 구간별 ADC lineage(신규/소멸)를 훈련 로그의 누적 clone/split/prune 카운터와 대조했다. 실사용 train camera 161개로 dominant competing-layer 위치의 동시 가시성과 view-space depth 분리도 측정했다.

**5-checkpoint 실측**: iteration 600은 실패 topology unit 0개(densification 초기). 2900/3000/3100/final에서 true-center multilayer 비율은 각각 93.49%/92.52%/93.91%/95.90%로, 실패 evidence 총량이 3073→9889→11368→6497로 크게 변하는 동안에도 92.5%~95.9% 범위에서 일정했다. covariance-only ambiguity는 대칭적으로 4.10%~7.48%로 항상 소수였다. Lineage 분석은 dominant competing-layer unit(최종 region 3/unit 0, 1386 evidence)의 member 대부분이 초기 densification 파동(600→2900 구간, 239개)에서 이미 태어나 이후 pruning(3100→final 구간 405,767개 소멸)에서도 유지됨을 보였다. Visibility 분석은 dominant 두 layer(1378 vs 3 member)가 train camera 161개 중 14개(8.7%)에서 동시에 보이고, 그 카메라들에서 평균 view-space depth 분리 0.277(범위 0.211~0.387)임을 확인해 이 구조가 view-conditioned 가림이 아니라 실제 competing-depth 구조임을 뒷받침했다.

**Decision A(당시 판정, Worklog 92가 재판정).** true center-distribution multilayer가 checkpoint 4개 전 구간에서 압도적이고 안정적이며, covariance frame이 surface normal 표현으로서 부적합하다는 B 가설은 기각한다. production topology/Worklog 82 relation/Worklog 89 boundary는 미변경이며 새 constructor·threshold도 추가하지 않았다. 다음 방향은 boundary 재작업이 아니라 ADC/densification/pruning이 이 competing-layer 구조를 왜 만들고 유지하는지 조사하는 것이다. 자세한 내용은 `docs/worklogs/91_surface_topology_temporal_lineage_attribution.md` 참고.

**Worklog 92 addendum(global-SVD confound 제거 후 최종 center-geometry 귀속, Worklog 91 Decision A 재판정 — boundary algorithm 계속 고정)**: Worklog 91은 chart unit 전체 member center에 SVD 평면 하나를 적합해 signed offset을 gap-clustering했다 — 곡률 있는 단일 표면이 global 평면 하나 기준으로는 여러 depth band처럼 보일 수 있다는 confound가 지적됐다. 신규 `torch_chart_unit_local_center_geometry_attribution.py`는 각 node의 local kNN(k=8, Worklog 82의 기존 상수 재사용, 새 sweep 아님) 이웃만으로 diagnostic-only local 평면을 적합하고, gap이 mode 경계로 인정되려면 local median gap의 3배·neighborhood extent의 20% 초과에 더해 **분리되는 양쪽 side 각각의 내부 spread보다 1.5배 이상 커야 하는 silhouette 스타일 tightness 검증**을 추가했다(k~8 소표본에서 순수 gap-ratio만으로는 매끄러운 단일 분포도 order-statistic 우연으로 3배 gap을 만든다는 것을 uniform/Gaussian thickness noise 5개 시드로 직접 확인 후 반영). 다중-모드성은 공간적으로 인접한 이웃 neighborhood에서도 재현될 때만 `TRUE_PERSISTENT_TWO_LAYER`/`MULTI_LAYER`로 인정하고, 고립된 단일 neighborhood의 우연한 분리는 `LOCALLY_SINGLE_CURVED_SHEET`로, neighborhood/mode population이 너무 작으면 `SPARSE_SATELLITE_OR_OUTLIER`로 처리한다. 각 Gaussian의 covariance normal/tangent/scale은 이 모듈 어디에서도 읽지 않는다(AST 검사로 import/함수 시그니처 직접 확인).

**실측(checkpoint 2900/3000/3100/final, Worklog 90의 `MULTILAYER_OR_VOLUMETRIC` evidence 전체를 local classifier로 재분류)**: `TRUE_PERSISTENT_TWO_LAYER`는 2.06%/1.74%/1.08%/1.50%로 4개 checkpoint 전부에서 소수이고 `TRUE_PERSISTENT_MULTI_LAYER`는 0%다. `LOCALLY_THICK_UNIMODAL_SHEET`(62.2~70.2%)와 `LOCALLY_SINGLE_CURVED_SHEET`(25.1~31.7%)가 대부분을 차지한다. Persistent layer 상세 조사(2900/final, 총 17/22개)는 population 2~7개의 소규모 구조만 보였고, opacity 평균 0.57~0.59, 20개 sample 카메라 전부에서 가시적(non-negligible rendered contribution)이지만 Worklog 91이 보고한 1378-vs-3 같은 대규모 split은 재현되지 않았다.

**Decision E.** Curvature와 sparse-satellite를 제거한 뒤 남는 진짜 persistent multi-layer center geometry는 checkpoint 전 구간에서 1~2%대 소수다. Worklog 91이 92.5~95.9%로 보고한 multilayer 대부분은 global 단일 SVD 평면이 곡률과 두꺼운 단일 sheet를 잘못 여러 layer로 쪼갠 결과였다. **Worklog 91 근거로 ADC/densification/pruning을 architecture target으로 채택하지 않는다.** 이 결과가 Worklog 90의 covariance-footprint 기반 `MULTILAYER_OR_VOLUMETRIC` primary-cause 분류 자체를 무효화하지는 않으나(covariance 기준으로는 여전히 압도적으로 겹침), 그 겹침의 원인 해석이 center 기준으로는 대부분 곡률/두께이지 진짜 별개 layer가 아니라는 뜻이다. 다음 단계 판단(covariance frame의 surface-normal 표현 적합성 재검토 여부 포함)은 이 배치의 범위를 넘어서므로 여기서 결정하지 않는다. 지시대로 또 다른 boundary 실험은 제안하지 않는다. 자세한 내용은 `docs/worklogs/92_local_center_geometry_attribution.md` 참고.

**Worklog 93 addendum(thick/curved center band의 latent midsurface 회수 가능성 귀속 — boundary algorithm 계속 고정)**: Worklog 92의 `LOCALLY_THICK_UNIMODAL_SHEET`(62.2~70.2%)/`LOCALLY_SINGLE_CURVED_SHEET`(25.1~31.7%) — Worklog 90 `MULTILAYER_OR_VOLUMETRIC` evidence 대부분 — 이 recoverable latent 2D midsurface를 갖는지 측정했다. 신규 `torch_chart_unit_latent_midsurface_attribution.py`는 Worklog 92의 local kNN(k=8) diagnostic 평면에 quadratic height field(`z=au²+buv+cv²+du+ev`)를 추가로 적합해 local curvature(Hessian trace)를 추정하고, 이웃 local patch 간 position/tangent/curvature 일치도, diagnostic-only projection displacement(model에 저장하지 않음, 입력 tensor 불변을 테스트로 직접 검증), raw 대 diagnostic-projected position-only same-surface adjacency(Worklog 82의 0.85/0.35 threshold를 covariance 대신 position-fit normal에 그대로 재사용, 성공 방향 조정 없음) 비교로 open/non-manifold·valid local face incidence를 측정하고, diagnostic curvature가 raw curvature의 10% 미만이면 flattening으로 fail-closed 처리하며, projection이 unit 자신의 관측 support band 안에 머무는지 report한다. Gaussian covariance normal/tangent/scale은 이 모듈 어디에서도 읽지 않는다(AST 검사로 직접 확인). Gaussian xyz는 수정하지 않는다.

**실측(checkpoint 2900/3000/3100/final, 대상 evidence 2609~10066개)**: manifold 개선 비율(diagnostic valid face incidence ≥ raw valid face incidence) 99.8~100%, curvature 보존 비율 86.6~91.9%, valid local face incidence가 raw 30.5~36.3%에서 diagnostic thickness-collapse 후 63.0~72.7%로 거의 2배, open/non-manifold 비율은 63.9~69.5%에서 27.3~37.0%로 절반 가까이 감소, support band fidelity 86.0~91.5%(회수된 latent surface가 관측 evidence를 크게 벗어나지 않음), neighbor tangent agreement 0.77~0.80, neighborhood preservation 0.92~0.94다. 4개 checkpoint 전부에서 결과가 안정적이다.

**Decision A: LATENT_SURFACE_RECOVERABLE.** Worklog 92의 thick/curved 대다수 evidence는 curvature를 보존하면서(global planarization 아님) manifold topology가 substantially 개선되는 안정적인 latent 2D 표면을 갖는다. **raw Gaussian center 자체가 visible surface topology를 위한 잘못된 geometry representation이며, 다음 architecture target은 boundary 추출 이전의 명시적 latent-surface evidence representation이어야 한다.** 이 배치는 그 representation 자체를 만들지 않았다 — position-only diagnostic quadratic patch 적합과 disclosure만 수행했다. 지시대로 새 boundary 실험이나 position-PCA-normal 대체 실험으로 결론짓지 않는다. 자세한 내용은 `docs/worklogs/93_latent_midsurface_recoverability_attribution.md` 참고.

**Worklog 94 addendum(bounded surface-evidence representation architecture gate — root-cause 진단에서 architecture 결정으로 전환, 단일 배치, 후속 고립 진단 없음)**: Worklog 93의 latent-surface 회수 가능성을 동기로만 삼아, 고정된 4개 representation을 같은 7개 real region·같은 unmodified downstream constructor contract로 비교했다. Visible Gaussian training, ADC, region ownership, Worklog 79 coverage, PCA-UV, 6×6 NURBS fitting, held-out evaluation, 기존 safety criteria는 모두 미변경이다. 신규 `torch_surface_evidence_representation_gate.py`는 각 representation을 `(raw_positions, raw_covariance) -> (adapted_positions, adapted_covariance)` 함수 하나로 정의하고, `build_chart_unit_face_topology_context`/`build_full_region_surface_face_topology`가 내부적으로 `extract_covariance_frame(covariance)`만을 유일한 기하 입력으로 쓴다는 것을 확인한 뒤 이 두 값만 교체했다 — 나머지 constructor 체인(`materialize_chart_unit_cut_boundaries`, `evaluate_fit`)은 기존 함수를 그대로 호출한다. Region 형성/ownership은 raw evidence로 한 번만 계산해 4개 representation이 공유한다. **A RAW_CENTER_BASELINE**(pure pass-through), **B CENTER_LATENT_SURFACE**(Worklog 92/93과 동일 local kNN(k=8) diagnostic 평면으로 독립 투영을 만든 뒤 cross-neighborhood consensus averaging 1-pass를 강제 적용 — 독립 per-point projection을 그대로 최종 representation으로 쓰지 않음, covariance는 latent tangent frame으로 재구성), **C COVARIANCE_SURFEL_SUPPORT**(position/orientation 불변, covariance normal을 ground-truth로 가정하지 않고 footprint reach만 별도 disclosure), **D HYBRID_LATENT_PLUS_SUPPORT**(B의 latent 위치 + raw covariance footprint의 in-plane 크기만 support extent로 반영)를 fallback 없이(실패한 unit이 다른 representation으로 재시도되지 않음) 각각 독립 실행했다. Real replay outcome으로 representation parameter를 튜닝하지 않았고, Worklog 82 threshold/NURBS capacity/PCA-UV/ADC/training은 어디에서도 건드리지 않았다.

**실측(checkpoint 2900/final, 7-region 실측, evidence 3526/7774)**: C는 두 checkpoint 모두 A와 수치가 정확히 동일하다(constructor가 covariance normal만 읽고 footprint 크기를 아직 소비하지 않으므로 C의 disclosure-only 설계가 의도대로 동작함을 실측으로 확인). D는 두 checkpoint 모두 B와 거의 동일하다(topology/coverage 결정이 position에 지배되고 covariance in-plane 크기 차이는 구조를 바꾸지 못함). B/D는 recoverable evidence를 A/C 대비 2.8~5.1배 늘리지만(0.170%→0.482%, 0.090%→0.463%) **valid_supported는 네 representation 전부에서 0.2% 미만**이고(2900: 전부 0%, final: A/C 0.051%, B/D 0.154%), **unresolved는 A/C 83.7~88.0%, B/D 83.7~86.2%로 여전히 압도적**이다. B/D는 held-out p95를 2.0~6.9배 악화시키고(2900: 2.67→18.54) geometry displacement가 local spacing의 0.59배에 달해, 소량의 추가 recoverable evidence가 fit 품질 저하와 실질적 geometry 이동을 대가로 얻어진다. Runtime은 B/D가 A/C보다 20~33% 더 걸리고 2900에서 peak memory는 최대 4.4배 크다.

**Decision 3.** 네 representation 후보 모두 coherent evidence의 대다수를 unresolved 또는 unsafe로 남긴다 — B/D가 recoverable을 5배 늘려도 unresolved 감소폭은 1.5%p 안팎이고 valid_supported는 어느 representation에서도 0.2%를 넘지 않는다. **Constructor-level 재설계(representation 교체)는 이 4개 후보 범위에서 문제를 풀지 못하므로 중단한다.** 다음 architecture target은 boundary/representation이 아니라 **training 중 visible geometric evidence가 어떻게 생성되는지(upstream)**로 옮긴다. 지시대로 또 다른 고립된 representation 진단은 이어가지 않는다. 자세한 내용은 `docs/worklogs/94_surface_evidence_representation_gate.md` 참고.

**Worklog 95 addendum(latent-surface curve-network constructor 프로토타입 — 첫 end-to-end 구현)**: Worklog 89/94의 raw Gaussian-center connectivity 의존(Worklog 82 same-surface kNN graph, chart-unit assembly, full-region face-incidence topology)을 완전히 제거한 새 constructor 경로를 처음부터 끝까지 구현했다. Visible Gaussian training, ADC, region ownership, 기존 NURBS fitter(`fit_torch_visible_surface_lsq`)는 미변경이다. 파이프라인: region-owned Gaussian → `torch_latent_surface_support.py`(MLS-style position-only local surface estimator, k=16 weighted-PCA query, 2-iteration 보정, covariance 미사용, query 시 support-radius/effective-neighbor-count/planarity 세 조건으로 명시적 `supported` 판정) → `torch_latent_surface_curve_network.py`(이미 존재하는 Worklog 79/80 sparse parametric chart boundary를 seed curve로 재사용, 각 seed edge를 6-step으로 densify하며 매 step MLS 투영·미지원 시 즉시 절단, seed 지점마다 surface-following transversal curve를 12-step 안쪽으로 걷고 미지원 시 절단, transversal curve들의 동일 깊이 지점을 잇는 interior rung curve 생성 — convex hull/bounding box/PCA-rectangle/alpha-shape 등 어떤 gap-closing fallback도 없음) → 기존 NURBS fitter(BASE_GRID=6/degree=2, Worklog 87과 동일 규약 재사용) → held-out 검증(Worklog 87의 PCA-UV checkerboard `_holdout` 분할을 그대로 재사용해 train 절반만으로 latent surface와 curve network를 짓고 held 절반으로만 평가, EXTRAPOLATION_BOUND=4.0 등 기존 classification 기준 재사용). Real replay 결과로 threshold를 조정하지 않았다(held-out 분리 도입은 방법론적 정정이지 outcome 기반 튜닝이 아니다).

**실측(checkpoint 2900/final, 7-region 실측, evidence 3526/7774)**: usable seed curve가 있는 region 5/7, curve network를 만든 region 4/7, materialized patch 4개(양쪽 checkpoint 동일). **valid_supported가 Worklog 89/94 baseline(RAW_CENTER_BASELINE)의 0.000%/0.051%에서 2.64%/11.95%로 nonzero가 되고, unresolved가 87.98%/84.73%에서 42.60%/32.51%로 절반 이하로 줄었다.** 다만 unresolved 감소분의 대부분은 extrapolative(40.05%/31.12%)와 unsafe_geometry(14.72%/24.43%)로 흡수되며, held-out p95(7.96/7.76)는 baseline보다 높다(baseline RAW_CENTER_BASELINE의 2.67~3.90보다 나쁨). Train-only 절반으로 지은 latent surface 기준으로 전체 region evidence의 55.98~59.53%가 미지원으로 보고돼, checkerboard-분할된 sparse train set에는 현재 support-radius(local spacing의 3배)가 다소 타이트함을 실측이 보여준다. Region 3/4/5는 curve network를 만들지 못한다(3은 seed walk 즉시 실패, 4/5는 eligible chart 자체가 없음, Worklog 79/80 이후 그대로).

이 배치는 "raw Gaussian-center connectivity 제거 + latent surface 기반 curve construction이 geometrically supported NURBS patch 수를 material하게 늘리는가"라는 architecture 질문에 **부분적으로 그렇다**는 첫 실측 근거를 제공한다 — valid_supported가 0에서 nonzero로, unresolved가 절반 이하로 줄었으나, 대다수 evidence는 여전히 extrapolative/unsafe이고 7개 region 중 3개는 curve network 자체를 만들지 못한다. 자세한 내용은 `docs/worklogs/95_latent_surface_curve_network_constructor_prototype.md` 참고.

**Worklog 96 addendum(latent-surface curve-network constructor 아키텍처 완성 — construction 성공, safe NURBS 미개선)**: Worklog 95의 legacy `eligible_parametric_chart_boundary` entrance gate를 제거하고 4개 architecture 한계를 함께 해결했다. Visible Gaussian training, ADC, region ownership, 기존 NURBS fitter는 미변경이다. 신규 `torch_latent_surface_seed_curves.py`는 boundary/crease/frontier seed(기존 chart segment_kind 그대로 매핑)가 존재하면 우선·보존하고, 존재하지 않을 때만 farthest-point-sampled interior anchor(고정 6개)로 fallback한다 — anchor는 시작점일 뿐이고 anchor 간 raw Gaussian-center connectivity는 전혀 생기지 않는다. 신규 `torch_latent_surface_curve_tracer.py`는 이전 방향을 새 local tangent plane에 투영하고 실제 이동량으로 재접지하는 parallel-transport 스타일 continuation을 쓴다(매 step PCA axis 부호 재선택 없음), `sample_segment_continuous_support`로 양 끝점만 지지된 segment를 거부한다(중간 sample 전부 지지 요구). 신규 `torch_latent_surface_curve_families.py`는 seed마다 family V(transversal curve)와 family U(연속 지지 검증된 rung)를 만들고, 고정 계약(family당 curve≥2, 서로 다른 depth 2개 이상으로 연결된 2×2 correspondence≥1 — 실측으로 튜닝하지 않음)을 충족하는 seed만 독립 block으로 승격한다. Block 판정은 NURBS fit 이전에 순수 구조로만 결정되며(모듈이 NURBS를 import하지 않음을 테스트로 확인) fit 오차로 나중에 나누지 않는다. Seed 하나당 block 하나이므로 한 region이 여러 독립 NURBS patch를 만들 수 있다(one-region-one-patch 가정 제거). 두 조건을 분리 보고했다: **A. ALL_VISIBLE_EVIDENCE_CONSTRUCTION**(전체 evidence로 production 능력 측정)과 **B. HELD_OUT_VALIDATION**(Worklog 87 checkerboard split, Worklog 95의 support threshold를 그대로 유지 — sparse train이라고 완화하지 않음).

**실측(checkpoint 2900/final, 7-region)**: construction 측면에서 usable seed가 있는 region 5/7→7/7·6/7, valid curve network 4/7→5/7·6/7로 늘고, 두 checkpoint 모두 legacy gate로 막혔던 region이 1개씩 interior fallback으로 새로 구성에 성공하며, multi-patch region이 3개·2개 나온다. Held-out(evidence-weighted, Worklog 87/89/94/95와 동일 규약): unresolved는 2900에서 크게 줄었다(42.60%→20.31%, final은 32.51%→33.67%로 거의 동일). 그러나 **valid_supported는 Worklog 95를 넘지 못했다**(2900: 2.64%→2.72% 거의 그대로, final: 11.95%→9.26% 소폭 하락). **extrapolative+unsafe 합계는 두 checkpoint 모두 늘었다**(2900: 54.8%→77.0%, final: 55.6%→57.1%) — 새로 구성에 성공한 region의 patch 대부분이 extrapolative로 분류되면서, unresolved 감소분이 valid_supported가 아니라 extrapolative로 흡수됐다.

**결정.** Condition 1~3(legacy gate 미의존, 연속 지지, multi-patch 구조)은 충족된다. Condition 4(valid_supported가 raw baseline보다 material하게 높음)도 raw-center baseline 대비로는 충족되지만(54~180배) Worklog 95 대비 개선은 아니다. **Condition 5(extrapolative/unsafe가 더 공격적인 구성만으로 단순 증가하지 않아야 함)는 충족되지 않는다.** 지시된 decision tree의 두 번째 분기에 따라: curve-network 구성은 이제 널리 가능해졌지만 safe NURBS는 개선되지 않았으므로, **더 이상 새 curve-seeding heuristic을 추가하지 않는다.** 병목은 curve construction이 아니라 **downstream parametric fitting/patch representation**(PCA-UV 6×6 NURBS가 latent-surface curve-network 표본을 안전하게 소화하지 못하는 것)으로 이동했다고 보고한다. 새 constructor를 production architecture로 지명하지 않으며, 또 다른 고립된 seed-rule/threshold 진단으로 잇지 않는다. 자세한 내용은 `docs/worklogs/96_latent_surface_curve_network_constructor_completion.md` 참고.

**Worklog 97 addendum(curve-network-native NURBS fitting — parameterization 유효성 검증, Decision C)**: Worklog 96의 coherent curve-network block에 대해, PCA를 전혀 호출하지 않고 curve network 자체에서 (u,v)를 유도하는 fitting path를 구현했다. NURBS degree(2)·control grid(6×6)·Worklog 95/96 latent-surface support/seeding/tracing/continuous support/family construction/block decomposition·held-out 규약은 모두 미변경이다. 신규 `torch_curve_network_uv_parameterization.py`: family V(transversal trace)마다 u는 그 trace가 시작한 seed curve 위 chord-length 위치를 [0,1]로 정규화한 고정값(`C_i(v) ≈ S(u_i, v)`), family U(rung)가 참조하는 v는 depth별로 모든 trace가 공유하는 depth 범위(`max_shared_depth`)에서만 각 trace의 chord-length 비율을 평균해 reconcile한 값(`D_j(u) ≈ S(u, v_j)`)이다 — 단조성이 구성상 보장된다. Rung 내부 점의 u는 두 trace의 u값 사이를 rung 위 chord-length 위치로 선형 보간한다. Stable ID/입력 순서는 동률만 깨고 geometry를 정의하지 않는다. 신규 `torch_nurbs.py::fit_torch_visible_surface_from_uv`는 외부 UV로 IDW seed와 단일 regularized LSQ solve(기존 `_solve_control_grid_lsq` 재사용)만 수행하고 foot-point 재투영을 호출하지 않아 UV가 fitting 도중 3D geometry로 재추정되지 않는다. 신규 `torch_curve_network_native_fit.py`는 A. `PCA_UV_POINT_FIT`(기존 `fit_torch_visible_surface_lsq` 그대로)과 B. `CURVE_NETWORK_NATIVE_FIT`을 동일 3D 표본·동일 capacity·동일 안전 기준으로 fallback 없이 paired 실행한다(AST 검사로 native가 PCA fit 함수를 호출하지 않음을 확인). `build_curve_network_uv`는 degenerate chord length/단조성 위반/불충분한 family 수/불충분한 공유 depth/모순된 (u,v) 매핑을 fail-closed한다. **구현 중 real-data replay로 처음 발견한 추가 실패 모드**: 인접한(=rung으로 연결되는 유일한) 두 transversal trace가 서로 반대 방향(cosine similarity ≤ 0)으로 걸으면 `inconsistent_transversal_curve_direction`으로 fail-closed하는 검증을 correspondence 유효성 검증에 추가했다 — Worklog 96의 per-seed-sample inward-hint 방향 선택이 인접 sample 간 일관성을 보장하지 않는다는 것을 이 배치가 발견했지만, Worklog 96 curve tracing 자체는 미변경이고 PCA로 복구하지 않는다.

**실측(checkpoint 2900/final, 7-region 실측)**: 2900에서 attempted block 7개 중 **7개(100%) 전부 `inconsistent_transversal_curve_direction`으로 parameterization 실패**, final에서 attempted 5개 중 **4개(80%) 실패**. 두 checkpoint 합쳐 12개 block 중 **11개(91.7%)가 intrinsic (u,v) 도메인 자체를 정의하지 못한다.** 유일하게 성공한 1개 block(final region 4)도 valid_supported가 아니라 extrapolative로 분류됐다. **Curve-network-native 경로는 두 checkpoint 어디서도 단 하나의 valid_supported patch도 만들지 못했으나, 동일 block에 대한 PCA_UV 경로는 두 checkpoint 모두 nonzero valid_supported를 유지했다**(2900: 3.41%, final: 13.96%). 방향이 실제로 일관된 합성 block에서는 native fitting이 정상 동작함을 별도 테스트로 확인했다 — 이 결과는 fitting/parameterization 구현의 결함이 아니라 입력 curve network 구조 자체의 불충분함이다.

**Decision C: CURVE_NETWORK_PARAMETERIZATION_INVALID.** Worklog 96의 coherent curve-network block(자기 2×2 correspondence 계약은 이미 통과한 block)의 91.7%가 intrinsic (u,v) 도메인조차 정의하지 못한다. 원인은 Worklog 96 curve-family 구조 자체(seed sample마다 독립적으로 고르는 inward-hint 기반 transversal 방향 선택이 인접 sample 간 일관성을 보장하지 않음)로 귀속하며, PCA fallback으로 숨기지 않고 그대로 보고한다. 지시대로 curve-seed heuristic이나 PCA/UV variant를 추가로 조정하지 않는다. 자세한 내용은 `docs/worklogs/97_curve_network_native_nurbs_fitting.md` 참고.

**Worklog 98 addendum(전역 동기화 tangent frame 기반 curve lattice — Decision B)**: Worklog 96의 per-seed 독립 transversal 방향 선택을 latent surface 위 전역 동기화 tangent frame field로 대체했다. Visible Gaussian training, ADC, region ownership, Worklog 95 latent-surface estimator, supported-query 규약, continuous segment-support 요구, 기존 seed provenance, NURBS degree(2)·6×6 control grid, Worklog 97 external-UV/native fitting, held-out·safety 기준은 모두 미변경이다. 신규 `torch_latent_surface_tangent_frame_field.py`: local PCA tangent axis는 tangent plane(normal)만 제공하고 in-plane 방향으로는 쓰지 않는다. Region train evidence에 Worklog 95의 k=16 kNN(전부 continuous-support 검증됨) 그래프를 만들고, 각 topological component마다 anchor(typed seed가 있으면 그 접선을 투영해, 없으면 centroid-최근접 node를 결정론적으로 선택)에서 **3D 거리 기준 Dijkstra**(단순 BFS hop-count는 회전 시 tie-break가 바뀌며 큰 path-dependent drift를 낳음을 실측으로 확인 후 정정한 것) spanning tree를 따라 `propagate_tangent_onto_plane`(Worklog 96 tracer 재사용)으로 frame을 parallel-transport 전이하고, tree edge를 따라 chord-length `(u,v)` potential을 적분한다. Cycle을 닫는 모든 non-tree edge에 대해 전이된 frame과 실제 frame의 cosine을 검사해(Worklog 97과 동일한 cosine≤0 고정 기준) 단 하나의 holonomy 불일치라도 있으면 그 component 전체를 fail-closed한다 — PCA로 복구하지 않는다. Frame propagation이 새 tangent plane과 거의 평행해 방향을 정의할 수 없는 singularity node는 확장을 멈추고 기록한다(자연스러운 pre-fit 분해). 신규 `torch_latent_surface_curve_lattice.py`는 동기화 field에서 U/V integral curve를 양방향 추적하되 매 step 방향을 가장 가까운 field node의 `e_u`/`e_v`에서 조회한다(독립 재선택 없음, 미지원 즉시 종료); 실제 fit 입력은 이미 holonomy 검증을 통과한 coherent component 전체의 field-derived `(u,v)`다. 신규 `torch_curve_lattice_native_fit.py`가 Worklog 97의 `fit_torch_visible_surface_from_uv`를 그대로 재사용한다(PCA 없음). 신규 `scripts/devtools/synchronized_frame_lattice_replay.py`가 동일 region evidence에 대해 **A. Worklog96+PCA-UV**, **B. Worklog96(독립 방향)+Worklog97 native**, **C. 신규 동기화 field+curve lattice+native**를 fallback 없이 paired 실행한다(세 경로 모두 동일 capacity·held-out·safety 기준).

**실측(checkpoint 2900/final, 7-region 실측)**: **B의 `inconsistent_transversal_curve_direction` 실패율(component 개수 기준)이 combined 91.67%(11/12 block)에서 C의 30.30%(20/66 component)로 크게 줄었다** — 2900에서 17.14%(6/35), final에서 45.16%(14/31)(*원래 배포판은 이 두 값을 evidence-weighted `parameterization_invalid` fraction(2900: 20.42%, final: 50.08%)과 혼동해 20.4%/50.1%로 잘못 기재했다 — 두 지표는 서로 다르며, raw component-count 기준이 "몇 개 중 몇 개가 실패했는가"의 정확한 답이다. Worklog 98 본문에서 정정함*). C는 patch candidate 수도 12개(Worklog 96 seed 수)에서 66개(전역 field가 여러 coherent 영역으로 자연 분해)로 크게 늘었다. 그러나 **parameterization이 성공한 component만 조건부로(evidence-weighted 값 기준) 봐도 최종 6×6 degree-2 NURBS는 여전히 압도적으로 extrapolative/unsafe다**: 2900 조건부 valid_supported 5.67%/(1-20.42%)=7.1%(extrapolative 55.4%, unsafe 37.5%), final 조건부 valid_supported 2.38%/(1-50.08%)=4.8%(extrapolative 42.6%, unsafe 52.6%). C의 valid_supported는 checkpoint마다 결과가 엇갈린다(2900: 5.67% > A의 3.41%, final: 2.38% < A의 13.96%) — A보다 일관되게 낫다는 근거는 없다. Extrapolative+unsafe 합계는 C가 두 checkpoint 모두 A보다 낮다(2900: 73.9% vs 96.6%, final: 47.5% vs 86.0%).

**결과 해석 정정 및 범위를 좁힌 결론.** 이 배치가 실제로 증명하는 것은 (1) latent-surface support 확보, (2) curve construction 널리 가능, (3) 전역 동기화된 tangent 방향이 Worklog 97의 독립-방향 실패를 상당 부분(component 기준 91.67%→30.30%) 해소, (4) 그러나 지금 경로(동기화 field→tree-integrated UV→고정 6×6 degree-2 tensor-product LSQ)는 parameterization 성공 component에서도 대부분 extrapolative/unsafe라는 것뿐이다. **NURBS representation 자체가 이미 병목으로 증명됐다고 단정하지 않는다** — 고정 capacity, generic point LSQ fitting, curve-network을 point cloud로 collapse하는 방식, tree-integrated UV 자체의 parametric domain 품질 중 어느 것이 원인인지는 이 배치만으로 분리되지 않았다. Worklog 98은 자체적으로 최종 architecture 결정을 내리지 않으며, 원인 분리는 후속 bounded gate 배치의 과제다. 자세한 내용은 `docs/worklogs/98_synchronized_tangent_frame_curve_lattice.md` 참고.

**Worklog 99 addendum(bounded parametric patch architecture gate — Decision D)**: Worklog 98이 남긴 원인 분리 과제를 다뤘다. Visible Gaussian training, ADC, region ownership, Worklog 95 latent-surface estimator, continuous support contract, Worklog 98 synchronized tangent-frame field·coherent component, 각 paired component의 input 3D evidence, held-out evaluation, 기존 extrapolative/unsafe/valid_supported 정의는 모두 미변경이다. Held-out 결과로 어떤 candidate도 튜닝하지 않았다.

**1. Pre-fit parametric domain validity**: 신규 `torch_parametric_domain_validity.py`가 Worklog 98의 coherent component마다 이미 계산된 `(u,v)`를 A/B/C 비교 이전에 검증한다. 각 점의 UV-공간 local kNN에 `delta_position ≈ J @ delta_uv` least-squares affine Jacobian을 적합해, `J`의 두 열 외적 방향을 그 점의 실제 normal과 비교(orientation reversal/foldover), singular value로 singularity와 condition number(=local spacing 대비 extreme stretching)를 함께 얻는다. Worklog 98의 기존 holonomy edge를 재사용해 orientation이 아닌 position 기준 cycle drift도 진단으로 report한다. Fold가 하나라도 있으면 domain 전체를 fail-closed한다(PCA 복구 없음).

**2~4. Candidate A/B/C**: A는 Worklog 98의 `fit_curve_lattice_native`를 그대로 baseline으로 유지한다. B(`torch_adaptive_nurbs_capacity.py`)는 capacity를 Worklog 98 자신의 U/V curve 개수·sample 수·aspect ratio로만(fit/held-out 오차 미사용, AST로 확인) 결정해 기존 `fit_torch_visible_surface_from_uv`(이미 second-difference 정규화 내장)에 그대로 넘긴다. C(`torch_gordon_curve_network_surface.py`)는 고전 Gordon transfinite interpolation(`S=S1+S2-S12`)을 discrete U/V level에 실제 quadratic least-squares curve를 적합해 근사하고(모듈 docstring에서 고전 원칙과 OSN-GS 근사를 명시적으로 분리), 두 family 독립 fit의 intersection 불일치를 residual로 report하며, 채워진 level이 부족하면(가짜 boundary curve 없음, closed boundary 요구 없음) fail-closed한다. 신규 `scripts/devtools/parametric_patch_architecture_gate_replay.py`가 domain-valid component에만 세 candidate를 fallback 없이 각각 독립 실행하고 5개 범주(PARAMETER_DOMAIN_INVALID/FIT_FAILED/EXTRAPOLATIVE/UNSAFE/VALID_SUPPORTED)를 분리 보고한다.

**실측(checkpoint 2900/final, 7-region)**: orientation-coherent component 46개(2900: 29, final: 17) 중 **combined 80.4%(37/46)가 A/B/C 비교 이전에 이미 local UV fold/orientation reversal로 무효**다(2900: 75.9%=22/29, final: 88.2%=15/17, 두 checkpoint 모두 domain-invalid 원인 100%가 foldover — duplicate-UV나 singular Jacobian은 0건). Evidence-weighted PARAMETER_DOMAIN_INVALID는 2900 78.77%, final 91.72%(세 candidate 모두 공유). Domain-valid로 남은 소수 component(2900: 7개, final: 2개, 모두 8~40점 소규모)에서도 valid_supported는 사실상 0%다 — 2900에서 A/B만 0.59%(2900 region 3의 11점 component 하나만 VALID_SUPPORTED), C는 0%; final에서는 세 candidate 모두 0%. B의 adaptive capacity는 소규모 component마다 최소값 4×4로 수렴했다. C는 여러 domain-valid component에서 level당 최소 인구를 못 채워 `insufficient_populated_curve_levels`로 fit 자체가 실패했다.

**Decision D: PARAMETER_DOMAIN_LIMIT.** Combined 80.4%의 orientation-coherent component가 A/B/C 어느 것으로도 비교되기 전에 이미 무효이므로 NURBS capacity(B)나 curve-network-native construction(C) 문제로 귀속할 수 없다. Domain-valid로 남은 소수에서도 세 candidate 모두 valid_supported가 사실상 0%였으므로 어느 patch-fitting 방식이 우월하다고도 판정할 수 없다. **Worklog 98은 tangent frame orientation coherence는 풀었지만 전역적으로 사용 가능한 parameter-domain 구성은 풀지 못했다.** 다음 architecture 결정은 NURBS capacity나 fitting formulation이 아니라 **intrinsic parameterization 자체**를 다뤄야 한다. 지시대로 curve seeding으로 되돌아가지 않는다. 자세한 내용은 `docs/worklogs/99_parametric_patch_architecture_gate.md` 참고.

**Worklog 100 addendum(bounded intrinsic parameterization architecture gate — Decision C)**: Worklog 99의 validator에 있던 두 confound(UV-공간 kNN 이웃, 독립 부호의 PCA/support normal 기준)를 같은 배치 안에서 직접 수정했다(별도 진단 worklog 없음) — local Jacobian 이웃을 Worklog 98이 이미 검증한 source-graph adjacency로 교체하고, orientation 기준을 synchronized frame(`e_u_i`, `e_v_i`)으로 교체했으며, 단일 전역 U/V 방향 반전은 fold로 보지 않고 먼저 canonicalize한다. 신규 `torch_latent_surface_edge_differential.py`가 모든 continuously-supported edge마다 양 endpoint의 synchronized frame을 대칭적으로 결합한 differential(`du_ij`, `dv_ij`, 고정 결정론적 weight)을 만들고, 신규 `torch_global_differential_uv_integration.py`가 이를 모든 edge에 대해 동시에(하나의 spanning tree가 아니라) 푸는 전역 least-squares 적분(candidate B)을, 신규 `torch_orientation_preserving_uv_integration.py`가 B에서 엄격히 초기화해 고정 스케줄로 local-injectivity를 보정하는 candidate C를 구현했다. Fixed and unmodified: visible Gaussian training, ADC, region ownership, Worklog 95 estimator, continuous support contract, Worklog 98 synchronized field·coherent component·curve construction, held-out evaluation, 기존 6×6/degree-2 NURBS fitter와 안전 기준.

**실측(checkpoint 2900/final, 7-region)**: 수정된 validator 기준 A(tree-integrated UV)의 domain-invalid율은 combined 67.4%(31/46) — Worklog 99의 confound 있는 80.4%보다 낮지만 여전히 다수. B(global differential integration)는 domain-valid를 15→18/46(+20% 상대)로만 늘렸다. **C(B에서 엄격히 초기화한 orientation-preserving 보정)는 46개 component 전부에서 B의 domain-valid 여부와 정확히 일치했다** — B가 이미 유효했던 18개는 반복 0회로 그대로 통과, 나머지 28개(60.9%)는 단 하나도 구제하지 못하고 전부 `not_globally_parameterizable_at_current_scale`로 fail-closed됐다. B의 domain-invalid component에서도 global differential residual 절대값은 작다(overall RMS 대부분 0.001~0.03) — fold가 큰 미분 불일치의 산물이 아니라 낮은 residual과 공존하는 진짜 local 방향 반전임을 의미한다. NURBS 평가에서는 두 checkpoint, 세 candidate 모두 VALID_SUPPORTED가 0.00%였다.

**Decision C: COMPONENT_SCALE_NOT_GLOBALLY_PARAMETERIZABLE.** B가 A 대비 대다수 실패를 제거하지 못했으므로 "tree 적분 자체가 한계"(Decision A)는 아니다. C(local-injectivity 제약)가 B 대비 단 하나의 fold도 추가로 풀지 못했으므로 "local injectivity 제약이 필요"(Decision B)도 아니다. **Synchronized tangent field는 국소적으로 일관되지만(holonomy 통과) 현재 component scale에서 다수가 하나의 chart로 전역 적분될 수 없다.** 새 curve seed를 추가하지 않는다. 앞으로의 분해는 NURBS fitting 이전에 intrinsic integrability·chart 구조 자체가 이끌어야 한다. 자세한 내용은 `docs/worklogs/100_intrinsic_parameterization_architecture_gate.md` 참고.

**Worklog 101 addendum(intrinsic-integrability-driven local chart atlas — Decision B)**: Worklog 100의 "component scale not globally parameterizable" 결론을 "하나의 component = 하나의 chart" 가정을 버리는 방향으로 이어받았다(다만 낮은 differential residual + 고정 3회 재가중 solver의 실패가 injective parameterization의 수학적 부재를 증명한다고 주장하지 않도록 해석을 좁혔다 — candidate C는 fold-aware 재가중 solver일 뿐 비존재 증명이 아니다). 신규 `torch_intrinsic_chart_atlas.py`: 동일한 continuously-supported source graph 위에서 결정론적 anchor(centroid-최근접, 이후는 현재 coverage로부터 BFS 그래프-거리가 가장 먼 미커버 node)로부터 BFS hop-count ring을 성장시키며, 매 ring마다 Worklog 100의 `integrate_global_differential_uv`를 그 ring의 induced subgraph(전체 edge, 스패닝 트리 아님)에 다시 풀고 수정된 validator로 검증해, 처음 무효가 되는 ring 직전을 maximal chart로 확정한다. NURBS/held-out 오차로 chart를 만들거나 늘리거나 합치거나 쪼개지 않는다(AST로 검증). Chart는 이미 커버된 node를 배제하지 않고 성장하므로 자연스럽게 겹칠 수 있으며, 두 chart를 가르는 edge는 `seam_edges`로만 report되고 물리적 경계 의미는 부여하지 않는다. 각 chart는 부모 component의 synchronized `e_u`/`e_v`를 그대로 물려받는다(독립 재추정 없음). 신규 `torch_chart_curve_lattice.py`가 Worklog 98의 `build_curve_lattice`를 chart-restricted view에 그대로 재사용하고, tracer가 chart 경계를 넘어간 지점만 사후 truncate한다(`PARTITION_SEAM`, 물리적 경계 아님). 기존 `fit_torch_visible_surface_from_uv`(degree=2, 6×6, 무변경)를 downstream probe로만 쓰고, 6×6 grid 자체의 제어점 수(36)에 못 미치는 domain-valid chart는 `CHART_DOMAIN_VALID_BUT_INSUFFICIENT_PATCH_SUPPORT`로 `PARAMETER_DOMAIN_INVALID`와 분리 보고한다.

**실측(checkpoint 2900/final, 7-region)**: **chart 단위 domain validity가 combined 100%(115/115)**로, 단일 전역 chart 방식의 39.1%(18/46, Worklog 100 candidate B와 동일)를 크게 상회한다. **(Worklog 102에서 정정) 이 100%는 이미 materialize된 chart만을 분모로 삼은 항등적 수치다 — 의미 있는 지표는 source-evidence 커버리지로, combined 1241개 node 중 1089개(87.8%)만 유효 chart로 커버됐고 152개(12.2%)는 명시적으로 unchartable로 남았다.** 그러나 domain-valid chart의 **92.2%(106/115)는 6×6 grid의 최소 evidence(36점)에도 못 미치는 소형**(median 9점)이었고, 실제 fit이 시도된 나머지 9개(7.8%, 36~84점)는 8개 unsafe·1개 extrapolative로 전부 실패해 **valid_supported는 combined 0%**다.

**Decision B: VALID_CHARTS_EXIST_BUT_CURRENT_PATCH_MODEL_FAILS.** Chart-domain validity가 39.1%→100%로 광범위하게 해소돼 intrinsic parameterization 문제는 chart scale에서 operationally 해소된 것으로 선언한다. 하지만 결과 chart의 92.2%가 고정 6×6 NURBS에 필요한 evidence보다 작았고 나머지도 전부 unsafe/extrapolative였으므로, 다음 architecture gate는 local/adaptive parametric patch representation을 다뤄야 한다. 6×6 fitting을 만족시키려고 chart를 인위적으로 키우지 않는다 — chart 크기는 이미 intrinsic integrability가 결정한 결과다. 자세한 내용은 `docs/worklogs/101_intrinsic_integrability_local_chart_atlas.md` 참고.

**Worklog 102 addendum(pre-fit patch identifiability / adaptive capacity gate — Decision C)**: Worklog 101의 "chart 단위 domain validity 100%" 표현을 정정했다 — 이는 이미 materialize된 chart만을 분모로 삼은 항등적 수치이며, 의미 있는 지표는 source-evidence 커버리지 87.8%(1089/1241 node, 12.2%는 명시적 unchartable)다. 또한 Worklog 101의 "36 control point 미만은 구조적으로 부족" 컷오프가 기존 정규화 NURBS solver의 수학적 요구사항이 아님을 반영해, 신규 `torch_patch_identifiability.py`가 실제 solver와 동일한 `TorchNURBSSurface._basis_tables` 기반 tensor-product B-spline design matrix의 SVD로 pre-fit algebraic identifiability(design matrix가 자기 shape의 achievable rank `min(sample_count, control_variable_count)`에 도달하는가)를 판정한다 — sample 36개 미만을 자동 기각하지 않되, u/v extent 퇴화나 진짜 rank deficiency는 무효로 판정한다. A(고정 6×6/degree2, 무변경)/B(신규 `torch_adaptive_patch_capacity.py::select_adaptive_quadratic_capacity`, degree 2 고정·3×3~6×6 중 최대 identifiable grid)/C(`select_support_adaptive_capacity`, degree 1(2×2 최소)~degree 2(3×3 최소) 중 identifiability만으로 order·capacity 결정)를 fallback 없이 비교했다. 신규 `torch_chart_overlap_consistency.py`가 겹치는 chart 쌍의 fit 결과를 merge 없이 평가만 한다.

**실측(checkpoint 2900/final, 7-region)**: C는 identifiable chart 비율을 A의 23.5%(27/115)에서 **68.7%(79/115)**로 크게 늘렸지만, **VALID_SUPPORTED는 A/B/C 모두 정확히 2/115(1.7%)로 동일**했다 — C가 새로 식별 가능하게 만든 52개 chart 중 어느 하나도 안전한 fit에 도달하지 못했고, C의 identifiable 79개 중 97.5%(unsafe 48+extrapolative 29)가 여전히 불안전하다. Overlap consistency(평가만, 조정 없음)는 C에서 fit에 도달하는 pair가 늘었지만 position/normal 불일치는 오히려 A/B보다 컸다.

**Decision C: TENSOR_PRODUCT_PATCH_STILL_FAILS.** 대부분의 valid chart가 algebraically identifiable해졌음에도(23.5%→68.7%) fitted patch는 여전히 압도적으로 unsafe/extrapolative이고 VALID_SUPPORTED는 전혀 늘지 않았다 — intrinsic parameterization이나 과도한 고정 capacity 문제가 아니라 현재 local tensor-product NURBS fitting/representation 자체의 한계다. Upstream(intrinsic parameterization, chart 구성)으로 되돌아가지 않는다. 자세한 내용은 `docs/worklogs/102_patch_identifiability_capacity_gate.md` 참고.

**Worklog 103 addendum(latent surface geometry 흐름 복구 및 spatial coverage 실측 감사 — architecture 결정 없음)**: Worklog 98이 도입한 tangent-frame component가 Worklog 95 latent surface 추정기(iterative weighted-PCA/MLS)의 실제 투영 좌표(`query.positions`)를 버리고 raw Gaussian center(`points[node_list]`)로 되돌아가 있던 divergence를 발견·수정했다 — normal은 정상적으로 latent query에서 왔지만, 실제 3D 좌표는 Worklog 98부터 102까지(`component.positions`, edge differential, 전역 UV 적분, fold 검출, chart 성장, patch identifiability, 최종 NURBS fit target) 전부 raw Gaussian center 위에서 계산되고 있었다. **이는 위 Worklog 99~102의 "Decision A~C" 전부를 포함해, geometry에 의존하는 모든 실측 결론이 corrected latent position으로 재실측되기 전까지는 authoritative로 취급하지 않아야 함을 의미한다.** 소스(`build_tangent_frame_field`)에서 좌표 provenance만 고쳤고(k·support radius·planarity·MLS iteration·kernel·region ownership·training·ADC·support-edge 판정 기준은 미변경), 다운스트림 전 지점(`torch_latent_surface_edge_differential.py` 등)을 grep으로 전수 감사해 raw-position 재대체가 없음을 확인했다(`_restrict_component`의 provenance 유실 1건만 발견·수정).

신규 `torch_latent_surface_coverage_audit.py`가 region-owned raw evidence 전체에 대해 latent-supported/unsupported를 나누고 Worklog 98의 kNN+continuous-support 연결성만으로(frame coherence·chart·identifiability 요구 없이) latent support unit을 조직한다. 신규 `torch_latent_surface_visualization_nurbs.py`가 모든 unit에 대해 고정 해상도 사다리로 시각화 전용 NURBS를 시도하며 안전성/식별성으로 결과를 절대 숨기지 않는다. `scripts/devtools/latent_surface_coverage_export.py`가 A(FULL_SCENE)/B(REGION_EVIDENCE)/C(RAW_VS_LATENT)/D(ALL_LATENT_SURFACES)/E(DOWNSTREAM_COMPARISON) 5개 stage와 region별 독립 export를 WebRenderer 형식(`point_cloud.ply`+`nurbs_surface.json`)으로 만든다.

**실측(checkpoint `baseline_compatible/final`, 7-region, 순수 사실 보고, 정성적 결론 없음)**: region-owned raw evidence 7,774개 중 latent-supported 4,599개(59.2%), unsupported 3,175개(40.8%), latent support unit 86개, visualization NURBS 시도 86개 중 성공 49개·실패 37개. Projection displacement는 local spacing 대비 median 0.22~0.30배, p95 0.77~0.96배, max 1.24~1.67배(region별). 자세한 내용과 전체 export 경로는 `docs/worklogs/103_latent_surface_geometry_flow_coverage_audit.md` 참고 — **이 worklog는 coverage가 충분한지, chart/NURBS 구성이 맞는지에 대해 어떤 판단도 내리지 않는다.**

**Worklog 104 addendum(latent surface visualization coverage 완결성 보정 — architecture 결정 없음)**: Worklog 103이 실패한 37/86 unit을 직접 재현·조사한 결과, **전부 `insufficient_points_for_any_surface`이며 예외 없이 크기 1~2 node짜리 고립 조각**이었다(표면은 최소 3개의 비공선 점이 필요하므로 원천적으로 subdivision으로 구제 불가능한 최소 크기). 신규 `torch_latent_surface_visualization_coverage.py::materialize_unit_with_subdivision`이 latent surface 자체·다운스트림 승인 기준을 전혀 건드리지 않고 **시각화 유닛만** 재조직한다 — 실패한 unit을 자신의 continuously-supported graph 연결성만으로(convex hull/bounding box/PCA rectangle/임의 bridging 없이) 결정론적으로 하위 조각으로 나누고 재귀적으로 재시도하며, 최소 크기 미만이면 정확한 source node ID와 사유를 담아 `UNREPRESENTED_LATENT_FRAGMENT`로 report한다. 구현 중 실제 버그를 발견·수정했다 — fit을 먼저 시도한 뒤 실패해야만 subdivision하는 순서였는데, fitter가 연결성을 신경 쓰지 않고 서로 연결된 적 없는 두 조각도 수치적으로 "성공"시킬 수 있어, connected-component 분석을 fit 시도보다 먼저 하도록 순서를 바꿨다.

**실측**: latent-supported 4,599개 중 **4,539개(98.7%)가 visualization NURBS로 표현**되고 나머지 60개(1.3%)만 명시적 unrepresented fragment로 남았다(전부 원래의 37개 unit과 동일한 1~2 node 고립 조각). Visualization NURBS patch 수는 49→59로 늘었다. `node_accounting_ok`(모든 latent-supported node가 표현되거나 명시적으로 미표현 처리됐는지)가 7개 region 전부 `true`로 기계적으로 검증됐다 — 어떤 node도 조용히 사라지지 않는다. Worklog 103의 원본 export는 그대로 보존했고, 재생성된 결과는 별도 디렉터리(`output/osn_gs_scene_latent_coverage_audit_subdivided/`)에 저장했다. 자세한 내용은 `docs/worklogs/104_latent_surface_visualization_coverage_completeness.md` 참고 — **이 worklog도 coverage 충분성이나 architecture 유지/폐기에 대해 어떤 판단도 내리지 않는다.**

**Worklog 105 addendum(coverage-first Gaussian Subset partition — 신규 top-level architecture 1단계, architecture 성공/실패 판단 없음)**: 사용자 지시로 Worklog 95~104의 selection-first 파이프라인 개선을 중단하고, top-level visible-surface construction architecture를 **coverage-first**로 교체하는 첫 구현 배치를 진행했다. 새 순서는 `전체 학습된 visible Gaussian scene -> Gaussian마다의 surface-orientation 표현 -> coverage 보존 normal-coherent·spatially-connected Gaussian Subset 분할 -> (미래) trusted evidence -> latent surface -> NURBS Patch`이며, 이번 배치의 범위는 **분할 1단계뿐**이다. Trustable Gaussian 추정·latent surface·신규 production NURBS는 구현하지 않았고, Worklog 95~104 구현·산출물은 삭제/덮어쓰기 없이 각자의 스크립트로 그대로 재생 가능하다. 핵심 계약은 **SUBSET OWNERSHIP != TRUSTABILITY** — 어떤 Gaussian도 normal confidence가 낮다는 이유로, 이웃 evidence가 약하다는 이유로, 미래의 latent surface가 어렵다는 이유로 소유권을 잃지 않는다.

신규 `osn_gs/surface/torch_gaussian_surface_orientation.py`: 3DGS parameterization(`Sigma = R diag(exp(scaling)^2) R^T`)에서 `R`의 열이 곧 고유벡터이므로 eigen-decomposition 없이 주축을 정확히 얻고, 저장 표현의 유일한 모호성인 **축 순서**를 고윳값 내림차순으로 결정론적으로 해소한다(`normal=axis(lambda3)`, `tangent_u=axis(lambda1)`, `tangent_v=normal x tangent_u`). 기존 `extract_covariance_frame`과 동일한 규약이며, 임의 covariance 입력용 두 번째 진입점도 **같은** 정규화 함수를 통과해 normal 정의가 모듈마다 갈라지지 않는다. 부호는 재현성 게이지일 뿐 물리적 outward 방향이 아니고, 비교는 항상 unsigned `|dot(n_i, n_j)|`다(전역 flipping 없음).

신규 `osn_gs/surface/torch_coverage_first_subset_partition.py`: kNN spatial adjacency(각 Gaussian 자신의 kNN 거리 중앙값 기준 scale-aware, `dist <= 2.0 * min(spacing_i, spacing_j)`) **위에서만** normal compatibility(`|dot| >= 0.85`)를 평가해 연결 성분을 만든다 — 전역 normal clustering이 아니므로 멀리 떨어진 평행 벽이 합쳐지지 않고, 공간 근접성은 연결성 제약일 뿐 분할 semantics가 아니다. 모든 heuristic은 `CoverageFirstPartitionConfig` 한 곳에 모여 report에 그대로 기록되며, `neighbor_count=8`/`normal_compatibility_min_alignment=0.85`는 기존 저장소 기본값(`ManifoldAffinityConfig`) 재사용이다. 렌더를 보고 조정한 값은 없고 hyperparameter search도 하지 않았다. 구현 중 실제 결함 1건을 발견·수정했다 — connected-component 솔버가 min-label을 edge endpoint에 scatter해 라운드당 1-hop만 전진(O(diameter))하는 바람에 실제 scene에서 예산 초과로 실패했고, root index에 hooking(Shiloach-Vishkin)하도록 고쳐 임의 길이 사슬이 한 라운드에 해소되게 했다(1.7M-node 직경 1.7M path가 0.05초에 1개 성분, scipy와 성분 분할 정확히 일치).

**실측(checkpoint `output/extent_ab/val64/baseline_compatible/final`, 순수 사실 보고)**: 입력 = visible Gaussian **1,685,549개 전부**(Worklog 95의 7-region 7,774개로 제한하지 않았고 latent support/boundary/topology/chart/identifiability/NURBS/held-out 중 무엇도 요구하지 않음 — AST 테스트로 강제). assigned 1,685,549 / unassigned **0** / multiply-owned **0** / spatially disconnected subset **0** / `coverage_identity_holds=true`. Subset **166,585개**: min 1, median 1, mean 10.12, p95 10, max **559,541(전체의 33.2%)**. Singleton 107,947개(subset의 64.80%, Gaussian의 6.40%), 크기 ≤8이 156,687개(subset의 94.06%, Gaussian의 15.75%). Edge: candidate 8,655,268 / spatial 7,344,950 / **normal-compatibility cut 3,389,357(spatial의 46.1%)** / accepted 3,955,593. Fallback ownership 107,947개(6.40%: normal 비호환 이웃만 107,215 + 공간 이웃 없음 732). 런타임 143초(RTX 5080).

**Review export**(`output/osn_gs_coverage_first_subset_partition/`, 전부 원본 좌표계·전체 scene·crop 없음·WebRenderer 폴더당 ply 1개 규약 준수): ORIGINAL_SCENE / NORMAL_ORIENTATION_VIEW(unsigned `|n|`) / GAUSSIAN_SUBSET_PARTITION(결정론적 subset 고유색) / SUBSET_BOUNDARY_VIEW(Gaussian별 cut ratio 램프 + cut segment `base_curves`) 4개 view와 `partition_report.json`. 사용자 추가 지시대로 **4개 view 전부 `render.ppm`을 생성했고**(trainer `_preview_camera`와 동일한 카메라 = eval split train camera 이름순 첫 번째 `DSC07957.JPG`, 1600×1036 — checkpoint 자신의 `render.ppm`과 픽셀 비교 가능), **Gaussian별 그룹 RGB 분할 시각화도 실행했다**(view 색을 SH DC에 넣고 상위 밴드 0·degree 0으로 flat 렌더; position/scale/rotation/opacity는 항상 checkpoint 원본, checkpoint 파일은 쓰지 않음).

§9의 complexity-driven subset refinement는 지시대로 구현하지 않았고, 나중에 평가할 수 있도록 scale만 기록한다 — 최대 subset이 559,541개(33.2%)를 소유하고 상위 6개가 각각 27,000개를 넘으므로, "임의로 거대한 normal-connected component가 항상 하나의 subset이어야 하는가"라는 질문은 이 scene에서 실제로 발생한다.

**이 addendum은 coverage-first architecture의 성공/실패, over-segmentation 수용 여부, normal 임계값 적절성에 대해 어떤 판단도 내리지 않는다.** 사용자가 GAUSSIAN_SUBSET_PARTITION을 직접 시각적으로 검토한 뒤에야 다음 단계(subset-local Trustable Gaussian 추정 -> latent surface -> 1 subset : 1 NURBS Patch)를 구현한다. 자세한 내용은 `docs/worklogs/105_coverage_first_gaussian_subset_partition.md` 참고.

## 7. 현재 검증 상태와 알려진 위험

Repository-wide pytest 최신 기준선(worklog 105 구현 후): 1152 passed, 1 skipped, 1 warning, 18 subtests passed in 376.95s.

알려진 위험:
- §2.2 표의 real 3k/10k closed-loop 부재는 미해결이며, §2.4 후보(candidate 생성 밀도) 전에는 착수 승인이 없다.
- §2 파이프라인의 direct/reverse tangent quality 비교는 scene 전체 단위 선택이라, 한 region의 수정이 다른 region의 결과를 바꿀 수 있다(worklog 53에서 실측). 이 구조를 변경하는 작업은 반드시 §2.3 negative-control 전체 재확인을 동반한다.
- §3(Isolated Boundary-first)은 재개 시 이 문서를 먼저 갱신하고 시작한다 — 현재는 활성 작업자가 없다.
- **`output/osn_gs_scene/*/checkpoint.pt`가 세션 사이 완료된 학습 run으로 교체됐다** — worklog 45-60이 참조하던 real 5k region 130/141 등 특정 region ID/개수는 더 이상 현재 checkpoint와 일치하지 않는다. 앞으로 real-checkpoint 수치를 보고할 때는 항상 그 라운드에서 실측한 값을 쓰고, 과거 worklog의 고정된 숫자와 다르다고 회귀로 오인하지 않는다.
- worklog 61에서 real 3k(cap 2048)가 `torch_gaussian_surface_region_formation.py`(2026-07-30, worklog 111-123)의 사전 존재 `KeyError`(`bridge_by_pair[(a, b)]`가 `consensus_by_pair`에는 있는 pair를 못 찾음)로 crash함을 발견했다 — 새 checkpoint 내용이 노출한 latent 결함이며 이번 작업 범위 밖이라 미수정. 3k(cap 1024)는 정상 동작한다. Region formation을 다루는 다음 작업자는 이 결함을 먼저 재현·수정 여부를 판단한다.
- **Known issue (worklog 64, 미수정)**: `gaussian_initialization_mode="baseline_compatible"`은 `_initialize_canonical`(production 기본 "initialize" 스케줄)에만 적용되고, `initialize_deferred`(`adc_post_commit`/`disabled` 스케줄)는 의도적으로 이 플래그를 무시하고 항상 `covariance_knn` 방식의 planar-surfel 초기값을 쓴다 — 그 경로의 첫 post-ADC surface 재구성이 모델 자신의 scale/rotation을 유일한 orientation evidence로 재사용하기 때문이다. 즉 `visible_nurbs_update_schedule=adc_post_commit`이나 `disabled`로 학습하면, `gaussian_initialization_mode=baseline_compatible`을 지정해도 실제로는 covariance-KNN 초기화가 적용되는 **의미 불일치**가 있다. Production 기본 스케줄("initialize")에는 영향 없음. deferred 스케줄을 실제로 쓰는 작업이 생기면 이 불일치를 먼저 해소해야 한다(현재는 미수정, 미착수).

다음 작업자는 먼저 이 문서와 docs/worklogs/README.md의 최신 인덱스, 그리고 가장 최근 작업로그 docs/worklogs/90_surface_topology_root_cause_attribution.md를 읽고 이어서 작업한다. 과거 방향의 세부 기록은 필요할 때 Git history로만 조회한다.
