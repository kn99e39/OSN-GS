# OSN-GS Urgent Work Master

최종 갱신: 2026-08-04

이 문서는 현재 진행 방향과 승인 경계를 정의하는 canonical master다. 과거 실험의 상세 경과는 Git 이력에 보존하며, 현재 판단에 필요 없는 작업로그는 `docs/worklogs/`에서 제거했다. 작업로그 번호는 `docs/worklogs/README.md`의 최신 인덱스(1부터 재번호, 현재 62까지)를 기준으로 한다.

## 1. 목표 모델과 불변 조건

OSN-GS에서 NURBS는 관측 가능한 표면을 설명하고 가려진 영역의 불확실 Gaussian 생성에만 기하 정보를 제공하는 중간 표현이다. visible/certain Gaussian의 위치는 NURBS가 아니라 영상 손실로 최적화한다.

- visible surface는 topology별 별도 방법론으로 분기하지 않는다.
- 모든 topology는 observed boundary loop, boundary role, source provenance, interior support를 공통 입력 계약으로 사용한다.
- multi-hole은 outer loop와 모든 interior loop를 보존한다. 비중첩 planar partition 증거가 없으면 `review_required`이며 임의 central fill 또는 hole별 overlapping annulus 복제는 금지한다.
- artifact의 chart가 생성되었다는 사실은 품질·안전·사용 가능성을 뜻하지 않는다.
- **evidence가 없으면 강제로 닫지 않는다.** gap 보간, 임의 candidate 생성, closure만을 위한 낮은-evidence edge 선택은 어떤 단계에서도 금지한다 — 이 원칙은 아래 2절 전체에서 반복적으로 검증·재확인됐다.

## 2. 현재 활성 작업 A — Canonical visible-NURBS production pipeline (full-cloud continuation → representative selection → boundary ordering → materialization)

**2026-07-30(worklog 18) 기준으로 이 경로가 `train.py`/benchmark의 유일한 production 경로가 됐다.** `legacy`, `voxel_patch_stage1`, IDW/local split fallback과 해당 CLI는 전부 제거됐다. 이전 절(과거 버전)에서 "isolated, dispatcher 미연결"로 기술했던 world-space boundary half-edge → ordered graph → materialization adapter 계열 작업(worklog 12-15)은 이 병합 이후 더 이상 isolated가 아니다 — 지금은 `construct_visible_nurbs_from_gaussians` / `TorchOSNGSPipeline._construct_canonical_with_full_evidence`가 곧 production 경로다.

### 2.1 파이프라인 단계

1. **Representative selection** (`torch_density_preserving_representative_selection.py`): 대량 Gaussian을 `canonical_construction_max_points`(기본 2048) 예산으로 mode-aware farthest-point sampling. worklog 49에서 boundary-evidence swap-in(예산 경쟁으로 탈락한 진짜 orientation-divergent evidence를 안전하게 복구)을 추가했다.
2. **Reliability + region formation** (`torch_gaussian_structural_reliability.py`, `torch_gaussian_surface_region_formation.py`): full-cloud contextual evidence 기반 reliability, seed/merge DSU 기반 region consolidation.
3. **Boundary candidate 추출** (`torch_full_cloud_continuation_shell.py`, `torch_boundary_support_termination.py`): representative 주변 same-mode angular gap을 관측해 `observed_support_termination`/`no_gap`/`parallel_sheet_conflict`/`crease_discontinuity`/`ambiguous_continuation`/`reliability_frontier`/`unresolved_sampling_gap`으로 분류. worklog 47(cross-surface leakage), 48(fold/gap-crossing locality), 50(single-radius over-reach)에서 순차적으로 오분류를 좁게 수정했다.
4. **Directed ordering / materialization** (`torch_directed_boundary_ordering.py`, `torch_visible_boundary_region_status.py`): region별 compatible edge에 대한 exact one-in/one-out Hungarian matching으로 closed cycle과 open path를 결정론적으로 복원. worklog 53에서 downstream-invalid 2-cycle이 capacity를 낭비하는 결함과, 그로 인해 노출된 direct/reverse tangent quality 비교의 self-intersection 인식 결함을 수정했다. worklog 54는 region별 5-state 계약(`eligible_closed_boundary`/`open_observed_fragment`/`insufficient_observation`/`ambiguous_boundary`/`rejected_unsafe`)을 확정해 validated closed loop만 materialization에 전달하도록 제한했다.

### 2.2 현재 결론 (worklog 40~53에 걸친 6단계 소진 감사)

Real 3k/5k/10k checkpoint(cap 2048) replay에서 **5k만 2개 region을 닫고(closed/materialized 2/2), 3k와 10k는 0개**다. worklog 40부터 53까지 다음 단계를 각각 독립적으로 감사했고, 전부 실제 결함을 찾아 좁게 수정했지만 **어느 것도 closed-loop 개수를 바꾸지 못했다**:

| worklog | 감사 대상 | 결과 |
|---|---|---|
| 40 | region-pair 단위 sphere seam 오탐 | 수정, sphere 22 false candidate 제거 |
| 45 | directed compatibility gate의 target tangent 부호 | 수정, Box 6번째 face + real 5k 0→2 closed 회복 |
| 47 | no_gap의 cross-surface(mode) leakage | 수정, 효과는 재분류만 |
| 48 | no_gap의 candidate-local fold/gap-crossing | 수정, 3k/5k 각 1 node만 재분류 |
| 49 | representative selection의 FPS 예산 손실 | 수정(swap-in), real swap 22/51/76건이나 closed 불변 |
| 50 | 단일 4x 반경의 원거리 support 과다 신뢰 | 수정(scale-persistence), 76/36/39 node 재분류, closed 불변 |
| 51 | raw full-cloud에 representative 축약으로 잃은 chain이 있는지 | 기각(3곳 raw에도 chain 없음) + 2곳은 Hungarian 경쟁 문제로 재분류 |
| 52 | 그 Hungarian "경쟁" 주장 자체의 정확성 | 정정 — edge는 실제로 matching에 포함돼 있었음, 진짜 원인은 topology 불가능(region 52) 또는 evidence 열세(region 56) |
| 53 | region 56의 진짜 원인(2-cycle 낭비) | 수정, fragment 2개→4-node open path 1개로 개선(닫히진 않음, 진짜 최댓값이 open이므로) |
| 54 | region별 boundary 상태를 production 계약으로 확정 | `eligible_closed_boundary`/`open_observed_fragment`/`insufficient_observation`/`ambiguous_boundary`/`rejected_unsafe` 5-state 도입, validated closed loop만 materialize, 2-cycle budget exhaustion fail-closed 추가 |
| 55 | eligible region의 downstream 전달 완성 | fail-closed 일관성 검사 + provenance threading + 단일 진입점(`eligible_materialized_surfaces()`) 추가. 첫 시도(region당 최대 1개 closed loop만 유지)가 thin_slab materialization을 회귀시켜 즉시 되돌리고 "전부 유지 + reason으로 명시 disclosure"로 확정 |
| 56 | `eligible_materialized_surfaces()` → Phase D/E(continuation domain·occluded candidate) production bridge | `build_eligible_boundary_continuation_bridge()` + `run_eligible_boundary_continuation_bridge_from_gaussians()` 신설, Phase D/E 두 모듈은 미변경. Phase D의 최소 4-sample 계약이 기하적 안전 조건이 아니라 표현 밀도 관례임을 실측 확인하고 검증된 loop를 결정론적으로 재표본화 — real 5k 2개 region 모두 domain 생성(candidate는 AABB 비접촉으로 0), 남는 실패는 `eligible_visible_only_not_continuation_ready`로 typed 분리 |
| 57 | Occluded candidate → safe uncertain proposal production integration | 기존 Phase F constrained fit → Phase F.1 sampled safety → Phase G proposal을 `run_safe_uncertain_proposals_from_gaussians()`로 Worklog 56 bridge 뒤에 연결. candidate별 1:1 typed accounting과 provenance chain 유지, `degenerate` domain은 Phase E provenance로만 보존하고 Phase F/G 입력으로는 fail-closed rejection. model append/appearance/opacity는 미수행 |
| 58 | Safe uncertain proposal → atomic append production integration | Worklog 57의 `proposed` proposal만 기존 `UncertainGaussianAppendAdapter.append()`(model-only, 4-way transactional)로 전달하는 production 진입점 추가. `initialization_provider`가 명시적 appearance/opacity를 제공해야 하며 합성하지 않음. `_commit_ledger` 실패 주입 시 model/sidecar/registry/ledger 전부 pre-transaction 상태로 롤백 확인 |
| 59 | Appended uncertain Gaussian trainer activation | `appended` receipt만 소비해 기존 trainer optimizer에 안전하게 활성화. Append adapter의 `model.optimizer is None` 전제조건을 만족시키는 임시 분리/재부착을 이 모듈이 담당하고, 기존 `_preserve_optimizer_state()`(ADC clone/split 재사용, 미변경)로 기존 Adam state는 bit-for-bit 보존·신규 행은 0 초기화. Visible/uncertain 분리는 새로 구현하지 않고 기존 `is_uncertain` 마스킹 관행에 의존. 동기화 실패는 전부 롤백 후 `appended_inactive`로 typed 분리 |
| 60 | Atomic trainer activation contract closure | `TorchOSNGSTrainer.activate_and_train_uncertain_step()`으로 실제 rasterizer render+loss 경로에 연결. `append_and_activate()`를 candidate 단위 composite transaction으로 확장해 activation 실패 시 model/sidecar/registry/ledger/optimizer 전부 롤백(`rolled_back`). `masked_optimizer_step()`으로 Adam momentum까지 포함한 true row-level isolation 구현, 롤백 후 optimizer parameter identity를 group-name 재-keying으로 보장 |
| 61 | Real visible chart boundary materialization (physical 경로와 무관한 신규 확장, §2.2 6단계 감사 대상 아님) | Accepted region topology만으로 chart boundary를 구성하는 `eligible_parametric_chart_boundary` 경로 신설(§2.5). Physical `eligible_closed_boundary` 경로는 완전히 미변경. Real 5k/10k 합산 materialized 3→92/4→78 |

**결론: 남은 병목은 파이프라인 결함이 아니라 candidate evidence 자체의 밀도/위상이다.** 3k/10k의 큰 region은 perimeter 전체를 덮을 만큼 충분히 독립적인 observed-termination evidence가 없다 — representative를 더 정확한 위치로 옮기거나(49), 오분류를 고치거나(47/48/50), matching 낭비를 없애도(53) 이 밀도 자체는 바뀌지 않는다. 5k가 성공하는 이유는 이런 결함이 없어서가 아니라 해당 region이 작아서(candidate 4개)다. worklog 54는 이 결론을 region별 5-state production 계약으로 확정했다 — real 5k는 region 130/141만 `eligible_closed_boundary`, 3k/10k는 전부 나머지 4-state로 명시 기록되며 빈 결과/실패로 뭉뚱그려지지 않는다.

**Worklog 61 addendum**: 위 결론은 physical-termination 경로(`eligible_closed_boundary`)에만 적용된다 — 이 경로 자체는 worklog 61에서도 변경하지 않았다. 대신 accepted region topology만으로 chart boundary를 구성하는 완전히 별도인 `eligible_parametric_chart_boundary` 경로를 추가해, physical evidence 밀도와 무관하게 visible NURBS materialization coverage를 real 5k/10k 기준 수십 배 확장했다(§2.5 참고). physical 경로의 candidate evidence 밀도 문제 자체는 여전히 미해결이다.

### 2.3 검증된 negative control (모든 worklog가 반복 확인)

cap=64 기준: Box `physical=51 closed=6 materialized=6`, Cylinder `16/2/2`, Sphere `14/0/0`, Thin slab `37/3/3`. 이 수치는 worklog 47부터 61까지 매 라운드 재확인했다 — 새 작업은 이 표를 반드시 재현해야 하며, 벗어나면 즉시 원인을 규명한다(worklog 53의 2차 self-intersection 결함, worklog 55의 multi-loop exclusion 회귀가 각각 이렇게 발견됐다).

### 2.4 다음 착수 후보 (미승인, 방향 제안일 뿐)

- Candidate GENERATION 밀도 자체(왜 큰 region이 candidate 3~6개에 그치는지) — 지금까지 전부 candidate 전달/분류/정렬 단계만 감사했고 생성 자체의 근본 원인은 아직 안 봤다.
- ordering/quality 비교(`recover_directed_boundary_components`)가 direct/reverse 전체 scene 단위로 이뤄지는 구조 자체의 재검토 — worklog 53에서 이 결합이 예상치 못한 부작용을 만든 사례가 있었다.

### 2.5 Parametric chart boundary 경로 (worklog 61, physical 경로와 완전 분리)

`osn_gs/surface/torch_region_parametric_chart_boundary.py`가 physical termination evidence와 무관하게, region의 이미 승인된 accepted-edge topology만으로 chart boundary를 구성한다(convex hull/bounding box 미사용, leftmost-turn walk로 outer face 추적). Boundary edge는 `physical_termination`/`crease`/`observation_frontier`/`partition_seam` 중 하나로 분류되며 위장하지 않는다. `eligible_parametric_chart_boundary` region만 기존 `materialize_visible_boundary_component()`(미변경)로 materialize되고, `eligible_parametric_chart_surfaces()`로 physical 경로(`eligible_materialized_surfaces()`)와 완전히 분리 노출된다 — Worklog 56 continuation bridge 등 기존 소비자는 여전히 physical 경로만 읽는다(연결 안 됨, 별도 승인 필요).

Real 5k/10k(cap 2048)에서 physical+parametric 합산 materialized surface가 5k 3→92, 10k 4→78로 확장됐다(§2.2 표의 physical-only 숫자와는 다른 지표). §2.3의 cap=64 negative-control physical 수치(6/2/0/3)는 byte-identical로 유지되고, parametric 경로가 추가로 Box 4·Cylinder 1·Sphere 1(physical 0 유지, 별도 provenance만)·Thin-slab 3(partition_seam 1건 명시)를 materialize한다.

## 3. 현재 활성 작업 B — Isolated Boundary-first hardening (구 Section 2, 현재 사실상 비활성)

과거(2026-07 초) 이 절이 기술하던 exporter/cubic seam wedge/observed-anchor central cap 작업(worklog 4-6)은 그 자체로는 dispatcher/production에 연결된 적이 없다. 2026-07-30(worklog 18) 이후 production은 §2의 canonical 경로로 통합됐고, 이 isolated hardening 라인은 이후 세션에서 별도로 재개되지 않았다. 재개할 경우 다음을 여전히 유지한다: exporter의 outer/interior/support/seam/chart 명시적 분리, sampled crossing/fidelity gate, degree-1/fan 임시면의 최종 근거 사용 금지, false-hole evidence 부족 시 `review_required` 유지.

## 4. 현재 활성 작업 C — Uncertain Gaussian model foundations

Phase G proposal, model-only append adapter, occluded chart ownership foundation은 각각 구현·검증된 계약으로 유지한다. 이들은 visible-surface quality를 대신 증명하지 않으며, append 대상의 appearance/opacity와 downstream lifecycle은 여전히 명시적 차단 조건이다.

- raw Gaussian evidence → safe uncertain proposal → model-only atomic append(worklog 57/58) → trainer optimizer activation(worklog 59)까지 production orchestration이 구현됐다. `appended` receipt만 활성화 대상이며, appearance/opacity는 여전히 `UncertainAppendInitialization`이 명시 제공한 값만 사용한다(합성 없음).
- Worklog 59의 activation은 `TorchGaussianModel.optimizer`(Adam)를 기존 `_preserve_optimizer_state()` grow-in-place 로직으로 확장하는 데 그쳤다. Worklog 60은 `TorchOSNGSTrainer.activate_and_train_uncertain_step()`으로 실제 render+loss 경로에 연결하고, append+activate를 candidate 단위 composite transaction으로(activation 실패 시 append까지 전부 롤백), `masked_optimizer_step()`으로 Adam momentum까지 포함한 row-level 학습 격리를 구현했다 — 여전히 `train()`의 실제 iteration 루프/CLI/checkpoint 스케줄에는 연결하지 않았다(`activate_and_train_uncertain_step()`은 호출자가 명시적으로 불러야 하는 별도 메서드). Visible/uncertain 분리 자체는 새로 구현하지 않고 기존 `is_uncertain` 마스킹(`torch_density_control.py`/`torch_pipeline.py`)에 의존한다.
- global ranking/selection, conflict resolution, checkpoint schema 변경, renderer 통합, 실제 `TorchTrainer` 루프로의 배선은 여전히 시작하지 않는다.

근거: `docs/worklogs/1_phase_g_uncertain_gaussian_proposal_foundation.md`, `docs/worklogs/2_uncertain_gaussian_append_adapter_foundation.md`, `docs/worklogs/3_occluded_chart_ownership_foundation.md`, `docs/worklogs/57_occluded_candidate_safe_uncertain_proposal_production_integration.md`, `docs/worklogs/58_safe_uncertain_proposal_atomic_append_production_integration.md`, `docs/worklogs/59_appended_uncertain_gaussian_trainer_activation.md`, `docs/worklogs/60_atomic_trainer_activation_contract_closure.md`.

## 5. 명시적 비범위

다음은 현재 착수 금지다.

- §2 canonical 경로의 candidate/threshold를 scene-specific하게 튜닝하는 일 (box/cylinder/sphere/thin_slab처럼 shape별 예외를 만드는 것 포함)
- gap 보간, 임의 candidate 생성, 또는 closure만을 위한 낮은-evidence edge/cycle 강제 선택
- representative cap 증가로 evidence 부족을 우회하는 일
- optimizer/trainer/renderer/checkpoint 통합 범위 확대(worklog 59/60의 좁은 예외: 이미 append된 uncertain row를 `TorchGaussianModel.optimizer`에 활성화하고, `TorchOSNGSTrainer.activate_and_train_uncertain_step()`을 통해 실제 render+loss로 row-level 격리 학습하는 것까지만 승인됐다 -- `train()`의 실제 iteration 루프·CLI·checkpoint schema로의 배선은 여전히 금지)
- global chart ranking·selection 또는 conflict resolution
- 불완전한 false-hole evidence를 이용한 자동 topology 확정
- benchmark artifact만으로 visible surface 품질이 해결되었다고 선언하는 일

## 6. Training core parity (worklog 62, §2와 독립)

Baseline과의 render/scale/anisotropy 격차(§2와 무관, `output/osn_gs_scene`의 iteration 600 이후 극단적 anisotropy 성장 및 3k+ screen-size prune 폭주의 실제 원인)를 실제 baseline 코드와의 lockstep parity harness로 추적해 `osn_gs/data/colmap_scene.py`의 데이터 로더 결함 2건을 확정·수정했다:

1. `camera_fovs()`가 downscale된 렌더 해상도로 FoV를 재계산하고 있었다(baseline은 COLMAP 원본 해상도에서 1회만 계산하고 이후 절대 건드리지 않음) — 원본 해상도 고정으로 수정.
2. Ground-truth 이미지 resize filter가 `BILINEAR`였다(baseline은 Pillow 기본값 `BICUBIC`) — `BICUBIC`으로 수정.

Fix 후 lockstep 재검증: step 1은 float32 noise 수준까지 일치, step 600(ADC 비활성화, population 통계만) anisotropy median이 baseline과 거의 일치(1.58 vs 1.59) — fix 전 real training에서 관측된 극단적 격차가 사라짐. Surface reconstruction/reliability 코드는 미변경. 자세한 내용은 `docs/worklogs/62_graphdeco_lockstep_training_parity.md` 참고.

**Worklog 63 addendum**: 위 fix를 real 3k production 학습(2900/3000/3100)에 적용했으나, iteration 3100 anisotropy median이 fix 전후 사실상 동일(35.06→35.68, baseline 5.46)하고 screen-size prune도 줄지 않았다(오히려 소폭 증가) — **완료 기준 미충족.** loader 결함은 lockstep 조건에서의 확인된 최초 발산 원인 중 하나일 뿐, 실사용 규모 anisotropy 폭주의 지배적 원인은 아니었다. 자세한 내용은 `docs/worklogs/63_fixed_loader_3k_production_replay.md` 참고.

**Worklog 64 addendum(지배적 원인 확정 및 fix)**: OSN-GS의 유일한 Gaussian 초기화 경로(로컬 PCA planar-surfel)가 `normal_scale = tangent_scale * 0.04`로 모든 새 Gaussian의 iteration-0 anisotropy를 설계상 ~25로 고정하고 있었다 — 이것이 실제 지배적 원인이다. `TorchPipelineConfig.gaussian_initialization_mode`("baseline_compatible" 기본값 / "covariance_knn" experimental)를 신설해 surface construction용 covariance(미변경)와 모델 자신의 학습 가능한 scale/rotation init(신규, Graphdeco의 `create_from_pcd`와 텐서 단위로 동일한 등방 init)을 분리했다. 3-way parity harness로 baseline_compatible이 step 0(anisotropy 정확히 1.0)부터 step 600(ADC 후 population/split 후보 규모)까지 baseline에 근접함을 확인했다. `initialize_deferred` 스케줄은 surface reconstruction 불변 유지를 위해 의도적으로 이 플래그 영향 밖에 둔다. 자세한 내용은 `docs/worklogs/64_gaussian_initialization_parity.md` 참고.

**Worklog 65 addendum(실사용 3k production 검증)**: `baseline_compatible`을 실제 3k 학습(iteration 0/600/2900/3000/3100)으로 검증했다. anisotropy median 35.77(covariance_knn)→3.66(baseline_compatible, baseline 5.49)로 회복, min-scale collapse도 baseline보다 낮은 0.027%로 개선, chart materialization 수 감소(90→11 @2900)는 baseline PLY를 동일 파이프라인에 돌린 참조값(3~8 regions)과 비교해 진짜 over-segmentation 완화로 확인했다(covariance_knn은 그 참조값의 20~60배). 렌더 품질은 매 checkpoint에서 일관되게 baseline 방향 개선이나(PSNR +0.3~0.4) 격차를 완전히 닫지는 못한다. **다만 iteration 3100의 screen-size prune 건수는 224,164→233,178로 줄지 않았다** — anisotropy와는 별개로 opacity-reset 이벤트 자체에 내재한 현상으로 보이며, 아직 원인 미규명이다. 자세한 내용은 `docs/worklogs/65_baseline_compatible_3k_production_validation.md` 참고.

**Worklog 66 addendum(patch fidelity 검증 + 부수 성능 수정)**: materialized visible NURBS patch가 실제 표면을 정확히 재구성하는지 patch당 point-to-surface/surface-to-evidence 정규화 거리·coverage·overlap·Jacobian/orientation/self-intersection·boundary provenance로 검증하고 5-way 분류(threshold는 전부 기존 코드 관례 재사용)했다. valid_supported 비율 baseline_compatible 0%→36%, covariance_knn 27%→33%, baseline 참조 50% — over-segmentation 완화가 진짜 품질 개선과 함께 옴을 확인(covariance_knn은 patch 수는 많지만 gap 비율이 오히려 더 나쁨, 55~59% vs 41~43%). 대다수 patch가 `under_supported`(evidence 3~4개)인 것은 양쪽 OSN-GS 조건 공통의 별개 근본 한계로 남음. 부수적으로 이번 분석 실행 중 `osn_gs/surface/torch_density_preserving_representative_selection.py`의 `_boundary_evidence_swap_in`에서 O(pool²) 개의 개별 GPU sync(파이썬 loop 안에서 CUDA tensor를 한 쌍씩 스칼라로 인덱싱)를 벡터화 — git-stash A/B로 결과 bit-identical 확인 후 10.8배 speedup(338→31초). 자세한 내용과 대표 patch 시각화는 `docs/worklogs/66_visible_patch_coverage_and_fidelity_validation.md` 참고.

**Worklog 67 addendum(under_supported 근본 원인 확정)**: worklog 66의 `under_supported`가 §2.2의 candidate evidence density 병목과 같은 계열인지 검증하기 위해, representative topology/chart boundary는 그대로 두고 각 region이 대표하는 원본 full-cloud evidence를 NURBS fitting support로 복원했다(신규 `osn_gs/surface/torch_region_owned_full_evidence.py`, 기존 production `_propagate_with_evidence_gating()`의 normal-alignment/residual 게이트를 재사용 — 재구현 없음 — 을 소비해 region-owned evidence를 모으고, additive-only로 `_construct_canonical_with_full_evidence`에 연결). 결과: representative-only 대비 full-evidence support가 55~333배 늘었고 `under_supported`/`unsafe_geometry`가 22개 patch 전부에서 사라졌다(전부 `materialized`, worklog 69에서 총계 오류 정정: 기존 "21개"는 오기) — **evidence 부족이 아니라 representative에게만 묻던 fitting 설계의 한계였다.** 대신 95%(21/22)가 `extrapolative`로 재분류됐는데, Graphdeco baseline 참조도 동일 패턴(4/4, 2/2)이라 OSN-GS 학습 품질 문제가 아니라 6×6/degree-2 NURBS fitting 해상도가 조밀한 실제 evidence의 국소 변화를 못 따라가는, 별개의 새로운 발견이다(이번 라운드는 지시대로 해상도를 조정하지 않고 발견만 기록). 자세한 내용은 `docs/worklogs/67_region_owned_full_evidence_patch_support.md` 참고.

**Worklog 68 addendum(extrapolative 원인 확정: overfitting)**: worklog 67의 21/22 `extrapolative`가 fitting capacity 부족/정규화 척도의 밀도 의존성/비균일 weighting 중 무엇인지 가려냈다. 신규 `osn_gs/surface/torch_local_orientation_folding.py`로 global reversal(결함 아님)과 local folding(인접 grid 샘플 간 normal 불일치, 실제 결함 신호)을 분리하고, 22개 patch 전부에(worklog 69에서 총계 오류 정정: 기존 "21개"는 오기) 6×6/8×8/10×10 grid × {uniform, density-compensated} weighting, deterministic spatial holdout, 4종 정규화 척도를 측정했다. **결과: 22개 중 20개(90.9%)가 `overfitting`**(해상도를 올리면 local fold fraction이 늘거나 train error만 개선되고 held-out은 개선 안 됨), 1개만 `capacity_insufficient`, 1개 `inconclusive`. `metric_density_dependent`/`weighting_problem`은 0건 — **grid 해상도를 올리는 것은 안전한 해결책이 아니다**(overfitting을 유발). 정규화 척도의 밀도 의존성 가설과 비균일 weighting 가설도 이번 데이터로는 지지되지 않았다. 자세한 내용은 `docs/worklogs/68_dense_evidence_fit_capacity_and_fidelity_calibration.md` 참고.

**Worklog 69 addendum(single-chart parameterization validity)**: worklog 68의 20/22 `extrapolative`/`overfitting`이 invalid single-chart 때문인지 검증했다. 신규 `osn_gs/surface/torch_single_chart_uv_validity.py`로 UV duplicate/neighborhood preservation/accepted-edge crossing/local fold(3D normal 부호)/boundary containment/parallel-sheet suspicion 6개 기준을 측정. **결과: 22개 전부 `partition_materialization_required`, `uv_valid` 0개.** 다만 neighborhood_preservation은 대부분 건강해(3/22만 위반) 진짜 다중 시트/접힘이 지배적 원인은 아님을 시사하고, 대신 **boundary loop(3~4개 representative 점)가 실제 evidence 범위(수백~수천 점)에 비해 지나치게 작다는 별개의 구조적 문제**(interior_outside_boundary 22/22 위반, 대부분 90~100%)가 드러났다. Partition은 기존 accepted-edge graph에서 안전한 분할 근거가 22/22 모두 없어 전부 fail-closed(0건 적용) — 임의 seam 없이는 분할하지 않는다는 원칙을 지켰다. Density subsampling에서 raw/정규화 오차가 함께 움직여 metric density-dependence 가설을 다시 기각했다. `parallel_sheet_suspected`가 outlier 1개에도 오탐하던 버그를 발견·수정(gap_ratio 39~2088→7~25). 자세한 내용은 `docs/worklogs/69_single_chart_parameterization_validity_and_repair.md` 참고.

**Worklog 70 addendum(dense boundary materialization 시도, 부분 개선·미해결)**: worklog 69의 22/22 `partition_materialization_required`를 canonical 판정으로 채택하지 않고, representative topology/chart boundary는 불변으로 둔 채 region-owned full evidence로 DENSE boundary를 별도 materialize했다(신규 `osn_gs/surface/torch_region_owned_boundary_materialization.py`: edge별 evidence 소유권을 3D point-to-segment distance로 판정, loop centroid 기준 `local_evidence_scale`(worklog 32 재사용, 새 상수 아님) 이상 먼 후보만 채택, edge tangent를 그 scale 폭 bin으로 나눠 bin당 최원점 1개만 순서대로 삽입 — hull/rectangle/강제 폐쇄/타 region 병합 없음, `validate_simple_closed_loop`(미변경) 실패 시 fail-closed). 이번 라운드 안에서 "edge당 1점"과 "모든 후보를 그대로 삽입" 두 대안을 실측 후 기각하고 binning 방식을 채택했으며, PCA-UV 공유 프레임이 outlier evidence에 따라 회전해버리는 불안정성도 발견해 이 모듈은 world 3D 좌표만 사용하도록 설계했다. **결과: 22개 patch 중 `full_evidence_boundary_materialization_required` 11개(dense 확장 자체가 self-intersection으로 fail-closed), `partition_materialization_required` 11개(dense boundary는 성공했으나 `interior_outside_boundary`가 11/11에서 여전히 10% 기준 초과), `valid_supported`/`extrapolative` 0개.** boundary vertex 평균 3.27→8.73, `interior_outside_boundary` 91.1%→54.8%로 측정 가능한 개선은 있었지만(worklog 69 진단을 구조적으로 뒷받침) 어떤 patch도 승인 기준을 통과하지 못했고 held-out fitting 오차도 개선되지 않았다(23.87→28.86) — **원본 topology가 3~4개 edge/wedge만 가져서, 아무리 조밀하게 채워도 evidence의 실제 외곽 형태를 wedge 경계 자체가 따라가지 못한다는 더 구체적인 구조적 한계**로 재확인됐다. `parallel_sheet_suspected`/raw-evidence `triangle_fold_fraction`은 지시대로 진단 전용으로만 기록, gate에서 제외했다. 자세한 내용은 `docs/worklogs/70_region_owned_full_evidence_boundary_materialization.md` 참고.

**Worklog 71 addendum(topology를 직접 복원해도 같은 결론에 수렴)**: worklog 70의 representative-edge densification을 canonical 방식에서 폐기(진단 결과는 유지)하고, worklog 61의 고정 3~4점 loop를 전혀 참조하지 않은 채 typed half-edge evidence(physical termination/crease/observation frontier/ambiguous)와 region-owned full evidence로부터 ordered boundary TOPOLOGY 자체를 복원하는 신규 `osn_gs/surface/torch_region_owned_full_evidence_boundary_topology.py`를 만들었다. 1차로 시도한 "evidence를 반경 기준으로 typed candidate에 직접 붙여 evidence-density 그래프를 통째로 구성"하는 설계는 실측 결과 모든 region이 최대 260-node짜리 하나의 branching blob으로 붕괴해 폐기했다. 최종 설계는 기존 production `build_boundary_compatibility`/`recover_ordered_boundary_components`(미변경)를 representative 밀도에서 그대로 재사용해 SEED-level topology를 복원하고(부수적으로 production이 crease/ambiguous 후보를 계산만 하고 실제로는 병합하지 않는 dead-code 결함을 발견, production은 미변경 유지하고 직접 호출로 우회), 그 함수의 "ordered" 출력이 실제로는 gaussian_id 정렬일 뿐 진짜 geometric walk가 아님을 확인해 동일 accepted edge로 진짜 walk를 재구성했다. `ordered_closed_loop`/`ordered_open_chain`만 region-owned evidence로 edge 단위 densify(worklog 70과 같은 world-3D/binning 알고리즘이지만 `validate_simple_closed_loop`의 nonplanar-즉시-실패 결합을 풀기 위해 별도 재구현)하고, 신규 `evaluate_closed_loop_geometry()`로 3D nonplanarity와 2D proper-crossing 검사를 분리했다(nonplanar만으로는 실패 처리하지 않음). **결과: 실제 37개 region·282개 seed-level component 중 `boundary_topology_closed_loop_recovered` 17건**(self-intersecting 0건, 일부 region은 독립 loop 2개를 병합 없이 각각 보존 — worklog 70과 달리 실제로 evidence-topology 기반 closed loop 복원에 성공). 그러나 17건 전부 최소 크기인 3-vertex 삼각형에 머물러 densify 후에도 `interior_outside_boundary`가 16/17에서 정확히 100%로 남고, 전부 `partition_materialization_required`(0 valid_supported/extrapolative)로 귀결됐다. **worklog 61의 leftmost-turn과 이번 half-edge-그래프 복원이라는 완전히 다른 두 방법이 같은 결론에 수렴**했다 — 문제는 특정 boundary 구성 알고리즘의 한계가 아니라, typed half-edge evidence 자체가 어떤 방법으로 조립하든 region의 실제 evidence 범위에 비해 본질적으로 희소하고 국소적이라는 더 근본적인 사실이다. 자세한 내용은 `docs/worklogs/71_region_owned_full_evidence_boundary_topology_reconstruction.md` 참고.

**Worklog 72~74 addendum(다른 세션, dense boundary support 및 closure attribution)**: sparse typed half-edge loop 대신 region-owned full evidence의 local angular support에서 dense boundary 후보를 추출하고(`torch_region_owned_dense_boundary_support.py`, baseline-compatible@2900에서 후보 785개), mutual directional connectivity의 실패를 half-line 단위로 귀속했다. half-line 68.03%가 full-evidence local scale 내 continuation 부재로 종료됐고, distance-stage cycle 34개 중 13개가 tangent incompatibility로 제거돼 covariance-derived orientation이 scale mismatch와 **독립된** cycle-destruction 병목임을 확인했다. closed loop은 여전히 0. 자세한 내용은 `docs/worklogs/72~74` 참고.

**Worklog 75 addendum(normal-source 아키텍처 결정 종결: KEEP covariance_normal)**: worklog 74가 지목한 "explicit structural-normal A/B"를 단일 bounded 비교로 수행해 **normal-source 질문을 종결**했다(추가 진단 라운드를 만들지 않는 것이 이 라운드의 명시적 목적). 신규 실험 전용 `osn_gs/surface/torch_structural_normal.py`가 region-owned observed 점의 **위치만**으로 local PCA normal을 만들고(scale/rotation/covariance/SH/opacity/renderer/optimizer 미사용, photometric training에서 완전 분리), 기존 missing-sector/outward-direction 로직으로 tangent만 재유도한다. candidate id·xyz, region ownership, boundary reason, candidate extraction, connectivity scale/distance threshold, ambiguity/mutuality, topology acceptance를 전부 고정했고 **B는 candidate를 독립적으로 재추출하지 않는다**(frozen 집합의 orientation frame만 재계산). Scene은 기존 fixture/checkpoint만 사용: `box_face`(평면), `cylinder`(crease/곡면), real `baseline_compatible@2900` 4개 region.

**결과: 합성면에서는 두 모드가 모든 지표에서 완전히 동일**(normal 불일치 median 0.16~0.42°, 곡면 side 7.31°) — structural normal이 단순 표면을 열화시키지는 않는다. **그러나 real 데이터에서는 normal이 median 30.8~63.6°(p90 76.9~82.8°) 어긋나면서도 final closed loop은 8개 region 전부 0 vs 0으로 동일하다.** 집계상 tangent rejection은 33→22로 줄지만 그 손실이 사라진 게 아니라 **normal 단계로 이동**할 뿐이고(5→40, 8배), 순 생존 edge는 tangent 240→198(−17.5%)·mutuality 94→86(−8.5%)로 real region 4개 중 3개에서 명확히 악화된다. 추가로 structural normal 기준 재검 시 A가 이미 인정한 candidate의 30~60%(cylinder side는 12/12)가 boundary admission을 통과하지 못해, drop-in 교체가 아니라 "어디가 boundary인가"를 조용히 바꾸는 변경이다. Rendering은 real CUDA rasterizer로 A/B 전후 `torch.equal` bitwise identical 확인, 비용은 오히려 저렴(structural normal 계산 region당 <3ms, 추가 메모리 N×3×4 byte, 영속 state 없음).

**판정: KEEP covariance_normal.** 단 이 결과는 covariance normal이 우수하다는 뜻이 **아니다** — worklog 74의 가설(covariance tangent이 cycle을 깬다)은 부분적으로 확인됐으나(normal source를 바꾸면 tangent rejection은 실제로 감소), 그 개선이 최종 topology로 이어지지 않고 손실이 상류로 재배치될 뿐이라는 것이 확정됐다. **즉 normal source는 현재 boundary-closure 실패의 구속 조건(binding constraint)이 아니다.** 두 orientation source가 real 데이터에서 median 30~64° 어긋나면서도 결과가 0 vs 0으로 같다는 것은 병목이 orientation 정확도가 아니라 그 상류(candidate/support 밀도·scale domain)에 있음을 가리킨다. 이 라운드에서 `boundary_support_spacing`은 활성화·재설계하지 않았고 connectivity threshold도 변경하지 않았다. 자세한 내용은 `docs/worklogs/75_structural_normal_vs_covariance_normal_decision.md` 참고.

**Worklog 76 addendum(scale domain도 병목에서 제거: full_evidence_spacing 유지)**: worklog 72~74가 지목한 "half-line 68%가 local scale 내 continuation 부재"가 scale domain 단위 오류인지 한 배치에서 종결했다. 신규 `osn_gs/surface/torch_boundary_support_spacing.py`가 `full_evidence_spacing`/`representative_spacing`(report-only)/`boundary_support_spacing`을 의미적으로 분리하고, estimator 3종(현행 full-evidence / region 단위 robust candidate spacing / candidate별 robust local spacing)만 비교한다. **2.5배 거리 배수와 0.1배 ambiguity 허용은 모드별로 조정하지 않았고 connectivity certificate도 재설계하지 않았다.** production에는 가산 인자만 추가했고(기본값이 이전 동작과 동일), real 7개 region 기본 경로가 worklog 72 발표 수치(open_or_ambiguous 621 / closed 0)와 정확히 일치함을 회귀 확인했다.

**결과: 단위 오류는 실재했고 두 독립 scale이 실제로 그것을 고친다** — `no_candidate` 1108/1652(67%)→254(region)/**21(local, 1.3%)**, neither 방향 486→46, branch explosion 0, proper crossing 0. **그러나 그 연속성의 출처가 관측되지 않은 gap이다**: 미지지(빈 구간 포함) edge가 9/185(4.9%)→191/397(48.1%)/211/427(49.4%)로 10배 뛰고, real 전 region에서 단일 edge 길이의 **75~92%가 관측된 빈 공간을 지나는** 사례가 나온다(baseline은 중앙값 0.0/p90 0.0). 이는 금지된 gap bridging이며, 그 대가로 얻는 real closed loop은 region6의 1개뿐이고 그 loop의 `interior_outside_boundary`는 99.78%로 worklog 70/71과 동일한 퇴화 결과다.

**결정: production 연결 scale은 `full_evidence_spacing` 유지(독립 scale 미채택).** 세 spacing을 분리한 계약 자체는 유지하되 기본 모드는 바뀌지 않으며, 남은 실패는 **boundary-support predicate/evidence 자체**에 귀속한다. production이 미변경이므로 real dense boundary는 closed loop 0 그대로이고, reduced UV gate/6×6 NURBS에 도달하는 boundary가 없어 `valid_supported`/`extrapolative`/`partition_materialization_required`가 전부 0이다 — **gate 탈락이 아니라 gate 입력 부재**이며 두 상태를 혼동해 기록하지 않는다. worklog 73의 "68% 거리 실패"는 단위 오류가 가린 회복 가능한 연속성이 아니었다: 단위를 바로잡으면 실패는 사라지지만 그때 연결되는 상대가 관측 근거 없는 먼 candidate다 — candidate들이 애초에 공간적으로 인접하지 않다. 자세한 내용은 `docs/worklogs/76_boundary_support_spacing_scale_domain_decision.md` 참고.

**Worklog 77 addendum(predicate 결함 규명·수정, 그리고 아키텍처 답 확정)**: worklog 75/76이 normal source와 scale을 제거한 뒤 남은 dense boundary-support predicate를 감사해 **실재하고 국소적으로 귀속되는 결함 하나를 규명·수정**했다. 각도 gap은 양옆 이웃 "광선" 사이로 측정되는데 이웃은 연속 표면의 점 표본이므로 국소 각도 표본 해상도만큼 체계적으로 과소평가되고, **직선 boundary의 참 empty sector는 정확히 pi**라서 raw 측정값이 pi에 아래에서 수렴한다 — threshold가 엄격한 것이 아니라 **추정기가 점근적으로 편향**돼 있었다(box_face 실측: 참 boundary gap/pi median 0.9989로 32점 중 25점이 pi의 1% 이내, interior는 0.25pi로 완전 분리). threshold(pi)는 유지하고 **그 점 자신의 비최대 gap 중앙값만큼 편향만 보정**했다(새 상수 없음, 밀도가 오르면 보정항 0으로 수렴해 `gap >= pi`로 회귀). **완화가 아님의 직접 증거: precision 전 fixture 1.000 불변, 닫힌 sphere candidate 0 유지, 신규 테스트가 수정 전 직선-boundary 2건만 실패하고 fail-closed 계열은 전부 통과.**

합성 ground truth(fixture 생성식에서 직접 유도) 기준 recall box_face 0.438→**1.000**, cylinder side 0.250→**1.000**, 연속 누락 run 최대 4~5→**0**. 수정 후 **기존 미변경 connectivity**에서 **box_face가 worklog 69~77 통틀어 처음으로 interior evidence를 100% 포함하는(interior_outside_boundary 0.000) 유효 closed loop**을 생성했고, cylinder side는 위/아래 두 boundary ring을 병합 없이 각각 복원했다(closed 2, crossing 0). Real baseline_compatible@2900 7개 region에서는 candidate **785→940(+19.7%)**, degenerate frame 0, branch 0, crossing 0이지만 **closed loop은 여전히 0**이고, accepted candidate 사이 경로의 **71.7%가 관측 증거 자체의 부재**이며 predicate 거부가 놓인 경우는 28.3%뿐이다.

**아키텍처 답 확정: 둘 다이지만 지배적인 것은 관측 증거의 부재다.** predicate에는 실제 결함이 있었고 실제로 고쳐졌지만(합성에서 recall 100%·유효 loop 생성), real의 잔여 실패는 predicate가 아니라 evidence에 귀속된다 — **동일한 수정 predicate가 관측이 연속적인 경우에는 완전한 perimeter와 유효 loop을 만들어낸다는 합성/real 대비가 결정적 증거다.** 증거가 없는 곳에서는 fail-closed를 유지했다(sphere 0, real 0 loop, 기하 fallback 없음). gap 거부 점의 40~66%가 near-normal 이웃 오염을 포함한다는 2차 관찰은 합성에서 0건이라 ground truth 검증 수단이 없어 측정만 하고 조치하지 않았다. 자세한 내용은 `docs/worklogs/77_boundary_support_predicate_discretization_bias.md` 참고.

**Worklog 78/79 addendum(constructor-wide 귀속과 chart-domain coverage 계약, 그리고 최종 판정)**: worklog 78(다른 세션)은 physical termination과 parametric chart frontier의 typed provenance 누락을 수정해 relation half-edge가 chart provenance에 전달되도록 했다. worklog 79는 그 뒤 남은 real 실패를 one-factor ablation 없이 **chart topology→domain→UV→NURBS fitting을 한 번의 constructor-wide pass**로 귀속했다.

**Eligibility 계약 검증**: chart boundary edge를 만드는 `adjacency`는 오직 `region.internal_accepted_edge_ids`로만 구성되고 relation half-edge는 **라벨만 제공하며 edge를 만들 수 없다** — ambiguous 증거가 topology 지지 없이 boundary가 되는 경로는 구조적으로 없고 실측에서도 ambiguous 승격 0건이다. 다만 **별개의 공백**이 있다: eligibility가 "accepted topology가 simple cycle인가"만 보고 **그 chart domain이 자신이 fitting될 evidence를 담는지는 보지 않아** 3-node 삼각형이 무조건 eligible이 된다.

**7-region 매트릭스**: materialized 5개 chart가 **전부 동일 원인 `boundary_chart_extent_mismatch`이고 실패 단계가 전부 parameterization 이전**이다. representative topology는 3~7 노드로 evidence extent의 0.15~0.67배만 덮는데, ownership propagation에 in-plane 거리 상한이 없어 region이 93~1035점을 소유한다(evid/member **31~301배**) — 결과적으로 evidence의 **89.1~99.8%가 chart domain 바깥**이다. Jacobian near-degenerate 0, local fold ≤0.63%로 **fitting geometry는 전부 건강**하므로 fitting/UV 개선으로는 사라지지 않는다. worklog 78이 region 3을 valid(p95 4.0)로 본 것은 train 기준이며, 결정론적 held-out에서는 6.43으로 **held-out 기준 valid_supported는 5개 중 0개**임을 정정했다. no-chart인 region 4/5는 강제 폐쇄·임의 분할 없이 자기 accepted-edge 구조로 분류했다(5 = pendant를 가진 genuinely open, 4 = 2-core 단일 성분의 ambiguous branching — 다중 chart 필요로 단정할 근거 없음).

**교정**: `fit_region_owned_full_evidence_patch`에 **chart-domain coverage 계약**을 추가해, evidence의 과반이 domain 밖이면 fitting 이전에 `chart_domain_does_not_cover_evidence`로 fail-closed한다(containment는 boundary loop 자신의 best-fit 평면에서 측정, 기존 `interior_within_boundary` 재사용). 실측 위반이 82.6~99.6%로 0.5에서 멀어 **0.5~0.85 전 구간에서 판정이 동일**함을 전용 테스트로 고정했다 — 외삽을 줄이려는 threshold 튜닝이 아니다. 부수적으로 기존 테스트 fixture가 `grid[:4]`(모두 x=-0.5인 **공선 4점, 면적 0인 boundary**)로 이번에 고친 결함을 그대로 인코딩하고 있었음을 발견해 실제 perimeter loop으로 교정했다.

**Before/after(real baseline_compatible@2900 전체 재실행)**: full-evidence fit `materialized` 5→**0**, `extrapolative` 5→**0**, `chart_domain_does_not_cover_evidence` 0→**5**(82.6/94.8/93.2/82.6/99.6%). eligible chart boundary 5, representative-scale chart surface 5, physical surface 0, no-chart region 2는 전부 **불변** — 바뀐 것은 "이 chart가 region의 owned evidence를 대표한다"는 주장이 거짓일 때 typed fail-closed된다는 점뿐이다.

**최종 판정: 현재 visible NURBS constructor는 real 데이터에서 사용 가능하지 않으며, region/chart 표현 자체의 재설계가 필요하다.** 5개 chart가 전부 같은 원인으로, 전부 parameterization 이전에 실패하며, geometry는 건강하다. 원인은 국소 버그가 아니라 표현의 구조다 — **topology를 담는 단위(3~7 representative)와 evidence를 담는 단위(93~1035 point)가 31~301배 어긋나 있고 전자가 후자의 공간 범위를 0.15~0.67배만 덮는다.** worklog 79의 계약은 이 어긋남을 정직하게 드러낼 뿐 없앨 수 없다. 자세한 내용은 `docs/worklogs/79_constructor_wide_chart_domain_coverage.md` 참고.

**Worklog 80 addendum(parametric chart 표현 재설계: topology/geometry 역할 분리)**: worklog 79가 확정한 구조적 실패를 진단이 아니라 **표현 재설계**로 다뤘다. 신규 `osn_gs/surface/torch_dense_parametric_chart_support.py`가 두 역할을 분리한다 — **sparse accepted topology는 perimeter의 순환 순서와 arc별 typed frontier provenance만** 제공하고 chart의 기하 범위로는 절대 쓰이지 않으며, **기하는 worklog 77의 보정된 predicate가 뽑은 region-owned dense boundary-support candidate**가 제공한다(모든 chart 정점이 관측 Gaussian). 실측 근거: dense candidate의 공간 범위/owned evidence 범위 = **0.966~1.020**, 이들이 대체하는 representative는 **0.148~0.667**. 각 candidate는 가장 가까운 sparse arc에 귀속돼 typed provenance를 상속하고, arc 내부 단조 정렬 후 순환 순서로 연결된다 — **representative는 최종 loop의 정점이 아니다.** dense support 부재 시 sparse polygon fallback 없이 fail-closed, 미지지 arc는 `evidence_backed=False`로 명시. 다중 chart는 2-core가 서로소 성분 2개 이상일 때만 허용.

구현 중 자체 결함을 발견·수정했다: arc binning의 bin 개수를 **sparse chord 길이**에서 유도해 이 모듈이 없애려는 스케일 불일치를 재도입하고 있었다(region 6이 candidate 218개 중 16개만 유지). candidate 자신의 투영 span 기준으로 고쳐 88정점/coverage 56.1%, region 0은 6→17정점/59.1%→31.2%가 됐다.

**7-region before/after**: chart-domain coverage 통과 **0/5 → 4/5**, chart 정점 3,3,3,3,3 → **17,84,92,20,88**, domain 밖 evidence **82.6~99.6% → 29.0~56.1%**, Jacobian near-degenerate 0 유지, fitted-surface folding ≤0.45%. **그러나 통과한 4개는 전부 `extrapolative`(held-out p95 5.49~19.92)이고 valid_supported는 여전히 0이다.**

**원인이 분리돼 드러났다**: region evidence 자체를 PCA-UV로 파라미터화하면 **UV-인접 삼각형의 21~36%에서 3D normal 부호가 어긋난다**(neighborhood preservation 0.53~0.70) — region이 단일 정규 chart가 아니다. worklog 69도 같은 것을 봤으나 그때는 extent 문제와 뒤섞여 있었고, extent를 제거한 지금 이것이 분리된 지배 원인으로 남는다. 다중 chart가 답인지 확인했으나 `independent_chart_components`가 region 0/1/2/3/4/6 전부 1, region 5는 0으로 **기존 topology는 어떤 분할도 증명하지 못한다** — 근거 없는 분할 금지 제약에 따라 채택하지 않았다.

**판정 (3): 기존 region/evidence 표현은 evidence-backed chart domain은 공급할 수 있으나, 유효한 *단일* chart domain은 공급하지 못한다.** 재설계 자체는 viable하고 채택 가치가 있다(worklog 79의 구조적 결함을 실제로 해소). 다만 real 사용 가능한 chart는 만들지 못하며, 실패 위치가 "chart가 evidence를 담지 못함(parameterization 이전)"에서 **"evidence가 단일 정규 chart가 아님(parameterization 단계)"** 으로 이동했을 뿐이다. 남은 병목은 chart 구성 알고리즘이 아니라 **region 자체가 단일 chart가 아니라는 사실**과 **기존 accepted topology가 그 분할을 증명할 해상도를 갖지 못한다는 것**이다. 자세한 내용은 `docs/worklogs/80_dense_parametric_chart_representation_redesign.md` 참고.

**Worklog 81 addendum(intrinsic boundary-conditioned parameterization 판정)**: worklog 80이 통과시킨 4개 real chart(region 0/1/2/3)가 왜 전부 `extrapolative`인지 — PCA-UV가 부적절한 parameterization인지(A), evidence 자체가 단일 injective chart를 지지 못하는지(B/C) — 를 하나의 constructor-level 결정으로 닫았다. 신규 `osn_gs/surface/torch_intrinsic_boundary_parameterization.py`: worklog 80의 dense chart boundary를 고정 convex 도메인(unit circle, 원 순회 순서·상대 arc-length 유지)으로 못박고, interior evidence는 3D 원시 kNN 그래프(전역 축 없음)로 discrete Laplace(Tutte) 방정식을 boundary Dirichlet 조건 아래 풀어 **injectivity를 수학적으로 보장**하는 대안 UV를 만든다.

**동일한** `fit_torch_visible_surface_lsq`(6×6, degree 2) 호출에 `initial_uv`만 PCA-UV/intrinsic-UV로 바꿔 real 4-region을 재적합한 결과, **intrinsic이 PCA-UV보다 4개 region 전부에서 더 나쁘다**: neighborhood preservation 0.53~0.70 → 0.39~0.63로 하락, fold% 21~36% → 47~70%로 상승, region 1/2에서 UV 근접 충돌 신규 대량 발생(2→79, 2→100). `knn_k` 8→20으로도(그래프 희소성 배제) 결과 불변. `interior_outside_boundary_count`는 설계대로 4개 전부 0.

원인을 직접 측정했다: owned evidence의 국소 PCA normal(k=10)이 region canonical normal과 60도 넘게 어긋나는 비율이 **16.3~37.4%**, canonical-normal 방향 두께/tangent extent 비율이 **0.169~0.546** — evidence 자신이 국소적으로도 단일 tangent plane을 따르지 않는, parameterization과 무관한 3D 형상 문제임을 확인했다.

**판정: A(PCA-UV를 intrinsic parameterization으로 교체)는 명확히 기각된다** — injectivity를 수학적으로 보장하는 대안조차 실측상 더 나쁘므로, 실패는 parameterization 선택의 문제가 아니다. **B와 C의 경계**: evidence가 단일 chart를 지지하지 못한다는 근거(국소 normal 불일치·두께비)는 이번에 직접 확보했으나, worklog 80의 `independent_chart_components`가 4개 region 전부 1이라 그 분해를 증명할 topology 해상도가 없다. 지시대로 임의 다중 chart 분할은 적용하지 않았고, production PCA-UV도 교체하지 않았다(판정이 A가 아니므로 전체 regression 미실행, 지시). 자세한 내용은 `docs/worklogs/81_intrinsic_boundary_conditioned_parameterization_decision.md` 참고.

**Worklog 82 addendum(evidence-scale surface-consistency chart-unit 분해: Region↔Chart 1:1 해체)**: worklog 81이 확정한 evidence 자체의 국소 비평탄성(normal 불일치 16.3~37.4%, 두께비 0.169~0.546)을 **Region과 Parametric Chart의 암묵적 1:1 관계 해체**로 다뤘다. 신규 `osn_gs/surface/torch_dense_surface_consistency_components.py`: region-owned dense evidence 위에 bounded-degree kNN(raw radius graph·dense clique 아님) candidate adjacency를 만들고, `torch_gaussian_manifold_affinity.py`의 same_surface 기준(normal alignment≥0.85, mutual tangent residual≤0.35, **재조정 없이 재사용**)을 통과한 edge만 `same_surface`로 승인한다. worklog 80의 이미 타입된 crease/frontier arc 양쪽에 걸친 edge는 기하가 아무리 좋아도 veto된다(재사용된 provenance, 새 separator 발명 아님). component는 `same_surface` edge만으로 만든 connected component(crease/ambiguous는 절대 병합 안 함); same_surface edge가 0인 점은 unresolved로 fail-closed, 내부 normal 불일치가 15% 넘는 component는 `non_manifold_suspected`로 표시해 chart 재료에서 제외한다.

`scripts/devtools/dense_surface_consistency_replay.py`로 region-owned evidence→component(신규)→component별 worklog 80 dense chart support(region 대신 component 단위로 적용)→worklog 79 coverage(변경 없음)→PCA-UV(worklog 81 확정, 대안 없음)→6×6 NURBS(변경 없음)→held-out 평가를 7개 real region 전체에 실행했다. **residual 정규화 선택을 직접 검증**: kNN spacing(선택값) 대신 대표-그래프 기본값 `tangent_major_scale`로 바꾸면 median residual이 0.34→0.63으로 더 엄격해져, 선택값이 이미 더 관대한 쪽이었음을 확인 — 파편화가 정규화 버그가 아니라 evidence 자체의 잡음이라는 것을 worklog 81과 독립적으로 재확인했다.

**실측: 7-region 합계 component 364개 → valid_supported 16(region 1/2/6), extrapolative 8, unsafe_geometry 9, no_chart 331(91%)** — worklog 79~81 전 라운드에서 정확히 0이었던 valid_supported가 처음 나왔다. 그러나 component 크기 중앙값은 3~6점(evidence 92~1035점 대비)으로 심하게 파편화됐고, 가장 큰 component(24~157점, `non_manifold_suspected=False`로 국소 일관성 자체는 통과)도 대부분 extrapolative/no_chart로 끝난다. region 4/5는 여전히 유효 chart 0개(worklog 80의 ambiguous branching/open topology 분류와 일치, 강제 참여 없음).

**판정 (B): evidence는 다중 chart 단위를 지지하지만, 현재 evidence-scale topology 해상도로는 그것을 chart 스케일 단위로 안전하게 조립하지 못한다.** valid_supported 16개는 (A) canonical 채택 또는 (C) evidence가 근본적으로 boundary-first parametric 가정에 부적합하다는 주장을 둘 다 기각하는 실재 증거다 — 다만 91%가 no_chart로 버려지는 표현을 그대로 canonical Region→Chart 계층으로 채택할 수는 없다. 자세한 내용은 `docs/worklogs/82_dense_surface_consistency_chart_unit_decomposition.md` 참고.

**Worklog 83 addendum(chart-scale topology/assembly: worklog 82 micro-component 조립)**: worklog 82의 91% no_chart 파편화(중앙값 3~6점)를 threshold/kNN/normal/residual 재조정 없이 다뤘다. 신규 `osn_gs/surface/torch_dense_chart_unit_assembly.py`: worklog 82 micro-component를 보수적 atomic 증거로 취급하고, **단일 pointwise edge가 아니라 aggregate 증거**로만 component-level adjacency를 만든다 — proximity gate(worklog 72 `_connect`의 2.5x multiplier 재사용) 통과 후, typed crease veto(worklog 82와 동일)가 없고, 3개 독립 aggregate 신호(normal alignment 평균≥0.85, 개별 same_surface correspondence 개수≥3, `measure_edge_support_occupancy`(worklog 76, 처음으로 acceptance 신호에 사용) 기준 빈 구간 없음) 중 **2개 이상**을 통과해야 `ACCEPTED`, 아니면 `AMBIGUOUS`로 명시 disclosure(병합·폐기 없음). fit 품질은 함수 입력에 없음(전용 테스트로 시그니처 검증).

`scripts/devtools/dense_chart_unit_assembly_replay.py`로 7개 real region 전체 실행한 결과: **micro-component 364개 → chart unit 178개(52% 감소)로 파편화는 해소**됐다(region당 evidence의 75~93%가 4점 이상 chart-unit 후보로 회수, 최대 239점 단일 unit까지 병합). 그러나 **no_chart 비율은 90.9%→91.0%로 사실상 그대로**다. **gap bridging을 직접 감사**: ACCEPTED edge 219개 전부 occupancy 신호를 실제로 통과했고(0개가 normal+correspondence만으로 수락), 관측되지 않은 공백을 건넌 병합은 없었다.

**병목이 이동했다**: 남은 no_chart의 원인이 이제 "micro-component가 너무 작음"이 아니라 worklog 80의 sparse macro-topology arc-typing 해상도 부족(38건)과 dense chart boundary 구성 자체의 실패(coverage 실패 27+self-intersecting 18=45건)다. valid_supported는 16→4로 줄었는데, 병합 자체의 결함이 아니라 더 커진 evidence 규모에서 기존 PCA-UV+6×6 fit(재검토 금지 대상)이 hold-out 기준을 만족시키기 어려워졌기 때문(예: region 6 unit 3은 74점으로 병합되고도 valid_supported 유지, p95 3.58 — 큰 assembled chart도 유효할 수 있다는 실증). region 4/5는 이번에도 valid chart 0개(worklog 80 분류와 일치).

**판정 (B, 세분화): local surface unit은 실재하고 aggregate 증거로 chart 스케일까지 조립 가능하지만(파편화는 해소), 그것을 최종 chart로 안전하게 완성할 만큼 macro-topology 해상도와 chart-boundary 구성 능력이 아직 충분하지 않다.** (A) 지지 안 됨(no_chart 비율 불변), (C) 다시 기각(assembly가 실재하는 대규모 coherent 증거를 gap bridging 없이 회수). 자세한 내용은 `docs/worklogs/83_dense_chart_unit_assembly.md` 참고.

**Worklog 84 addendum(chart-unit coherence audit + evidence-scale boundary 표현)**: worklog 83이 남긴 결합된 두 질문 — assembled unit이 진짜 coherent인가 over-merge인가, sparse macro node 없이 자기 evidence만으로 boundary topology를 세울 수 있는가 — 를 한 배치로 닫았다.

**coherence audit**: worklog 82의 `internal_normal_disagreement_fraction`(0.15 bound, 동일 공식)을 함수로 분리해 assembled unit 전체 규모에 재적용했다(fit 품질 아님, 순수 evidence 기반).

**evidence-scale boundary**: 첫 설계(`extract_dense_boundary_support`의 `_connect` closed-loop을 순서로 재사용)는 real 178개 unit에서 **0/178 materialize**로 실패했다 — worklog 71이 이미 기록한 한계(282개 중 17개만 closed_loop_recovered)를 그대로 재현함을 직접 측정 후 확인하고 재설계했다. 신규 `osn_gs/surface/torch_chart_unit_evidence_scale_boundary.py`: `extract_dense_boundary_support`는 candidate admission(worklog 77 predicate, 미변경)에만 쓰고, 승인된 candidate를 **unit 자신의 tangent plane에서 centroid 기준 각도순 정렬**한다(sparse 의존 없음) — `evaluate_closed_loop_geometry`(worklog 71)로 self-intersection 검증, **`measure_edge_support_occupancy`(worklog 76, 이번엔 gap-bridging을 실제로 막는 최종 안전장치로 사용)로 loop의 모든 edge(wrap-around 포함)에 빈 interior bin이 없는지 검증**한다 — 열린 조각을 직선으로 닫아버리는 경우를 self-intersection이 못 잡는 것을 이 장치가 대신 잡는다(half-ring fixture로 직접 검증).

**실측(evidence 3526점 가중, 7개 real region 전체): assembled/coherent 88.1%(178/178 unit 전부 coherent, over-merge 0건), materialized 1.5%, valid_supported 0.9%, unsafe 0.5%, no_chart 86.7%**(unsupported_closure 95건, no_dense_support 71건, coverage_failed 2건). **16→4 손실을 11개 unit 전부 직접 추적해 판별**: 5개는 valid_supported 유지, 6개는 전부 `unsupported_closure`로 실패 — `ambiguous_or_over_merged`는 0건.

**판정 (B): chart unit은 조립 가능하고 over-merge는 실재하지 않지만(C 기각), 관측 evidence는 대부분에 대해 안전한 boundary를 세우기엔 아직 불충분하다(A 기각).** 손실은 legitimate exposure이지 over-merge나 혼합이 아니다. sparse macro node 의존 제거 목표는 달성했으나, evidence-scale candidate 밀도 자체가 unit이 커질수록 전체 perimeter를 감싸기에 부족해진다는 더 정확한 병목이 드러났다. 자세한 내용은 `docs/worklogs/84_chart_unit_coherence_audit_and_evidence_scale_boundary.md` 참고.

다음 방향(worklog 84 판정 기준): (a) opacity-reset 직후 screen-size prune storm 원인 추적, (b) evidence-scale boundary-support candidate 밀도가 큰 unit의 전체 perimeter를 감쌀 만큼 충분해지는 조건(예: unit 크기별 필요 candidate 밀도, occupancy 실패가 집중되는 unit 형태) 규명 — 이번 배치가 시도하지 않은 별개 진단이라 새 근거 없이 재개하지 않는다. orientation(75)·scale(76)·predicate(77)·chart 기하(79/80)·parameterization(81)·chart-unit 분해(82)·chart-unit 조립(83)·coherence/evidence-scale boundary(84)는 각각 소진됐으므로 새 근거 없이 재개하지 않는다.

## 7. 현재 검증 상태와 알려진 위험

Repository-wide pytest 최신 기준(worklog 80 full regression): `884 passed, 1 skipped, 2 warnings, 18 subtests passed`. worklog 65는 코드 변경 없는 replay라 재실행 안 함. **worklog 66~80은 production 코드를 변경했지만(각각 `torch_density_preserving_representative_selection.py` 순수 성능 최적화, `torch_pipeline.py`+신규 `torch_region_owned_full_evidence.py` additive-only 기능 추가, 신규 `torch_local_orientation_folding.py` 단독 모듈, 신규 `torch_single_chart_uv_validity.py` 단독 모듈, 신규 `torch_region_owned_boundary_materialization.py` 단독 모듈, 신규 `torch_region_owned_full_evidence_boundary_topology.py` 단독 모듈, 72~74의 dense-boundary support/진단 3개 단독 모듈, 75의 실험 전용 `torch_structural_normal.py` 단독 모듈, 76의 신규 `torch_boundary_support_spacing.py` + dense-boundary 2개 모듈에 대한 가산 인자, 77의 `torch_region_owned_dense_boundary_support.py` 각도-gap 편향 보정, 78의 `torch_visible_surface_construction.py` chart provenance 전달, 79의 `torch_region_owned_full_evidence.py` chart-domain coverage 계약, 80의 신규 `torch_dense_parametric_chart_support.py`) 지시에 따라 매 라운드 focused pytest만 실행했다(66: 86개, 67: 232개, 68: 5개, 69: 13개, 70: 7개, 71: 14개, 72: 16개, 73: 3개, 74: 2개, 75: 15개, 76: 42개, 77: 49개, 78: 27개, 79: 84개, 80: full regression, 전부 통과). **worklog 81은 신규 실험 전용 `torch_intrinsic_boundary_parameterization.py` 단독 모듈만 추가했고 production PCA-UV는 교체하지 않았다(판정이 A가 아니므로) — 지시대로 focused만 실행(신규 12개 + 관련 기존 48개, 전부 통과). worklog 82도 신규 실험 전용 `torch_dense_surface_consistency_components.py` 단독 모듈만 추가했고 canonical Region→Chart 계층은 교체하지 않았다(판정이 A가 아니므로) — 지시대로 focused만 실행(신규 7개 + 관련 기존 34개, 전부 통과). worklog 83도 신규 실험 전용 `torch_dense_chart_unit_assembly.py` 단독 모듈만 추가했고 canonical Region→Chart 계층은 교체하지 않았다(판정이 A가 아니므로) — 지시대로 focused만 실행(신규 7개 + 관련 기존 54개, 전부 통과). worklog 84도 신규 실험 전용 `torch_chart_unit_evidence_scale_boundary.py` 단독 모듈만 추가했고(기존 `torch_dense_surface_consistency_components.py`에서 disagreement 계산을 함수로 분리하는 순수 additive 리팩터만 있었고, 분리 전후 값이 동일함을 기존 테스트 재실행으로 확인) canonical Region→Chart 계층은 교체하지 않았다(판정이 A가 아니므로) — 지시대로 focused만 실행(신규 9개 + 관련 기존 36개, 전부 통과). full-suite 숫자는 worklog 80 시점(884)으로 유지된다.** 다음 라운드에서 production 코드를 추가로 건드리기 전에 먼저 전체 pytest를 한 번 재확인한다. §2.3의 negative-control 표와 함께 매 라운드 재확인한다.

알려진 위험:
- §2.2 표의 real 3k/10k closed-loop 부재는 미해결이며, §2.4 후보(candidate 생성 밀도) 전에는 착수 승인이 없다.
- §2 파이프라인의 direct/reverse tangent quality 비교는 scene 전체 단위 선택이라, 한 region의 수정이 다른 region의 결과를 바꿀 수 있다(worklog 53에서 실측). 이 구조를 변경하는 작업은 반드시 §2.3 negative-control 전체 재확인을 동반한다.
- §3(Isolated Boundary-first)은 재개 시 이 문서를 먼저 갱신하고 시작한다 — 현재는 활성 작업자가 없다.
- **`output/osn_gs_scene/*/checkpoint.pt`가 세션 사이 완료된 학습 run으로 교체됐다** — worklog 45-60이 참조하던 real 5k region 130/141 등 특정 region ID/개수는 더 이상 현재 checkpoint와 일치하지 않는다. 앞으로 real-checkpoint 수치를 보고할 때는 항상 그 라운드에서 실측한 값을 쓰고, 과거 worklog의 고정된 숫자와 다르다고 회귀로 오인하지 않는다.
- worklog 61에서 real 3k(cap 2048)가 `torch_gaussian_surface_region_formation.py`(2026-07-30, worklog 111-123)의 사전 존재 `KeyError`(`bridge_by_pair[(a, b)]`가 `consensus_by_pair`에는 있는 pair를 못 찾음)로 crash함을 발견했다 — 새 checkpoint 내용이 노출한 latent 결함이며 이번 작업 범위 밖이라 미수정. 3k(cap 1024)는 정상 동작한다. Region formation을 다루는 다음 작업자는 이 결함을 먼저 재현·수정 여부를 판단한다.
- **Known issue (worklog 64, 미수정)**: `gaussian_initialization_mode="baseline_compatible"`은 `_initialize_canonical`(production 기본 "initialize" 스케줄)에만 적용되고, `initialize_deferred`(`adc_post_commit`/`disabled` 스케줄)는 의도적으로 이 플래그를 무시하고 항상 `covariance_knn` 방식의 planar-surfel 초기값을 쓴다 — 그 경로의 첫 post-ADC surface 재구성이 모델 자신의 scale/rotation을 유일한 orientation evidence로 재사용하기 때문이다. 즉 `visible_nurbs_update_schedule=adc_post_commit`이나 `disabled`로 학습하면, `gaussian_initialization_mode=baseline_compatible`을 지정해도 실제로는 covariance-KNN 초기화가 적용되는 **의미 불일치**가 있다. Production 기본 스케줄("initialize")에는 영향 없음. deferred 스케줄을 실제로 쓰는 작업이 생기면 이 불일치를 먼저 해소해야 한다(현재는 미수정, 미착수).

다음 작업자는 먼저 이 문서와 `docs/worklogs/README.md`의 최신 인덱스, 그리고 가장 최근 작업로그(현재 `docs/worklogs/84_chart_unit_coherence_audit_and_evidence_scale_boundary.md`)를 읽고 이어서 작업한다. 과거 방향의 세부 기록은 필요할 때 Git history로만 조회한다.
