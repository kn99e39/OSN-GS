# Real-scene matched camera review

이 camera 폴더의 A-F는 동일한 frozen Gaussian render를 배경으로 한다. A는 Gaussian Scene, B는 clean oracle, C는 full-domain representative, D는 frozen support-constrained materialization, E/F는 oracle과 각 representative의 결합이다.

overlay는 review용이며 Gaussian row·checkpoint·renderer를 변경하지 않는다.
